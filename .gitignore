# deps & build
node_modules/
dist/
build/
coverage/
.turbo/
*.tsbuildinfo

# env & secrets
.env
.env.*
!.env.example
# ADR-030: apps/web/.env (public pk. Mapbox token) is deliberately UNTRACKED — GitHub
# push protection blocks Mapbox tokens. Recreate it per the README env table; staging
# receives it via rsync.

# claude code
.claude/worktrees/

# misc
*.log
.DS_Store

# playwright artifacts
apps/web/playwright-report/
apps/web/test-results/

# separate Lovable design project (deliberately untracked; see memory design-project)
orbetra_design/
infra/pgbackrest/storagebox_*
