# deps & build
node_modules/
dist/
build/
coverage/
.turbo/
*.tsbuildinfo

# env & secrets
.env
.env.*
!.env.example
# ADR-030: the web Mapbox token is a PUBLIC pk. token (URL-restricted) — committed deliberately

# claude code
.claude/worktrees/

# misc
*.log
.DS_Store

# playwright artifacts
apps/web/playwright-report/
apps/web/test-results/

# separate Lovable design project (deliberately untracked; see memory design-project)
orbetra_design/
infra/pgbackrest/storagebox_*
