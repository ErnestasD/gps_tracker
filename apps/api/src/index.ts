// @orbetra/api — stub (E01-1). Real implementation lands in its own story.
export const packageName = '@orbetra/api'

// E01-1 AC demo: deliberate violations, PR must go RED in CI
export const bad: number = "type error on purpose"
import '@prisma/client'
