{
  "summary": "Skeptical multi-round multi-agent audit of the whole affiliate platform (F1/F3/F4/F5) — data model, admin API, attribution, commission accrual, partner auth",
  "agentCount": 84,
  "logs": [
    "Round 1: 13 fresh findings -> adversarial verify",
    "Round 2: 11 fresh findings -> adversarial verify",
    "Round 3: 8 fresh findings -> adversarial verify"
  ],
  "result": {
    "confirmedCount": 29,
    "report": {
      "summary": "The affiliate/partner platform is functionally complete and its money-critical happy paths (single-payment accrual, per-invoice idempotency on sourceInvoiceId, no double-pay) are sound and tested. But the audit surfaced a consistent theme of missing durability and defensive rigor around money and identity: the commission ledger is not treated as an immutable financial record (it cascade-deletes with the tenant, its terms are read live instead of snapshotted despite an explicit spec mandate, and its status is freely reversible with no audit trail), and the two identity keys — referral codes and partner emails — are matched with case/pattern-insensitive lookups layered over case-sensitive unique indexes, producing genuine money-misrouting and login-lockout defects. A second theme is thin test coverage on exactly the load-bearing branches (recurring months-2..N accrual, the fault→500→Stripe-retry path, brute-force lockout, token invalidation), so several of these regressions would ship green. None of the defects breaks the core no-double-pay invariant, and most require a privileged platform_admin action or specific delivery timing, so the platform is shippable — but the ledger-integrity and code/email-matching items (the two high-severity findings plus the un-snapshotted terms and cascade-delete) should be fixed before real partner money moves, since they cause silent over-pay, mis-attribution, or unrecoverable loss of financial records.",
      "ranked": [
        {
          "severity": "high",
          "title": "Commission ledger is cascade-deleted when a tenant (or affiliate) is deleted",
          "file": "packages/db/prisma/migrations/20260803120000_affiliates/migration.sql:54,57",
          "why": "Both commissions FKs are ON DELETE CASCADE, and DELETE /v1/tenants/:id -> tenants.remove does an unconditional prisma.tenant.delete (packages/db/src/repos/tenants.ts:129) with no guard. Deleting a churned referred tenant silently destroys every commission row for it, including status='paid' (money already paid out) and status='pending' (money owed). The audit 'before' payload captures only the tenant row, so the financial trail is unrecoverable — you cannot reconcile Stripe payouts or honor outstanding amounts. Note the asymmetry: tenants.referredByAffiliateId is deliberately ON DELETE SET NULL, but the ledger itself has no such protection.",
          "fix": "Treat Commission as an immutable ledger: change both FKs to ON DELETE RESTRICT (block deletion while commissions exist) or ON DELETE SET NULL with nullable *Id columns plus denormalized affiliate/tenant identifiers retained on the row. Minimally, block hard tenant delete when commissions exist, mirroring the accounts-with-users guard from audit E1."
        },
        {
          "severity": "high",
          "title": "Referral-code lookup is an unescaped case-insensitive ILIKE over a case-sensitive unique index -> commission mis-attribution",
          "file": "packages/db/src/repos/affiliates.ts:115",
          "why": "getActiveByCode uses Prisma { equals: code, mode: 'insensitive' }, which compiles to WHERE code ILIKE $1 with the value bound UNESCAPED, while the DB unique index (migration L42) is a plain case-sensitive btree. Two failures result, both misrouting recurring commission money to the wrong partner: (a) an admin can create both `promo` and `PROMO` as distinct active rows, and findFirst (no orderBy) returns a nondeterministic one; (b) the ref regex /^[a-zA-Z0-9_-]{3,64}$/ (entities.ts:241) permits `_`, which ILIKE treats as a single-char wildcard — a public ?ref=AUTUMN_ matches AUTUMN1 and AUTUMNX (empirically reproduced), letting a prefix attribute to a code the tenant never referenced. crud.ts:1210 writes the matched id to referredByAffiliateId, and every invoice.payment_succeeded then accrues to that affiliate.",
          "fix": "Replace the insensitive ILIKE with genuine equality: raw SQL WHERE lower(code)=lower($1) backed by CREATE UNIQUE INDEX ON affiliates (lower(code)) (also closes the case-duplicate gap), normalize code on write, add an explicit orderBy for determinism, and drop `_` from the ref regex (auto-generated codes use an alnum-only alphabet)."
        },
        {
          "severity": "medium",
          "title": "Commission terms are read live, not snapshotted — editing commissionPct/commissionMonths reopens closed windows and re-prices history",
          "file": "packages/db/src/repos/affiliates.ts:159",
          "why": "accrueForPaidInvoice computes the window boundary addMonthsUtc(anchor, affiliate.commissionMonths) and amount floor(amountPaid * affiliate.commissionPct / 100) from the LIVE Affiliate row; Commission stores only amountCents/currency, with no rate_pct or base_amount_cents despite PROJECT_PLAN §6.9 (L232) mandating per-entry terms. Both fields are freely editable via PATCH /v1/affiliates/:id. Because the window is recomputed every invoice against an unchanged anchor, editing commissionMonths 6->24 makes a legitimately-closed window (e.g. 2026-01..2026-07) judge a 2027-04 invoice in-window and accrue a fresh commission ~9 months past the contracted term; raising commissionPct re-prices all still-in-window invoices with no record of the originally agreed rate.",
          "fix": "Snapshot terms at attribution/first accrual: add rate_pct and base_amount_cents columns to Commission (per §6.9) and pin a concrete windowEndsAt (or anchor+term) on the tenant/referral binding; have accrueForPaidInvoice use the pinned values for both amount and boundary instead of the live affiliate row."
        },
        {
          "severity": "medium",
          "title": "Commission window is anchored on the earliest commission row's insert time, not the tenant's true first payment",
          "file": "packages/db/src/repos/affiliates.ts:157",
          "why": "anchor = first?.createdAt ?? invoice.paidAt, where first is the earliest EXISTING commission row and createdAt is the DB insert clock — compared against Stripe invoice.paidAt (a different clock). There is no persisted first-payment time. Consequences: (1) createdAt is always >= paidAt, and out-of-order/retry-delayed webhook delivery drifts the anchor later, extending the window and over-paying; (2) a first invoice that floors to 0 (amountCents<=0 bail) or payments made while the affiliate is suspended leave no row, so the anchor jumps to a later accruing payment — the floored-zero case extends the earn window by ~one billing cycle past commissionMonths (genuine over-pay). Only tested for the in-order first-payment-accrues case. (The pure suspend->reactivate variant is closer to net-neutral, since the suspended months forfeit their own accruals, but the floored-zero and insert-clock drifts are real over-pays.)",
          "fix": "Persist a paidAt/first_paid_at (on Commission and/or the referral) at first invoice.payment_succeeded regardless of affiliate status, and anchor/order the window on MIN(paidAt), distinguishing 'no row because trial/$0' (start later, intended) from 'no row because suspended' (do not move the window)."
        },
        {
          "severity": "medium",
          "title": "Spec-mandated self-referral anti-fraud guard is entirely absent",
          "file": "apps/api/src/routes/crud.ts:1207",
          "why": "PROJECT_PLAN §6.9 (L230) mandates a self-referral guard ('affiliate cannot refer a tenant whose admin email domain matches the affiliate's'). No such check exists: POST /v1/tenants resolves ?ref via getActiveByCode and writes referredByAffiliateId with zero domain/self validation, and accrueForPaidInvoice pays whatever affiliate that id points to. An affiliate whose own tenant is provisioned under their code harvests commissionPct of their own subscription+usage spend for commissionMonths. The guard cannot even fire today because tenantCreateSchema carries no admin email at tenant-create time.",
          "fix": "Implement the §6.9 guard where the admin email becomes available (capture intended admin email on the lead/tenant-create body, or enforce when the first admin user is added): drop or refuse the referredByAffiliateId attribution when the tenant admin's email domain matches the affiliate's."
        },
        {
          "severity": "medium",
          "title": "past_due (grace) subscription clobbered by a late foreign-subscription deletion",
          "file": "packages/db/src/repos/tenants.ts:239",
          "why": "The per-subscription webhook guard defines 'a live sub to protect' as subscriptionStatus IN ['active','trialing'], but plans.ts (effectiveEntitlements) grants the full entitlement matrix during past_due/incomplete and billing.ts (hasLiveSubscription) treats past_due/unpaid/incomplete/paused as live. So when the tenant is past_due on sub_B, a late customer.subscription.deleted for a superseded sub_A (newer event.created) passes both the monotonic and per-sub guards and overwrites subscriptionStatus->canceled and stripeSubscriptionId->the dead sub_A. getEntitlements then drops to FLOOR, prematurely stripping a live, retrying, entitled subscription and corrupting the stored sub id. The P4 test only covers a current status of 'active'.",
          "fix": "Base the guard's 'live sub to protect' test on the same live-status set the rest of the system uses (billing.ts hasLiveSubscription / not-in-RESUBSCRIBABLE), so only a null/matching sub id or a genuinely re-subscribable state (canceled/incomplete_expired) lets a non-live incoming event mutate the tenant."
        },
        {
          "severity": "medium",
          "title": "Partner argon2id passwordHash leaked on every affiliate-returning platform route",
          "file": "apps/api/src/routes/crud.ts:1251",
          "why": "The affiliates repo returns the raw Prisma row (no select) from list/get/create/update, and the json/toJson helper only coerces BigInt/Decimal — it strips nothing. So GET /v1/affiliates, GET /v1/affiliates/:id, and PATCH /v1/affiliates/:id all serialize passwordHash verbatim once a partner has set a password (POST is null at create). The web AffiliateView type omits the field but a TS type does not remove it from the wire JSON, so the offline-crackable hash lands in the admin SPA response, network traces, and proxy logs. This directly contradicts the codebase's own posture: /v1/partner/me explicitly strips it ('never leak the hash').",
          "fix": "Introduce one passwordHash-free Affiliate DTO/select (id, name, email, code, commissionPct, commissionMonths, status, createdAt) applied to every affiliate-returning route (or select safe columns at the repo), and add a test asserting no response body contains passwordHash."
        },
        {
          "severity": "medium",
          "title": "Affiliate email stored verbatim: case-sensitive unique index + login lowercasing -> lockout and duplicate partners",
          "file": "packages/db/src/repos/affiliates.ts:121",
          "why": "create stores email: data.email unchanged (zod is plain z.string().email(), no transform), the unique index is a plain case-sensitive btree, but partner login lowercases the input (partner.ts:76) then does a case-sensitive findUnique (affiliates.ts:182). Result: any affiliate stored with an uppercase letter (e.g. 'Partner@Acme.com') can NEVER authenticate (login always downcases to a value the exact match can't find -> 401), yet appears fully provisioned because set-password is keyed by affiliateId. Separately, 'Foo@Bar.com' and 'foo@bar.com' both insert as distinct rows/codes for one person. The tenant users repo already normalizes on create (users.ts:51); affiliates is the lone deviation.",
          "fix": "Normalize email with .trim().toLowerCase() at the write boundary in create/update (mirroring users.ts:51), and back it with a citext column or UNIQUE (lower(email)) index so DB uniqueness matches the case-insensitive login lookup."
        },
        {
          "severity": "medium",
          "title": "setCommissionStatus has no transition state machine and no audit trail",
          "file": "packages/db/src/repos/affiliates.ts:176",
          "why": "setCommissionStatus does a blind update to any target status after only an existence check — pending->paid->pending->paid and void->paid are all permitted, and the route/schema validate only the enum. A commission marked 'paid' (the flag the platform pays partners against) can be flipped back to 'pending' (misclick or a re-flipping payout tool) and re-picked-up for a second payout, or a void after a refund/chargeback can be un-voided. Worse, createAffiliateRepo is built WITHOUT an AuditRepo (unlike createTenantRepo) and there is not one audit.record call in the file, so no row records who changed a money status or when — the whole affiliate write surface is untraceable while tenants.ts audits all its writes.",
          "fix": "Enforce an allowed-transition table (pending->paid, pending->void, paid->void for clawback; reject paid->pending and void->paid), guard with update WHERE {id, status: <expected>} for idempotency, and wire an AuditRepo into createAffiliateRepo so every commission status change and affiliate rate/status edit records an audit row."
        },
        {
          "severity": "medium",
          "title": "Recurring in-window accrual (months 2..N), the core partner payout, has zero test coverage",
          "file": "packages/db/__tests__/affiliates.spec.ts:129",
          "why": "Every accrual test uses either the first commission for a pair, a byte-identical duplicate of that invoice id, or an out-of-window payment (which expects null). No test ever feeds a SECOND, DISTINCT, in-window invoice for the same (affiliate,tenant) and asserts a second Commission row + summed payout. So a regression that treated 'a prior commission exists for this tenant' as 'already accrued -> return null' would silently zero out every month-2..12 commission — the entire recurring payout — while the full affiliate and billing suites stay green.",
          "fix": "Add a test: active affiliate, first invoice accrues, then a second distinct invoiceId with paidAt still inside commissionMonths accrues a second Commission; assert listCommissions length 2 and the summed amountCents. Mirror once at the webhook level in billing.spec with a second distinct invoice id."
        },
        {
          "severity": "medium",
          "title": "The genuine-DB-fault -> HTTP 500 -> Stripe-retry accrual path is untested",
          "file": "apps/api/__tests__/billing.spec.ts:346",
          "why": "The webhook design deliberately distinguishes a benign null (dedupe/nothing-owed -> 200 ack) from a genuine fault (throw -> return 500 so Stripe retries), and affiliates.ts documents that the repo 'no longer swallows non-dedupe errors'. But every invoice test exercises only the null->200 branches; none forces a non-dedupe throw and asserts 500. The old swallow pattern (catch { return null }) still lives one function up in accrueCommission, so a copy-paste refactor could make accrueForPaidInvoice swallow a transient DB error, the webhook would 200, Stripe would never retry, and the commission would be lost forever — with the suite staying green.",
          "fix": "Add a billing.spec case that stubs accrueForPaidInvoice to reject with a non-unique error for a referred tenant and asserts a 500; and a db-level affiliates.spec case asserting accrueForPaidInvoice rethrows a non-P2002 error rather than returning null."
        },
        {
          "severity": "medium",
          "title": "Partner login brute-force lockout (429) is completely untested",
          "file": "apps/api/src/routes/partner.ts:79",
          "why": "The partner login limiter has its own hardcoded LOGIN_RL_MAX=10 per IP+email (independent of the app's lockout config), with a Retry-After header, an INCR/EXPIRE Lua script, and a del-on-success reset. No test drives repeated failed logins to the 11th attempt; partner.spec only asserts a single 401 with distinct per-test emails, so the >=LOGIN_RL_MAX gate, the 429 response, the EXPIRE re-arm, and the reset are all uncovered on a public, unauthenticated endpoint. A regression in the lockKey composition, threshold, or TTL would silently remove brute-force protection and pass CI. The set-password limiter (REDEEM_RL_MAX=30) is likewise untested.",
          "fix": "Add a test that posts 10 wrong-password logins then asserts the 11th returns 429 with a Retry-After header, and that a correct login after reset still succeeds; add an analogous case for the set-password limiter."
        },
        {
          "severity": "medium",
          "title": "Sibling set-password-token invalidation has zero test coverage",
          "file": "apps/api/src/routes/partner.ts:111",
          "why": "invalidatePwTokens is the ONLY mechanism that kills a still-outstanding sibling set-password token after another is redeemed (consumePwToken only single-uses the redeemed token). No test issues two live tokens for one affiliate and asserts redeeming one kills the other — the single-use test only redeems the SAME token twice. A regression that dropped the call, broke its WHERE affiliateId/usedAt:null clause, or reordered it would leave a stale credential-setting link live (an account-seizure vector for anyone holding an earlier leaked invite) while every existing test passes.",
          "fix": "Add a test: create affiliate, createPwToken A and B, consume B via /v1/partner/set-password, then assert consumePwToken(A) returns null (token A invalidated)."
        },
        {
          "severity": "medium",
          "title": "'Nothing owed' bail writes no idempotency marker — Stripe redelivery re-evaluates against mutated state",
          "file": "packages/db/src/repos/affiliates.ts:153",
          "why": "When accrueForPaidInvoice bails (affiliate not active, out-of-window, floored to <=0) it returns null and persists NO row; the only durable marker is the commission row itself. Because affiliate.status and commissionPct are read live each call and there is no event-id dedup, a later manual resend or duplicate delivery of the SAME invoice re-runs the full resolution: an invoice skipped while the affiliate was suspended can accrue after reactivation, and a pct edited between deliveries determines the amount that sticks. The outcome for a fixed invoice is delivery-timing-dependent rather than deterministic. (A counter-view notes re-evaluation also correctly recovers the more common transient null where the tenant/customer mapping wasn't ready, so a naive sticky tombstone would over-correct — the fix must distinguish the two.)",
          "fix": "Record each invoice's disposition durably (a processed-invoice ledger, or a terminal 'skipped'/zero commission row keyed by sourceInvoiceId) ONLY for genuine business skips (suspended/out-of-window), so those are true no-ops on redelivery, while still allowing recovery of transient not-yet-mapped nulls."
        },
        {
          "severity": "low",
          "title": "Set-password token lifecycle: no revocation on re-issue or suspension, and not transactional",
          "file": "apps/api/src/routes/partner.ts:142",
          "why": "issuePartnerSetPwToken only inserts, so re-issuing an invite leaves prior 24h tokens independently redeemable (an admin who re-sends a link does not rotate the old one), and the set-password handler/consumePwToken/setPassword perform no affiliate-status check, so a suspended affiliate's password can still be set (200) and goes live on any later reactivation. Separately, the three-step redeem (consume -> setPassword -> invalidate siblings) is not wrapped in a transaction: a fault between setPassword and invalidatePwTokens leaves the password changed AND sibling tokens live until TTL, and returns 500 on a committed change. Impact is bounded because login/partnerAuth require status='active', so a suspended partner cannot authenticate meanwhile.",
          "fix": "In issuePartnerSetPwToken, call invalidatePwTokens first so a fresh link supersedes all prior ones (single-outstanding-token invariant); gate consumePwToken/setPassword on status!=='suspended'; and run consume+setPassword+invalidate inside one prisma.$transaction so a fault rolls the whole redemption back."
        },
        {
          "severity": "low",
          "title": "Partner password reset does not revoke outstanding access tokens",
          "file": "apps/api/src/routes/partner.ts:58",
          "why": "The partner access token is a stateless HS256 JWT (8h TTL, no refresh, no revocation channel); partnerAuth validates only signature/exp plus a live status='active' recheck, with no iat/revocation check, and setPassword writes no revocation marker. So after a partner (via the admin-conveyed reset link) changes a compromised password, an attacker's already-minted token keeps reading GET /v1/partner/me and /commissions for up to the remaining 8h, with no partner-facing logout to shorten it. The surface is read-only own-data, hence low.",
          "fix": "On setPassword bump a per-affiliate revocation marker (passwordChangedAt/tokenEpoch column or a Redis key), and have partnerAuth reject any token whose iat predates it — mirroring the tenant revokedAfter(iat) check; optionally shorten the TTL and add a partner logout."
        },
        {
          "severity": "low",
          "title": "commissionPct accepts >2 decimal places; DECIMAL(5,2) silently rounds the effective rate",
          "file": "packages/shared/src/entities.ts:245",
          "why": "commissionPctSchema is z.number().min(0).max(100) with no step constraint, so 15.555 validates and 200s, but the DECIMAL(5,2) column silently rounds it to 15.56 (Postgres does not error on scale overflow). accrueForPaidInvoice then pays at the rounded rate, so on a €100 invoice the partner accrues 1556 instead of the intended 1555 cents — a systematic per-invoice divergence the admin can't see because the UI/API echo the rounded stored value. Not a double-pay (accrual is deterministic against the stored rate), just entered-vs-effective drift.",
          "fix": "Add .multipleOf(0.01) to commissionPctSchema so a rate DECIMAL(5,2) cannot represent is rejected at the edge rather than silently rounded."
        },
        {
          "severity": "low",
          "title": "No index supports the hot-path window-anchor lookup on the synchronous webhook",
          "file": "packages/db/src/repos/affiliates.ts:157",
          "why": "Every invoice.payment_succeeded runs findFirst({ where:{affiliateId,tenantId}, orderBy:{createdAt:'asc'} }) to find the anchor, but commissions has only unique(sourceInvoiceId) and @@index([affiliateId,status]). With no status predicate, Postgres seeks the affiliateId prefix then filters tenantId and sorts createdAt in memory, scanning all of a high-volume affiliate's commission rows (which grow as referred-tenants × commissionMonths and are never deleted) on the Stripe-facing path.",
          "fix": "Add @@index([affiliateId, tenantId, createdAt]) in a new append-only migration so the anchor findFirst becomes an index-only seek to the first row."
        },
        {
          "severity": "low",
          "title": "Monotonic webhook guard uses strict lt against second-granular event.created — distinct same-second events drop",
          "file": "packages/db/src/repos/tenants.ts:230",
          "why": "applySubscriptionEvent orders writes by lastBillingEventAt { lt: eventAt } and eventAt derives from Stripe event.created (one-second granularity), with the code equating an equal eventAt to a replay. Two genuinely distinct same-second events for one customer (e.g. subscription.created(incomplete) + subscription.updated(active) at checkout, or two rapid plan-change updates) collapse to first-applied-wins: whichever lands first advances lastBillingEventAt to T, the second matches zero rows and is silently dropped, potentially leaving the tenant stuck at 'incomplete' and off listActiveSubscribers until a later out-of-second event. Self-heals on the next event and doesn't touch commission money, hence low.",
          "fix": "Don't rely on second-granular event.created for ordering distinct events: use lte plus an explicit event-id idempotency table (so true replays are caught by id, not by equal timestamp), or order by Stripe's strictly-increasing event id."
        },
        {
          "severity": "low",
          "title": "Assorted test-coverage and UI gaps around commission guards, the void state, and the invoice extractor",
          "file": "packages/db/__tests__/affiliates.spec.ts:150",
          "why": "Four small but real gaps on otherwise-correct code: (1) the 'floors a fractional commission rate' test uses amountPaidCents=10000 * 33.33% = exactly 3333.0, so floor/round/ceil are indistinguishable — a Math.floor->round regression that over-pays a cent passes unchanged; (2) the guard amountCents<=0 (affiliates.ts:161) and billing.ts amount_paid<=0 are never exercised — the test titled '...or a $0 invoice' never feeds a $0/floored invoice; (3) the 'void' commission status is a live API capability (PATCH /v1/commissions/:id) with no test and no UI affordance, so a refund-clawback path is unguarded and unusable from the dashboard; (4) paidInvoiceFrom's null-return branches (missing customer/id, amount_paid<=0) and its 'eur' currency default are never hit through the webhook — every test feeds a well-formed positive invoice.",
          "fix": "Add: an accrual assertion with a fractional-cent input (e.g. amountPaidCents=10_050 @33.33% -> assert 3349, proving floor); a $0/floored-to-zero invoice case (repo + webhook) asserting null/200 and no row; a PATCH-to-'void' test plus a void action in CommissionsPanel; and webhook cases with amount_paid 0/negative, missing customer/id, and omitted currency."
        }
      ]
    },
    "confirmed": [
      {
        "severity": "medium",
        "category": "money-correctness",
        "file": "packages/db/src/repos/affiliates.ts",
        "line": 157,
        "title": "Commission window anchor drifts later on suspend->reactivate -> overpay past commissionMonths",
        "description": "The commissionMonths window is anchored on the earliest EXISTING commission ROW for (affiliate, tenant): anchor = first?.createdAt ?? invoice.paidAt (affiliates.ts:157-159). The intent (comments L154-156, test L129) is \"commissionMonths from the tenant's FIRST payment\". But when the affiliate is suspended during the tenant's early paid invoices, accrueForPaidInvoice bails at L153 (status !== 'active') and writes NO commission row. When the affiliate is later reactivated, the FIRST surviving commission row is a much later payment, so the window is anchored on that later date instead of the true first payment — silently extending the earn window.",
        "failureScenario": "commissionMonths = 12, tenant pays monthly. Affiliate is suspended for months 1-3 (three paid invoices produce no rows). Reactivated at month 4 -> first commission row's createdAt = month 4 -> window = month 4..16. The affiliate now accrues on months 13-16, which are OUTSIDE the intended month-1..13 window: ~3-4 extra monthly commissions the platform never owed. The existing test only exercises the trial ($0-first-invoice) case, not suspend/reactivate, so this passes CI.",
        "suggestedFix": "Anchor the window on the tenant's actual first payment, not the first accrued row — e.g. persist a first_paid_at on the tenant (or referral) at first invoice.payment_succeeded regardless of affiliate status, and measure addMonthsUtc(firstPaidAt, commissionMonths) against it. Distinguish 'no row because trial' (start window later, intended) from 'no row because affiliate was suspended' (do not move the window).",
        "verifierNotes": [
          "CONFIRMED: Confirmed against real code in packages/db/src/repos/affiliates.ts. The commission window anchor at L157-159 is `first?.createdAt ?? invoice.paidAt`, where `first` is the earliest EXISTING Commission row for (affiliate, tenant). The suspended-affiliate bail at L153 returns null BEFORE writing a row, so payments received while the affiliate is suspended leave no row. If an affiliate is suspended across a referred tenant's earliest paid invoices and reactivated later, the earliest surviving Commission row (hence the anchor) is that later payment, not the true first payment.\n\nTrace (commissionMonths=12, monthly): suspended months 1-3 → no rows; reactivated month 4 → `first`=null → anchor=month 4 → window month4..month16, accruing months 4-16 (13 commissions). The interface's documented contract (L90-91) states the window is \"measured from the tenant's FIRST payment,\" which would give month1..month13 and accrual only for months 4-13 (10 commissions). Months 14-16 (~3 commissions) are paid past the intended cutoff = overpay.\n\nReachability is solid: update() (L132-136) permits arbitrary status transitions (suspend→reactivate), the tenant's referredByAffiliateId is fixed at signup and unaffected by suspension, and billing.ts adds no external window guard (all window math is in accrueForPaidInvoice). The tests do not cover this: the \"windows from FIRST payment\" test only exercises the trial ($0/delayed-first-invoice) case, and the suspended test only asserts a single suspended payment yields null without ever reactivating and re-checking the anchor. So the drift passes CI.\n\nThe only nuance is semantic: comment L154 says \"FIRST accrued payment\" (first ROW), which the code implements, while the interface doc L90 and test title say \"FIRST payment.\" The code contradicts its own documented external contract under suspend-reactivate, silently extending the earn window and overpaying. The mechanism, line references, failure scenario, and money impact in the claim all match the actual code. This is a genuine money-correctness defect at medium severity.",
          "REJECTED: The anchor-drift behavior is real (window anchors on the earliest EXISTING commission row's createdAt, and suspend/pending across the tenant's first paid invoices writes no rows, moving the anchor later on reactivation — affiliates.ts:157-159). However, the claimed money-correctness harm (\"overpay past commissionMonths / 3-4 extra commissions the platform never owed\") does NOT occur in net terms. commissionMonths bounds the LENGTH of a calendar window (addMonthsUtc, L159); the number of monthly invoices inside a fixed-length window is the same whether it sits at months 1-13 or drifts to 4-16. In the claim's scenario the affiliate accrues ZERO for the suspended months 1-3, then 13 accruals for months 4-16 — total 13, identical to the un-suspended case (months 1-13). The claim's arithmetic counts months 14-16 as extra while ignoring the three forfeited zero-pay suspended months, so there is no net double-pay and no net over-count; total platform exposure per tenant stays capped at ~commissionMonths. Moreover the drift cannot compound: the anchor is orderBy createdAt asc (earliest row), so once any commission row exists it is pinned forever; the effect only occurs before the very first accrual. Finally the anchoring is documented deliberate design (affiliates.ts:154-156, 'a trial must not eat the window; the earliest existing commission is that anchor'), consistent with the pending->approved flow (affiliates default to status 'pending'). The only genuine issue is a wording mismatch between schema.prisma:133 ('from the tenant's first payment') and the code's 'first accrued payment' — a window-timing/policy ambiguity, not an over- or under-payment. The specific defect asserted does not materialize."
        ]
      },
      {
        "severity": "medium",
        "category": "auth-correctness",
        "file": "packages/db/src/repos/affiliates.ts",
        "line": 121,
        "title": "Affiliate email stored verbatim but partner login lowercases + looks up case-sensitively → mixed-case affiliates can never log in",
        "description": "The partner login path normalizes the submitted email to lowercase (apps/api/src/routes/partner.ts:76, `parsed.data.email.trim().toLowerCase()`) and then calls `findByEmailForAuth` (partner.ts:84), which does a case-SENSITIVE Prisma `findUnique({ where: { email } })` (affiliates.ts:182). But the affiliate create path stores the email exactly as supplied — `email: data.email` (affiliates.ts:121) — with no normalization, and neither the crud.ts POST /v1/affiliates handler (crud.ts:1259-1264) nor the zod `affiliateCreateSchema` (packages/shared/src/entities.ts:271, plain `z.string().email()`) lowercases it. This diverges from the tenant users repo, which correctly normalizes on create (packages/db/src/repos/users.ts:51, `email: data.email.trim().toLowerCase()`). There is no citext/unique-lower index on the affiliate email column (confirmed: no `citext`/`lower(` in schema.prisma or either affiliate migration). Result: any affiliate whose stored email contains an uppercase character is permanently unable to authenticate to the partner portal — the login always downcases to a value that the case-sensitive `findUnique` never matches, so `findByEmailForAuth` returns null and login returns 401 'invalid credentials'. The set-password flow still succeeds (it is keyed by affiliateId via the one-time token, not by email — partner.ts:107-109), so the partner looks fully provisioned yet can never sign in. The bug is masked by tests: every email in apps/api/__tests__/partner.spec.ts and packages/db/__tests__/affiliates.spec.ts is already lowercase (e.g. 'acme@partner.co'), so no test exercises a mixed-case address.",
        "failureScenario": "platform_admin creates an affiliate with email 'Partner@Acme.com' (POST /v1/affiliates) and issues a set-password token; the partner redeems it and sets a password successfully. The partner then POSTs /v1/partner/login with email 'Partner@Acme.com' (or 'partner@acme.com'). partner.ts lowercases to 'partner@acme.com'; findByEmailForAuth runs findUnique on the case-sensitive email column where the stored value is 'Partner@Acme.com' → no row → partner===null → 401 'invalid credentials'. The partner is locked out of the portal with no self-service recovery.",
        "suggestedFix": "Normalize the affiliate email to `.trim().toLowerCase()` at the write boundary in the affiliates repo `create` (affiliates.ts:121), mirroring users.ts:51, so the stored value matches what the login path queries. (Optionally also normalize in `findByEmailForAuth` input for defense in depth, and consider a lower(email) unique index / citext to keep case-uniqueness consistent with the login lookup.)",
        "verifierNotes": [
          "CONFIRMED: CONFIRMED. Every link in the claimed chain is real code:\n\n1. Store verbatim — affiliates.ts:121 `create` sets `email: data.email` with no normalization. The crud.ts POST /v1/affiliates handler passes `...data` straight through (spreads the parsed body), doing no lowercasing. And `affiliateCreateSchema` (entities.ts:250) is `email: z.string().email().max(320)` — no `.transform`/`.toLowerCase()`. So a mixed-case email like 'Partner@Acme.com' is persisted exactly as typed.\n\n2. Login lowercases — partner.ts:76 `const email = parsed.data.email.trim().toLowerCase()`, then partner.ts:84 `deps.db.affiliates.findByEmailForAuth(email)`.\n\n3. Lookup is case-sensitive — affiliates.ts:181-182 `findByEmailForAuth: (email) => prisma.affiliate.findUnique({ where: { email }, ... })`. `findUnique` on a plain `String @unique` column is an exact-equality match. Notably, `getActiveByCode` (line 115) deliberately uses `{ equals: code, mode: 'insensitive' }` for the code column, but `findByEmailForAuth` uses NO such insensitive mode — a telling asymmetry.\n\n4. No citext / lower() index — schema.prisma:128 `email String @unique` (plain TEXT), migration 20260803120000_affiliates/migration.sql:14 `\"email\" TEXT NOT NULL` and :39 `CREATE UNIQUE INDEX \"affiliates_email_key\" ON \"affiliates\"(\"email\")`. Postgres TEXT equality under the default collation is case-sensitive, so 'partner@acme.com' ≠ 'Partner@Acme.com'.\n\n5. Divergence from tenant users — users.ts:51 normalizes on create: `email: data.email.trim().toLowerCase()`. The affiliate repo does not, confirming the inconsistency.\n\nResult: an affiliate stored with any uppercase char in its email can never authenticate — login always downcases to a value the case-sensitive `findUnique` cannot match, returning null → 401. set-password succeeds because it is keyed by affiliateId via the one-time token (consumePwToken → setPassword by id, partner.ts:107-109), so the partner appears provisioned yet is permanently locked out with no self-service recovery. The scenario ('Partner@Acme.com') is fully reachable through the real platform_admin create route; nothing in the create path or DB layer normalizes or case-folds. No test exercises a mixed-case address (all fixtures use lowercase like 'acme@partner.co'), so it is not caught. This is a genuine, reproducible auth-correctness defect.",
          "CONFIRMED: Every link in the claimed chain is verified in real code. Login normalizes to lowercase (apps/api/src/routes/partner.ts:76) then looks up via findByEmailForAuth, which is a case-SENSITIVE Prisma findUnique on the email column (packages/db/src/repos/affiliates.ts:182). The create path stores the email verbatim (affiliates.ts:121, `email: data.email`); the crud.ts POST handler passes the parsed body straight through with no lowercasing (crud.ts:1262); and the zod affiliateCreateSchema is a plain `z.string().email().max(320)` with no transform (packages/shared/src/entities.ts:249) — and z.string().email() accepts uppercase. The DB has no case folding: the affiliate migration declares `\"email\" TEXT NOT NULL` with a plain unique btree index (20260803120000_affiliates/migration.sql:14,39); there is no citext or lower() expression index in schema.prisma or either affiliate migration, and Postgres TEXT equality is case-sensitive. Therefore an affiliate created with any uppercase character in the email (e.g. 'Partner@Acme.com') can never authenticate: login downcases to 'partner@acme.com', findUnique against the stored 'Partner@Acme.com' returns no row, partner===null → 401 'invalid credentials'. The set-password flow still succeeds because it is keyed by affiliateId via the one-time token, not by email (partner.ts:107-109), so the partner looks provisioned yet is locked out with no self-service recovery. Notably, the same repo's getActiveByCode DOES use mode:'insensitive' (affiliates.ts:115) and the tenant users repo normalizes on create (users.ts:51, `.trim().toLowerCase()`), confirming both the correct pattern and its omission here. Tests miss it because all fixture emails are already lowercase. I tried to refute via a hidden zod transform, a body()-helper normalization, a citext/collation, and an alternate login path — none exist. Real, reachable defect."
        ]
      },
      {
        "severity": "medium",
        "category": "sensitive-data-exposure",
        "file": "apps/api/src/routes/crud.ts",
        "line": 1251,
        "title": "Platform affiliate GET routes leak partner argon2id passwordHash over the API",
        "description": "The `Affiliate` Prisma model carries `passwordHash String?` (packages/db/prisma/schema.prisma:138), the argon2id hash for partner self-service login. The platform routes return the raw affiliate row with no field allowlist: `GET /v1/affiliates` -> `db.affiliates.list()` (crud.ts:1251) and `GET /v1/affiliates/:id` -> `db.affiliates.get(id)` (crud.ts:1254-1255). Both repo methods (`prisma.affiliate.findMany`/`findUnique`, affiliates.ts:112-113) select every column, and the `json`/`toJson` helper (crud.ts:178-181) only coerces BigInt/Decimal — it does NOT strip fields. So `passwordHash` is serialized verbatim into the HTTP response body sent to the admin browser (POST create at 1264-1265 and PATCH at 1282-1283 return the row too). The web `AffiliateView` type (apps/web/src/lib/affiliates.ts:11-20) omits the field, but a TypeScript type does not remove it from the JSON on the wire; the hash lands in browser memory, network traces, and any logging/XSS reach. This directly contradicts the codebase's own stated posture: the partner `/v1/partner/me` handler explicitly strips it ('never leak the hash', partner.ts:120-125). Even though the audience is a trusted platform_admin, exposing credential material is a real leak and inconsistent with the rest of the surface.",
        "failureScenario": "A partner sets a self-service password (POST /v1/partner/set-password), giving their affiliate row a non-null passwordHash. A platform_admin then loads the affiliates admin page, which calls GET /v1/affiliates. The JSON response for every affiliate now contains `\"passwordHash\":\"$argon2id$...\"`. Anyone who can read that admin's network traffic, browser history/memory, an intermediary proxy log, or exploits an XSS in the admin SPA harvests every partner's password hash — offline-crackable credential material that should never leave the DB.",
        "suggestedFix": "Add a `select` (or an explicit view mapper) to `affiliates.list`/`affiliates.get` in packages/db/src/repos/affiliates.ts that omits `passwordHash`, or map to a passwordHash-free DTO in the crud.ts handlers before `json(...)` — mirroring the field allowlist already used in partner.ts:/me. Add a test asserting the platform GET responses never contain `passwordHash`.",
        "verifierNotes": [
          "CONFIRMED: Verified against actual code. schema.prisma:138 defines Affiliate.passwordHash String? (argon2id partner login hash). affiliates.ts:112-113 list()/get() call prisma.affiliate.findMany/findUnique with NO select, returning the full row including passwordHash; create (118) and update (135) likewise. crud.ts:1251/1254-1255/1265/1283 serialize the raw row via json→toJson (crud.ts:173-181), which only coerces bigint and does not strip any field, so passwordHash is emitted verbatim once non-null. Refutation failed: no global sanitizer exists (grep of crud.ts/main.ts), no test asserts the field absent (affiliates.spec has no such assertion), and the codebase's own convention contradicts this route — users.ts:7,32,45-46 uses an explicit VIEW select ('passwordHash NEVER leaves the repo') and partner.ts:120 manually strips the hash ('never leak the hash'). The affiliates platform routes are the lone deviation. Audience is platform_admin (trusted) and the field is null until a partner sets a password, which correctly places this at medium severity, but the leak is real once a password is set: the argon2id hash lands in the admin SPA response body, exposed to network traces, browser memory, proxy logs, and XSS reach.",
          "CONFIRMED: The defect is real and reachable.\n\nEvidence chain:\n1. Schema: `Affiliate.passwordHash String?` is the argon2id hash for partner self-service login (packages/db/prisma/schema.prisma:138).\n2. Repo: `list: () => prisma.affiliate.findMany({ orderBy: { createdAt: 'desc' } })` and `get: (id) => prisma.affiliate.findUnique({ where: { id } })` (affiliates.ts:112-113) use NO `select`/`omit`, so Prisma returns every column including `passwordHash`. Same for `create` (line 118) and `update` (line 135), which also return full rows.\n3. Routes: `GET /v1/affiliates` -> `json(c, await db.affiliates.list())` (crud.ts:1251); `GET /v1/affiliates/:id` -> `json(c, row)` (crud.ts:1254-1255); POST create -> `json(c, created, 201)` (crud.ts:1264-1265); PATCH -> `json(c, row)` (crud.ts:1282-1283). All pass the raw row.\n4. Serializer: `toJson`/`json` (crud.ts:173-181) only coerces `bigint` to string via JSON.stringify replacer; it performs NO field allowlisting or stripping. So `passwordHash` is serialized verbatim into the HTTP body.\n5. No global middleware or affiliate-specific view strips the field — grep of crud.ts shows no affiliate select/omit/view/sanitize; the only `passwordHash` references in crud.ts are for USER creation (lines 269, 310), unrelated to affiliate responses.\n\nReachability: A partner setting a password (POST /v1/partner/set-password consuming a token) makes `passwordHash` non-null via `setPassword`. Any subsequent platform_admin GET on /v1/affiliates then includes `\"passwordHash\":\"$argon2id$...\"` in the JSON. Confirmed by the DB test at packages/db/__tests__/affiliates.spec.ts:96 that `findByEmailForAuth` returns the stored hash after setPassword.\n\nInconsistency confirmed: the partner surface deliberately strips the hash — `/v1/partner/me` builds an explicit field allowlist with the comment \"never leak the hash\" (partner.ts:120-125), and a test asserts `'passwordHash' in body === false` (partner.spec.ts:106). No equivalent protection or test exists for the platform affiliate GET/POST/PATCH routes; the web `AffiliateView` type omits the field but a TS type does not remove it from the wire JSON.\n\nMitigating context (justifies the medium, not lower): the routes are `scopeClass: 'platform'`, reachable only by platform_admin, a trusted internal audience. But exposing offline-crackable credential material over the API — into browser memory, network traces, proxy logs, and any XSS reach in the admin SPA — is a genuine sensitive-data-exposure leak and directly contradicts the codebase's own stated posture. The claimed file:line and mechanism are all accurate."
        ]
      },
      {
        "severity": "medium",
        "category": "ordering-idempotency",
        "file": "packages/db/src/repos/affiliates.ts",
        "line": 158,
        "title": "Commission window anchored on DB insert clock (createdAt), not payment time — out-of-order / delayed delivery drifts the anchor later and over-pays",
        "description": "accrueForPaidInvoice computes the 12-month window from `first?.createdAt ?? invoice.paidAt` (L157-159): the anchor for every invoice after the first is the earliest existing commission's `createdAt` — a DB INSERT timestamp — while the boundary is compared against Stripe `invoice.paidAt` (the real payment time). Two different clocks. The Commission table has no paidAt column (schema.prisma L174-190, migration L25-36), so the true earliest PAYMENT time is unrecoverable; createdAt is used as a proxy that is only ≈ correct under prompt, in-order delivery. Stripe delivery is at-least-once and unordered, and retries after an outage can land commission rows hours/days after the payment, so the anchor systematically drifts LATER than the first real payment → the window is EXTENDED → commissions keep accruing past the intended cutoff (over-pay). floor() and the unique index don't help here because each drifted invoice is a distinct, legitimately-created row.",
        "failureScenario": "Referred tenant has two in-window invoices delivered out of order: INV_FEB (paidAt = Feb 1, the true first payment) and INV_MAR (paidAt = Mar 1). Stripe delivers INV_MAR first; its commission row is inserted with createdAt ≈ Mar 2. Then INV_FEB arrives: `first` now resolves to the INV_MAR row, so anchor = Mar 2 (its createdAt), not Feb 1. The window becomes Mar 2 + commissionMonths instead of Feb 1 + commissionMonths — every subsequent monthly invoice accrues one extra month of commission beyond the intended 12-months-from-first-payment, a systematic over-pay. The same drift occurs whenever the first accrued invoice's webhook is delayed (retry after an outage / backfill replay): createdAt >> paidAt extends the window by the delay.",
        "suggestedFix": "Add a `paidAt TIMESTAMPTZ` column to commissions, persist invoice.paidAt on each accrual, and anchor the window on MIN(paidAt) for (affiliate, tenant) rather than MIN(createdAt). That makes the window independent of insert time and delivery order.",
        "verifierNotes": [
          "CONFIRMED: Every factual pillar of the claim is confirmed in real code.\n\n1. Two clocks. In `accrueForPaidInvoice` (packages/db/src/repos/affiliates.ts ~L155-160): `const first = await prisma.commission.findFirst({ ... orderBy: { createdAt: 'asc' }, select: { createdAt: true } })`, then `const anchor = first?.createdAt ?? invoice.paidAt`, and the window check `if (invoice.paidAt > addMonthsUtc(anchor, affiliate.commissionMonths)) return null`. So the boundary compares `invoice.paidAt` against `addMonthsUtc(commission.createdAt, months)`.\n\n2. createdAt IS the DB insert clock. Schema Commission (schema.prisma L173-190) and migration (L25-36) have NO paidAt column — only `createdAt DateTime @default(now())`. The create call in accrueForPaidInvoice sets only affiliateId/tenantId/amountCents/currency/sourceInvoiceId, never createdAt, so createdAt = DB now() at insert time. The true first-payment time is genuinely unrecoverable from the table.\n\n3. invoice.paidAt is the real Stripe payment clock. billing.ts L244: `paidInvoiceFrom(event.data.object, new Date(event.created * 1000))` — Stripe event time, a different clock from DB now().\n\n4. Over-pay direction and reachability. Because `first` is selected by earliest createdAt, out-of-order or delayed delivery makes the anchor drift LATER than the true first payment, extending the window (`paidAt > anchor+months` fires later), so extra commissions accrue. The unique index on sourceInvoiceId and floor() don't help — each drifted invoice is a distinct, legitimately-created row.\n\nThe scenario is realistic, not hypothetical: Stripe explicitly gives at-least-once, unordered delivery, and billing.ts returns non-2xx on fault to force retries, so a first-invoice webhook can land hours/days after paidAt (outage/backfill), and a later invoice can be processed before an earlier one. Tellingly, the same codebase defends subscription events against exactly this via a per-customer monotonic `event.created` guard in applySubscriptionEvent — but the commission window has NO equivalent guard, and the test suite (affiliates.spec.ts L129-140) only exercises in-order delivery and even anchors its own \"late\" payment off `c1.createdAt`, so no test covers the out-of-order/delayed case.\n\nThere is also a persistent smaller manifestation even under prompt in-order delivery: createdAt is always ≥ paidAt by processing latency, so the window end is always slightly later than paidAt+months; combined with the strict `>` boundary this can pull an anniversary invoice into the window. Either way the defect is real, reachable, and money-affecting (over-pay), with no code or test mitigating it.\n\nAdversarial refutation attempts all fail: (a) \"Stripe delivers in order\" — false; the repo itself guards subscriptions against reordering. (b) \"earliest createdAt always = earliest paidAt\" — only under in-order prompt delivery, which is not guaranteed. (c) \"drift too small to cross a boundary\" — Stripe anniversary invoices bill near the same wall-clock instant, so even small drift plus strict `>` can tip an extra accrual, and outage-delayed first invoices drift by days.\n\nSeverity medium is fair: bounded over-pay under specific but genuinely-occurring delivery conditions.",
          "CONFIRMED: Verified against source, schema, migration, and test. affiliates.ts:157-159 anchors the commission window on the earliest existing commission's `createdAt`, compared against the incoming Stripe `invoice.paidAt` — two different clocks, exactly as claimed. schema.prisma (Commission) and 20260803120000_affiliates/migration.sql confirm `createdAt TIMESTAMPTZ DEFAULT CURRENT_TIMESTAMP` and NO paidAt column; create() (L166-168) never overrides createdAt, so it is the DB insert time and the true first-payment time is unrecoverable — both sub-claims hold. billing.ts:244 feeds paidAt = event.created (a real payment-event clock), so the mismatch is live on the webhook path. Direction is genuine: an insert can never precede its payment, so stored createdAt >= true first paidAt, always extending the window end -> systematic over-pay. The test at affiliates.spec.ts:135-139 actually proves the code anchors on insert time, not payment time: it sets firstPaidAt = now+2mo but the stored c1.createdAt ~= now and it computes the window boundary from c1.createdAt — a 2-month discrepancy baked into the test yet undetected (it only probes 7 months out), and the comment even mislabels createdAt as the payment anchor. Caveat: the headline failureScenario (INV_MAR delivered before INV_FEB, a full extra month) overstates magnitude — Stripe's retry backoff caps ~72h and its out-of-order guarantee only concerns near-simultaneous events, so a month-long reorder needs operational backfill/replay, not normal delivery. Realistic drift is bounded by webhook delay (seconds normally, up to a few days after an outage/retry, arbitrary on manual replay). But the defect itself — wrong anchor clock, unrecoverable payment time, systematic boundary over-pay under any payment-to-insert delay — is real, verified, and can actually occur; medium severity is fair. Fix: persist the payment time (dedicated paidAt/anchor column) and anchor/order on it instead of createdAt."
        ]
      },
      {
        "severity": "medium",
        "category": "idempotency",
        "file": "packages/db/src/repos/affiliates.ts",
        "line": 153,
        "title": "\"Nothing owed\" bails write no idempotency marker, so a Stripe redelivery re-evaluates the same invoice against mutated state",
        "description": "When accrueForPaidInvoice bails without owing (affiliate not active L153, out-of-window L159, amount floored to <=0 L161) it returns null and persists NO row — the invoice id is only recorded when a commission is actually created (L166-168). The billing webhook acks these null cases with 200 (billing.ts L245-257). Because there is no negative/tombstone marker, a later Stripe redelivery or manual resend of the SAME invoice (at-least-once delivery; Stripe also retries and lets you resend events) re-runs the full resolution against whatever the state is at redelivery time. So the accrual outcome for one fixed invoice is delivery-timing-dependent rather than idempotent: it can flip from no-accrual to accrual, or accrue at a different commissionPct, depending on which delivery wins.",
        "failureScenario": "INV1 for a referred tenant is delivered while the affiliate is status='suspended' → L153 bails, null, 200 ack, no row. Admin later flips the affiliate back to 'active'. Stripe redelivers INV1 (a normal duplicate / retry). This time L153 passes, the window check passes, and a commission is created — the platform pays for a payment that on first delivery it decided to skip. Symmetrically, if commissionPct is edited between two deliveries of an invoice whose first delivery floored to 0 (or was suspended), the amount that finally sticks is whichever delivery happens to create the row first. Outcome depends purely on Stripe's redelivery behavior, not on a deterministic rule.",
        "suggestedFix": "Decide the invoice's disposition once and record it durably (e.g. insert the commission row up-front in a 'skipped'/zero terminal state keyed by sourceInvoiceId, or a separate processed-invoice ledger), so a redelivery is a true no-op regardless of interim state changes to affiliate.status or commissionPct.",
        "verifierNotes": [
          "CONFIRMED: CONFIRMED mechanism, grounded in code. In packages/db/src/repos/affiliates.ts:146-173, accrueForPaidInvoice's three \"nothing owed\" exits (L151 not-referred, L153 status!=='active', L159 out-of-window, L161 amount<=0) return null and persist NO row; the unique-sourceInvoiceId idempotency marker is written only when a commission is actually created (L166-168). apps/api/src/routes/billing.ts:239-261 has no Stripe-event-id dedup — the invoice.payment_succeeded branch re-invokes accrueForPaidInvoice on every delivery and acks null with 200. The gate inputs affiliate.status (L152-153) and affiliate.commissionPct (L160) are read live each call, so a redelivery/resend of the same fixed invoice re-evaluates against whatever state exists then. Trigger is reachable: Stripe manual event resend (supported operator action) and documented rare duplicate delivery; combined with a normal admin state change (suspended->active, or raising a floored-to-0 pct) the same invoice flips from no-accrual to accrual. The test at packages/db/__tests__/affiliates.spec.ts:160-174 asserts suspended=>null/no row but does NOT cover reactivation+redelivery, so nothing handles the flip. Caveats that lower severity but do not refute: automatic Stripe retries only fire on non-2xx and the first delivery is 200, so the claim's appeal to automatic retries is imprecise — the realistic trigger is deliberate manual resend or a rare duplicate coinciding with an intervening admin mutation; and under stable state the handler IS idempotent (the non-determinism only appears across state changes). Still, the code genuinely lacks any durable negative/tombstone marker and any event-level dedup, so the described delivery-timing-dependent outcome for a fixed invoice can actually occur and is untested. Not REJECTED (no code/test prevents it) and provable from code rather than merely speculative, so CONFIRMED.",
          "PLAUSIBLE: MECHANISM IS REAL (verified in code). In accrueForPaidInvoice (packages/db/src/repos/affiliates.ts:146-173) every \"nothing owed\" exit — not referred (L151), affiliate not active (L153), out-of-window (L159), floored amount <=0 (L161) — returns null and persists NO row. The only durable marker is the Commission row itself, whose sourceInvoiceId unique index (L166-168) provides idempotency ONLY for the positive case. billing.ts:245-257 acks a null with 200 and only returns 500 on a thrown fault, so there is no upstream event-id dedupe table either. Therefore a second delivery of the same invoice does re-run the full resolution against current state, and the outcome for a fixed invoice is not deterministic across deliveries. Stripe genuinely permits this: at-least-once delivery (duplicates can arrive after a 200) plus manual dashboard resend. So the scenario CAN occur and is not cleanly REJECTED.\n\nBUT I CANNOT PROVE IT IS A GENUINE DEFECT, for three reasons that blunt the claim:\n\n1. The stated hard invariant — \"commission accrual idempotent on invoice id; money must never double-pay or under/over-pay\" — is NOT violated. The unique constraint on sourceInvoiceId makes a double-pay impossible, and the tests prove it (affiliates.spec.ts:79-82, 124-126; billing.spec.ts:346). The claim itself concedes this is not a double-pay. At worst the flip produces ONE correct-amount commission — never two.\n\n2. The absence of a negative/tombstone marker is a defensible design, not obviously a bug. The far more COMMON transient null is a read race: e.g. the first delivery arrives before tenant.stripeCustomerId is mapped (L147-151 finds no tenant → null) or before referredByAffiliateId is set. A redelivery then correctly recovers the commission. A sticky tombstone would convert every such recoverable null into a PERMANENT commission loss for the partner — strictly worse for money safety. So re-evaluating on redelivery protects money in the common case.\n\n3. The specific harmful \"flip\" (suspended-at-first-delivery → reactivate → resend same invoice, or a mid-suspension pct edit) requires a compound, operationally rare sequence, and the \"wrongness\" hinges on an unspecified business policy: no cited spec says a payment made during suspension is permanently forfeited even after reactivation. The result is identical to what the code already intends when a payment simply arrives after reactivation. Automatic Stripe duplicates arrive within seconds, during which admin state will not have mutated; only a deliberate resend after a deliberate reactivation triggers the flip, which reads as operator intent as much as a bug.\n\nNet: the non-idempotency of the negative decision is real and the outcome is delivery-timing-dependent in principle, but it never breaches the money-safety invariant and the no-tombstone choice is a reasonable tradeoff that protects against a more common failure. I can neither prove it can't occur (so not REJECTED) nor prove the flipped outcome is actually wrong rather than an accepted design tradeoff (so not CONFIRMED). PLAUSIBLE."
        ]
      },
      {
        "severity": "high",
        "category": "data-model",
        "file": "packages/db/prisma/migrations/20260803120000_affiliates/migration.sql",
        "line": 42,
        "title": "Case-insensitive code lookup over a case-SENSITIVE unique index → duplicate effective codes + nondeterministic (mis-routed) attribution",
        "description": "`affiliates_code_key` is a plain case-sensitive btree unique index (migration L42; schema.prisma `code String @unique`). But attribution lookup `getActiveByCode` runs `findFirst({ where: { code: { equals: code, mode: 'insensitive' }, status:'active' } })` (packages/db/src/repos/affiliates.ts:115), and `affiliateCodeSchema = /^[a-zA-Z0-9_-]{3,64}$/` (packages/shared/src/entities.ts:241) lets a platform_admin supply mixed-case codes. Nothing enforces the 'unique, case-insensitive by convention' claim in the schema comment: the DB will happily accept both `promo` and `PROMO` as two distinct affiliate rows. Auto-generated codes dodge this (uppercase-only alphabet, crud.ts:155) but admin-chosen codes do not. The lookup then uses `findFirst` with NO `orderBy`, so among case-variant matches the row returned is arbitrary/nondeterministic.",
        "failureScenario": "platform_admin creates active affiliate A with code `promo` and active affiliate B with code `PROMO` (both inserts succeed — case-sensitive unique index sees them as different). A tenant is created with ?ref=Promo. getActiveByCode does a case-insensitive findFirst that matches BOTH rows and returns one arbitrarily → the referral (20%/12mo of that tenant's revenue) is attributed to the wrong partner, and the choice can flip between Postgres plan/row-order changes.",
        "suggestedFix": "Enforce case-insensitivity in the DB, not just in the query: store/compare `code` as citext, or add a functional unique index `CREATE UNIQUE INDEX ON affiliates (lower(code))` and normalize on write; and give getActiveByCode a deterministic tiebreak (or make the collision impossible). Same applies to the `ref` capture path.",
        "verifierNotes": [
          "CONFIRMED: Real, reachable data-model defect. The DB unique index on affiliates.code is a plain case-SENSITIVE btree (migration 20260803120000_affiliates/migration.sql:42; schema.prisma:130 `code String @unique`, whose \"case-insensitive by convention\" comment is not DB-enforced — no citext, no lower(code) functional index). Nothing normalizes the code on write: crud.ts:1264 passes admin-supplied `data.code` through verbatim, and affiliateCodeSchema (entities.ts:241, /^[a-zA-Z0-9_-]{3,64}$/) permits mixed case; grep found no toUpperCase/toLowerCase on the code path. So a platform_admin can create both `promo` and `PROMO` as two distinct active rows (both INSERTs succeed). The attribution lookup getActiveByCode (affiliates.ts:115) is `findFirst({ where: { code: { equals: code, mode:'insensitive' }, status:'active' } })` with NO orderBy, so when a `?ref=Promo` tenant is created (crud.ts:1210 → referredByAffiliateId), it matches BOTH rows and returns an arbitrary one — nondeterministic misrouting of the 20%/12mo commission stream (accrueForPaidInvoice, affiliates.ts:146-172, keys all payouts off referredByAffiliateId). No test guards this: the api duplicate-code test (affiliates.spec.ts:110) uses an EXACT duplicate the case-sensitive index catches, never a case variant; the db test (143-147) only exercises the single-row case-insensitive read. Auto-generated codes (uppercase-only alphabet, crud.ts:156) correctly dodge the collision, so exploitation requires an admin-chosen mixed-case code — an operational footgun rather than an external attack, but the integrity gap and money-misrouting are genuine. The schema's own uniqueness claim is violated: the correct fix is a citext column or a UNIQUE index on lower(code) (plus normalizing on write), so read and write agree on identity.",
          "CONFIRMED: Every load-bearing claim checks out against the actual code, and I could not refute the scenario.\n\n1. The unique index IS case-sensitive. migration.sql:42 is a plain btree `CREATE UNIQUE INDEX \"affiliates_code_key\" ON \"affiliates\"(\"code\")` — no `lower(code)` expression index, no citext type, no trigger. schema.prisma:130 is `code String @unique` with a comment claiming \"unique, case-insensitive by convention\" — a convention that nothing enforces.\n\n2. The lookup IS case-insensitive with no deterministic tiebreak. affiliates.ts:115 `getActiveByCode` = `prisma.affiliate.findFirst({ where: { code: { equals: code, mode: 'insensitive' }, status: 'active' } })`. `findFirst` with no `orderBy` → Postgres returns an arbitrary matching row when more than one case-variant matches (no ORDER BY = no row-order guarantee; can flip across plan/heap changes).\n\n3. Admin-supplied mixed-case codes are accepted verbatim. `affiliateCodeSchema = z.string().regex(/^[a-zA-Z0-9_-]{3,64}$/)` (entities.ts:241) permits both cases. The create route (crud.ts:1264) passes `code: data.code ?? genAffiliateCode()` with NO normalization, and the repo create (affiliates.ts:116-131) inserts `data.code` unchanged. There is no toUpperCase/toLowerCase anywhere on the write path. So inserting both `promo` and `PROMO` as two distinct active affiliates succeeds — the case-sensitive unique index treats them as different keys and does not raise P2002.\n\n4. The only thing that dodges this is the AUTO-generated code path (genAffiliateCode, crud.ts:154-157, uppercase-only alphabet), exactly as the defect states — but that does not protect admin-chosen codes.\n\nThe existing test (affiliates.spec.ts:143-147) only proves the happy path (single stored `WHOOK1` resolves from `whook1`/`WhOoK1`); no test creates two case-variant codes, so nothing contradicts the defect. The failure scenario — admin creates active `promo` and active `PROMO`, tenant signs up with `?ref=Promo` (crud.ts:1210 → getActiveByCode), and the 20%/12-month commission stream is attributed to whichever of the two rows Postgres happens to return — is fully reachable with the code as written. Real data-model integrity gap with money-misrouting impact."
        ]
      },
      {
        "severity": "high",
        "category": "referential-integrity",
        "file": "packages/db/prisma/migrations/20260803120000_affiliates/migration.sql",
        "line": 57,
        "title": "commissions.tenantId (and affiliateId) FK is ON DELETE CASCADE → deleting a tenant/affiliate silently destroys paid & pending commission (money) records",
        "description": "`commissions_tenantId_fkey ... ON DELETE CASCADE` (migration L57) and `commissions_affiliateId_fkey ... ON DELETE CASCADE` (L54). A hard tenant delete exists and is reachable: DELETE /v1/tenants/:id (crud.ts) → tenants.remove → `prisma.tenant.delete` (packages/db/src/repos/tenants.ts:129), an unconditional row delete. Cascading through the FK wipes EVERY commission row for that tenant — including `status='paid'` rows (money already transferred to the partner) and `status='pending'` rows (money owed). There is no soft-delete or retention. Note the asymmetry: tenants.referredByAffiliateId is correctly ON DELETE SET NULL (L51) to preserve the tenant, but the commission ledger itself has no such protection in either direction.",
        "failureScenario": "A referred tenant churns and platform_admin deletes it via DELETE /v1/tenants/:id. All of that tenant's commission rows cascade-delete. The partner had 8 months of `paid` commissions and 1 `pending` — the entire financial trail is gone: you can no longer prove what was paid, reconcile Stripe payouts, or honor the outstanding pending amount. Same loss if an affiliate row is ever deleted.",
        "suggestedFix": "Commission is a financial ledger and must survive entity deletion. Change both FKs to ON DELETE RESTRICT (block deletion while commissions exist) or ON DELETE SET NULL with the *Id columns made nullable plus denormalized tenant/affiliate identifiers retained on the row. At minimum, block hard tenant delete when commissions exist (as was done for accounts-with-users in audit E1).",
        "verifierNotes": [
          "CONFIRMED: CONFIRMED for the tenant-delete path. Evidence: (1) migration 20260803120000_affiliates/migration.sql L54 and L57 declare both commissions FKs as ON DELETE CASCADE, matched by schema.prisma:177,180 (onDelete: Cascade on Commission.affiliate and Commission.tenant). (2) A hard tenant delete is reachable: crud.ts:1220-1224 exposes DELETE /v1/tenants/:id (platform_admin) → db.tenants.remove. (3) tenants.ts:126-132 remove() does an unconditional prisma.tenant.delete({where:{id}}) with NO guard against existing commissions and no soft-delete; the DB cascade then wipes every commission row for that tenant, including status='paid' (money already transferred) and status='pending' (money owed). (4) The audit.record 'before' payload only captures the tenant row, not its commissions, so the financial ledger is unrecoverable — you cannot reconcile Stripe payouts or honor outstanding pending amounts. The team demonstrably adds such guards elsewhere (account-delete blocked when it has users, audit E1, commit aab3d24), underscoring that tenant delete lacks equivalent protection. The affiliate-delete arm of the claim is not currently API-reachable (no DELETE /v1/affiliates/:id route exists), but the schema-level CASCADE remains and the tenant-delete arm alone makes the defect real and triggerable. The scenario in the claim (platform_admin deletes a churned referred tenant) executes exactly as described.",
          "CONFIRMED: All links in the chain are verified against real code. (1) The commissions FKs are ON DELETE CASCADE: migration 20260803120000_affiliates/migration.sql L54 (affiliateId) and L57 (tenantId). The tenants.referredByAffiliateId FK at L51 is deliberately ON DELETE SET NULL, confirming the noted asymmetry and that CASCADE on the ledger is an oversight. (2) A hard tenant delete is reachable and unconditional: apps/api/src/routes/crud.ts:1220 registers DELETE /v1/tenants/:id (platform-scoped), handler L1222 calls db.tenants.remove, which in packages/db/src/repos/tenants.ts:129 executes prisma.tenant.delete({ where: { id } }) with no guard/soft-delete/commission check. (3) The cascade actually fires and is not blocked: every FK referencing tenants across all migrations (tenant_domains, accounts, users in 0_init L332/335/338, plus commissions) is ON DELETE CASCADE, so no RESTRICT dependency blocks the delete; when it succeeds, Postgres cascade-deletes all commission rows including status='paid' (already paid out) and status='pending' (owed), with no retention. (4) No test asserts commission survival on tenant/affiliate delete. The failure scenario — platform_admin deletes a churned referred tenant and destroys the entire commission/financial trail — is directly executable through the existing API surface. Genuine high-severity financial referential-integrity defect."
        ]
      },
      {
        "severity": "medium",
        "category": "data-model",
        "file": "packages/db/prisma/migrations/20260803120000_affiliates/migration.sql",
        "line": 39,
        "title": "Affiliate email unique index is case-sensitive and login lookup is exact-match with no write-time normalization → duplicate partners and login failures",
        "description": "`affiliates_email_key` is a plain case-sensitive btree unique index (migration L39). The create path passes `data.email` through unmodified (crud.ts POST /v1/affiliates → affiliates.ts create:120-126); zod `z.string().email()` (entities.ts:250) validates but does not lowercase. Login resolves the account with `prisma.affiliate.findUnique({ where: { email } })` — an exact case-sensitive match (affiliates.ts:181). Email is treated as case-insensitive by every mail system, so the DB uniqueness and the login lookup disagree with reality.",
        "failureScenario": "platform_admin invites a partner as `Foo@Bar.com`. Later the same human (or a second admin) is invited as `foo@bar.com` — the insert succeeds because the unique index is case-sensitive, producing two affiliate rows / two referral codes for one person. Separately, a partner enrolled as `Foo@Bar.com` who logs in via the partner surface typing `foo@bar.com` gets no findUnique match and is denied (indistinguishable from wrong-account), despite valid credentials.",
        "suggestedFix": "Normalize email to lowercase at write time (create/update) and on the login lookup, and back it with a citext column or a `UNIQUE (lower(email))` functional index so the DB constraint matches the case-insensitive reality.",
        "verifierNotes": [
          "CONFIRMED: Confirmed via direct code reading. (1) affiliates_email_key is a plain case-sensitive btree (migration.sql:39); no citext/lower() index anywhere, and the second migration does not add normalization. (2) The create path stores email verbatim: crud.ts:1264 spreads ...data and affiliates.ts create (116-126) writes email: data.email unchanged; zod schema entities.ts:250 (z.string().email()) has no lowercase transform. Duplicate-row scenario is therefore real: Foo@Bar.com vs foo@bar.com are distinct unique keys, so both inserts succeed — two affiliate rows/codes for one person. No test covers this (all tests use lowercase emails).\n\nThe login-failure scenario is also real, though the claim's stated mechanism is imprecise. The claim says login is an exact match with 'no normalization', but partner.ts:76 actually does email.trim().toLowerCase() before the exact findUnique (affiliates.ts:181). The real bug is asymmetric normalization: login lowercases the input, create does NOT lowercase on write. Consequently an affiliate stored with any uppercase in its email (e.g. Foo@Bar.com) can never match the lowercased lookup and is permanently locked out regardless of typed casing — the claimed outcome (no match -> denied) holds and is in fact worse than described.\n\nOverall the core defect (case-sensitive uniqueness + no write-time normalization causing duplicate partners and login failures) is accurately identified and demonstrably realizable. The only inaccuracy is characterizing the login lookup as having no normalization; that does not rescue the code, it aggravates the failure. Attempts to refute (search for citext/expression index, zod transform, server-side create normalization, or a guarding test) all came up empty.</parameter>\n</invoke>\n",
          "CONFIRMED: The defect is real and demonstrable. (1) Duplicate rows: affiliates_email_key is a plain case-sensitive btree unique index (migration L39; schema.prisma:128 email String @unique, no citext/functional index). The create path stores email verbatim — crud.ts:1264 spreads ...data into affiliates.ts:119 create, and affiliateCreateSchema (entities.ts:248-254) validates z.string().email() with NO lowercasing transform. So Foo@Bar.com and foo@bar.com are distinct keys; both inserts succeed → two affiliate rows / two referral codes for one human. (2) Login failure: partner.ts:76 lowercases the typed email (parsed.data.email.trim().toLowerCase()) and then findByEmailForAuth does an exact case-sensitive findUnique (affiliates.ts:181-182). This read/write asymmetry means any affiliate stored with uppercase letters can NEVER authenticate — even typing the exact enrolled casing fails, because login always lowercases before the exact match. The claim slightly mischaracterizes login as having no normalization (it does lowercase the input), but that mismatch is precisely the bug mechanism, so the conclusion and failure scenario are correct. The authors applied case-insensitive matching to referral codes (getActiveByCode mode:'insensitive', affiliates.ts:115, tested at affiliates.spec.ts:143) but not to email, and no test covers mixed-case email create/login. Medium severity is apt: impact is duplicate partner records and login lockout for mixed-case enrollments, not direct double-pay (accrual keys on the tenant's single referredByAffiliateId FK)."
        ]
      },
      {
        "severity": "medium",
        "category": "test-coverage",
        "file": "apps/api/__tests__/billing.spec.ts",
        "line": 346,
        "title": "The genuine-DB-fault → HTTP 500 → Stripe-retry accrual path has zero test coverage",
        "description": "The whole design rationale of the F4 webhook accrual is the distinction between a benign null (dedupe / nothing-owed → 200 ack) and a genuine fault (throw → return 500 so Stripe retries). See billing.ts L247-256 (catch → `console.error` → `return c.text('accrual failed', 500)`) and affiliates.ts L169-172 (`if (isUniqueViolation(err)) return null; throw err`). The comment at affiliates.ts L162-164 explicitly states 'the repo no longer swallows non-dedupe errors' — a deliberate reversal of prior behavior. Yet every existing invoice test exercises ONLY the null→200 branches: billing.spec L346-362 (idempotent dedupe), L364-369 (unreferred), and the affiliates.spec accrue tests. No test injects a genuine fault and asserts a 500 / Stripe-retry. Note that the old swallow pattern (`catch { return null }`) literally still lives one function up in the same file (accrueCommission, affiliates.ts L142-144), so the regression this design guards against is a one-line copy-paste away.",
        "failureScenario": "A refactor makes accrueForPaidInvoice swallow a transient DB error and return null (mirroring accrueCommission L142-144). The webhook then returns 200, Stripe marks the event delivered and never retries, and the partner's commission is silently lost forever. Every test in the suite still passes — nothing asserts that a non-dedupe fault produces a non-2xx response.",
        "suggestedFix": "Add a billing.spec case that forces a genuine fault (e.g. stub db.affiliates.accrueForPaidInvoice to reject with a non-unique error, or point at a torn-down DB) for a referred tenant and assert the webhook returns 500; and a db-level affiliates.spec case asserting accrueForPaidInvoice rethrows a non-P2002 error rather than returning null.",
        "verifierNotes": [
          "CONFIRMED: Verified against the real code. The webhook's genuine-fault path (billing.ts L250-256: catch → console.error → return 500) and the repo's rethrow semantics (affiliates.ts L169-172, with the L162-164 comment documenting the deliberate reversal away from swallowing) both exist and are money-critical. Grepping apps/api/__tests__ and packages/db/__tests__/affiliates.spec.ts for 500/accrual-fault assertions yields nothing on this path. The only accrual tests exercise 2xx/null branches: billing.spec.ts L346-362 (idempotent dedupe → 200), L364-369 (unreferred → 200), and affiliates.spec.ts L112-173 (all assert a returned Commission or .toBeNull()). billing.spec.ts uses the real createDb, and no test forces accrueForPaidInvoice to throw, so the throw→500→Stripe-retry branch has zero coverage. The failure scenario is concrete: a refactor mirroring the still-present swallow at accrueCommission (L142-144) would make accrual return null on a transient DB error, the webhook would 200, Stripe would mark delivered, and the commission would be lost forever — and every existing test would still pass. This is a legitimate, verifiable test-coverage gap on a load-bearing money-correctness branch. All line references in the claim are accurate.",
          "CONFIRMED: Verified against the actual code. billing.ts L246-256 implements the claimed null→200 vs throw→500 split (catch → console.error → c.text('accrual failed', 500) for Stripe retry). affiliates.ts L162-172 deliberately rethrows non-dedupe errors, with a comment stating the repo 'no longer swallows non-dedupe errors' — a documented behavior reversal. The regression risk is concrete: accrueCommission (L137-145) still uses the old catch{return null} swallow one function up in the same file.\n\nTest coverage is genuinely absent for the 500/retry branch. Grep across billing.spec.ts, packages/db/__tests__/affiliates.spec.ts, and apps/api/__tests__/affiliates.spec.ts shows: the billing invoice webhook tests (L346-362 dedupe, L364-369 unreferred) assert only status 200; the two '500' matches in billing.spec are comment text (L227, L313). The db-repo accrue tests (L112-173) assert null returns / successful creates only. The only .rejects assertions target AffiliateConflictError on create (L48-55), never accrueForPaidInvoice on a genuine fault. The webhook route runs against a real testcontainers db with no repo-stub seam, so nothing forces a non-dedupe throw and asserts a non-2xx response.\n\nI tried to refute by (a) checking every relevant spec for a 500/throw assertion on the accrual path (none exists), and (b) checking whether the branch is unreachable dead code (it is not — a transient DB error genuinely triggers it in production). The coverage gap is real and the failure scenario (a refactor mirroring accrueCommission's swallow silently loses a partner's commission while the entire suite stays green) can actually occur. This is a legitimate test-coverage defect, correctly scoped as medium severity."
        ]
      },
      {
        "severity": "medium",
        "category": "test-coverage",
        "file": "packages/db/__tests__/affiliates.spec.ts",
        "line": 129,
        "title": "Commission-window anchor drift (floored/suspended first payments) is untested and over-pays",
        "description": "affiliates.ts L154 comments the window is measured 'from the tenant's FIRST payment', but the code anchors on the earliest existing COMMISSION row's createdAt (L157: `findFirst ... orderBy createdAt asc`), then bails only if `paidAt > addMonthsUtc(anchor, commissionMonths)` (L159). These diverge whenever the true first payment(s) produce no commission row: a proration/first invoice whose floored amount is 0 (L160-161 `amountCents <= 0 return null`), or payments made while the affiliate is `suspended` (L153). In those cases the anchor advances to the first ACCRUING payment, silently extending the payout window beyond commissionMonths. The only window test (L129-141) exclusively covers the case where the very first payment accrues, so this drift has no coverage.",
        "failureScenario": "A 12-month partner refers a tenant. The tenant's first Stripe invoice is a small mid-cycle proration (e.g. 3 cents) → floor(3*20/100)=0 → no commission written (L161). One month later the first full invoice arrives and becomes the anchor. Commissions now keep accruing until 12 months after that full invoice — ~13 months after the real first payment — over-paying the partner by roughly one extra billing cycle. No test catches it. Same drift occurs if the affiliate is suspended across the tenant's early paid invoices and later reactivated.",
        "suggestedFix": "Add a test where the first paid invoice floors to 0 (or occurs while suspended) and a payment exactly at commissionMonths-after-the-real-first-payment is asserted OUT of window. If the intended behavior really is 'first accrued commission', update the L154 comment to say so; otherwise anchor the window on a persisted first-payment timestamp independent of whether that payment accrued.",
        "verifierNotes": [
          "REJECTED: REJECTED.\n\nPremise is built on a misquote. The claim states L154 \"comments the window is measured 'from the tenant's FIRST payment'.\" The actual L154 comment reads \"commissionMonths from the tenant's FIRST **accrued** payment,\" and L155-156 explicitly spell out the mechanism: \"The earliest existing commission is that anchor; if there is none yet, THIS invoice IS the first payment → always in-window.\" The code at L157-158 (`findFirst ... orderBy createdAt asc` → `anchor = first?.createdAt ?? invoice.paidAt`) implements precisely that. So the code does NOT diverge from its implementation-site documentation — it matches it verbatim. The word \"accrued\" the claim drops is the entire load-bearing distinction. The only looser wording is the interface JSDoc at L90 (\"FIRST payment\"), a paraphrase, not the authoritative local contract.\n\nBecause the anchor-on-first-accrual behavior is the documented, intended design (a $0/floored/suspended early invoice deliberately does not start the clock — the clock starts when accrual actually starts), the window \"extension\" in those edge cases is not an over-payment bug; it is the specified behavior. The claim reframes documented intent as a defect. That maps to \"the code already handles it\" (deliberately).\n\nRealism of the two scenarios, for completeness:\n1. Floored-to-0 first invoice: with the default 20% rate, floor(amountPaidCents*20/100) <= 0 requires amountPaidCents < 5 (under 5 cents). A real subscription's first invoice being under 5 cents is essentially impossible; heavily contrived.\n2. Suspended-then-reactivated: this can occur, but only in a narrow shape — the affiliate must be active at signup (else getActiveByCode returns null and referredByAffiliateId is never set, so nothing is ever attributed), then suspended before ANY payment accrues, then reactivated. If even one commission accrued earlier, findFirst still returns it and there is no drift. So the window only \"extends\" when accrual genuinely never started during suspension — which is arguably the correct design (the N-month window counts active-accrual months, per L154-156).\n\nOn the pure test-coverage point: it is true that no test exercises the floored-first / suspended-from-start case (the L129-141 test has the first payment accrue). But a bare coverage gap whose motivating \"bug\" is actually documented intended behavior — and whose supporting evidence is a misquoted comment — does not constitute the claimed medium over-payment defect. Per the instruction to default to REJECTED when the code (deliberately) handles the scenario, this is REJECTED.",
          "CONFIRMED: CONFIRMED. The claim accurately describes real, reachable code behavior and a genuine test gap. affiliates.ts L157-158 anchors the commission window on the earliest existing COMMISSION row (`findFirst ... orderBy createdAt asc`), with the only fallback being the current invoice's paidAt — there is no true-first-payment timestamp (tenant select at L147-150 fetches only id + referredByAffiliateId). Two code paths produce a paid invoice with NO commission row: (1) a floored-to-zero amount — billing.ts paidInvoiceFrom (L202) only rejects amountPaidCents<=0, so a 1-4 cent invoice reaches accrueForPaidInvoice where floor(3*20/100)=0 hits L161 return null; (2) a payment while the affiliate is non-active (L153 return null). In both, the anchor later lands on the first ACCRUING payment, silently extending the payout window by ~one cycle past the true first payment — a real over-pay. The code's own comments are self-contradictory: L155-156 equates 'earliest existing commission' with 'the first payment', while L90-91 and the test at L129 say the window runs 'from the FIRST payment'; the equivalence breaks precisely in these cases. Coverage: the sole window test (L129-141) only exercises the first-payment-accrues case; the 'does NOT accrue' test (L160-174) never follows a skipped first invoice with a later payment, so the drift is untested. Adversarial refutation attempts (searching for a true-first-payment fallback, and a billing-layer filter blocking tiny invoices) both failed. The suspended sub-scenario is narrower (requires suspension before any accruing payment) but still possible; the floored-zero sub-scenario is squarely reachable. Category test-coverage / severity medium is fair."
        ]
      },
      {
        "severity": "low",
        "category": "test-coverage",
        "file": "packages/db/__tests__/affiliates.spec.ts",
        "line": 160,
        "title": "Test named '...or a $0 invoice' never exercises a $0 / floored-to-zero invoice",
        "description": "The test titled `does NOT accrue for an unreferred tenant, a suspended affiliate, or a $0 invoice` (affiliates.spec L160-174) asserts only three things in its body: unreferred tenant (L164), unknown customer (L166), and suspended affiliate (L173) — every accrueForPaidInvoice call passes amountPaidCents: 10_000. There is no $0 or floored-to-zero invoice anywhere in the body, so the `amountCents <= 0 return null` guard (affiliates.ts L161) is uncovered despite the title claiming it. The sibling guard in billing.ts L202 (`amount_paid <= 0` → paidInvoiceFrom null) is also never exercised — no billing.spec invoice event sends amount_paid <= 0.",
        "failureScenario": "A regression deletes affiliates.ts L161 (the `amountCents <= 0` bail). A referred tenant's tiny/zero invoice then inserts a 0-cent (or otherwise meaningless) Commission row that later shows up in payout review. The full test suite stays green because the assertion the test title promises was never written.",
        "suggestedFix": "Add an assertion in this test (or a new one) that a referred+active tenant with a floored-to-zero amount — e.g. amountPaidCents: 1 with commissionPct default, or amountPaidCents: 0 — returns null and writes no commission row; and a billing.spec invoice event with amount_paid: 0 that acks 200 with no accrual.",
        "verifierNotes": [
          "CONFIRMED: CONFIRMED. affiliates.spec.ts:160-174 is titled \"...or a $0 invoice\" but its body has three accrueForPaidInvoice assertions (L164 unreferred, L166 unknown customer, L173 suspended affiliate), each passing amountPaidCents: 10_000. No $0 or floored-to-zero invoice is exercised. A grep of the entire suite shows every amountPaidCents value in affiliates.spec.ts is 10_000 (and the floor test yields 3333 > 0), so the `amountCents <= 0 return null` guard at affiliates.ts:161 is covered by zero tests. The sibling guard at billing.ts:202 (`amountPaidCents <= 0`, confirmed present) is likewise never exercised — all three invoiceEvent calls in billing.spec.ts (L353/360/367) pass amount_paid: 5_000. I could not refute it: the guard is not covered anywhere, the title literally promises a $0 case the body omits, and the failure scenario (regression deleting L161 → 0-cent commission row inserted, suite stays green) is valid. Correctly rated low / test-coverage: the production guards are correct; only the test claiming to cover them is absent.",
          "CONFIRMED: The claim is accurate. The test at packages/db/__tests__/affiliates.spec.ts:160 is titled \"...or a $0 invoice\" but its body (L162-173) makes only three assertions — unreferred tenant, unknown customer, suspended affiliate — each passing amountPaidCents: 10_000. There is no $0 or floored-to-zero invoice in the body. Verified by grep: every accrueForPaidInvoice call in the DB test uses amountPaidCents 10_000, and the only fractional test (L150) uses commissionPct 33.33 → 3333 cents; no test uses commissionPct 0, amountPaidCents 0, or a rate that floors to 0. Therefore the guard at affiliates.ts:161 (`if (amountCents <= 0) return null`) is uncovered. The sibling guard at billing.ts:202 (`amountPaidCents <= 0`) is likewise uncovered: all invoiceEvent(...) calls in apps/api/__tests__/billing.spec.ts (L353, L360, L367) send amount_paid 5_000, never <= 0. The failure scenario holds — deleting affiliates.ts:161 would allow a 0-cent Commission row and the suite would remain green. Severity low is appropriate: both guards currently exist and work; this is a genuine test-coverage gap, not an active money defect."
        ]
      },
      {
        "severity": "medium",
        "category": "correctness",
        "file": "packages/db/src/repos/affiliates.ts",
        "line": 176,
        "title": "setCommissionStatus has no state-machine or idempotency guard — payout status freely reversible, enabling double payout / un-void",
        "description": "setCommissionStatus (L176-180) blindly does `prisma.commission.update({ where:{id}, data:{ status } })` for any target status, with no check of the current status and no allowed-transition table. Any status can move to any other: pending→paid→pending→paid, or void→paid. There is also no audit trail on this money event: createAffiliateRepo(prisma) is constructed WITHOUT an AuditRepo (contrast createTenantRepo(prisma, audit)), the `_actor` params on create/update are underscore-prefixed and unused, and there is not one `audit.record` call in the entire affiliates repo — while tenants.ts audits all 8 write paths. Marking a commission `paid` (the real-money bookkeeping flag the platform_admin pays partners against) is thus fully mutable and untraceable.",
        "failureScenario": "Admin marks commission X `paid` and wires the partner the money externally. A later PATCH /v1/commissions/X (misclick, a payout-batch tool that re-flips `pending`→`paid`, or an operator correcting something else) sets X back to `pending`; the next payout run picks it up as an unpaid commission and pays the partner a SECOND time. Symmetrically, a commission `void`ed after a refund/chargeback can be flipped back to `paid` (un-voiding money the platform gave back). Because no row records who changed the status or when, neither double-payout leaves any forensic trace.",
        "suggestedFix": "Enforce a transition table in setCommissionStatus (pending→paid, pending→void, paid→void for clawback; reject paid→pending and void→paid), read+compare the current status inside the update predicate (e.g. update where {id, status: <expected>} → null on no-op), and wire an AuditRepo into createAffiliateRepo so every commission status change and affiliate rate/status change records an audit row like tenants.ts does.",
        "verifierNotes": [
          "CONFIRMED: All factual assertions verified against real code. affiliates.ts setCommissionStatus (L176-180) does only an existence check then a blind update({data:{status}}) — no current-status check, no allowed-transition table, no per-transition idempotency guard; so pending↔paid and void→paid are all permitted. The route PATCH /v1/commissions/:id (crud.ts L1297-1303) and commissionStatusUpdateSchema (entities.ts L263) validate only that the target is a legal enum value, adding no transition guard. The affiliates repo has NO audit: createAffiliateRepo(prisma) (db.ts:72) is built without an AuditRepo, unlike createTenantRepo(prisma, audit) (db.ts:71); the _actor params on create/update are unused and there is not one audit.record call in the file, whereas tenants.ts audits create/update/delete/branding (L165/179/186/192). affiliates.spec.ts only tests the forward pending→paid case (L84-85) and never asserts any transition restriction, so no test handles this. The mutation of the real-money bookkeeping flag is therefore fully reversible and untraceable. Caveats: the route is platform-admin only (trigger is a trusted operator/tool, not an external attacker), and there is no automated payout executor in the repo, so the literal 'second payout' is an out-of-band ops consequence rather than code the repo runs. But the defect as titled — no state-machine/idempotency guard on a reversible money-status flip, and no audit record of who/when — is directly true, reachable, and inconsistent with the stated 'money must never double-pay' invariant and with the audited tenant write paths. Medium severity is fair.",
          "PLAUSIBLE: Every factual assertion in the claim is confirmed against the code. affiliates.ts:176-180 setCommissionStatus does an unconditional prisma.commission.update to any target status after only a null-existence check — no current-status read, no transition table. The API layer adds no guard: commissionStatusUpdateSchema (entities.ts:263) only validates the enum, and the route (crud.ts:1297-1303) passes it straight through. Any status is freely reversible (paid→pending, void→paid). The audit gap is also real and unambiguous: createAffiliateRepo(prisma) (affiliates.ts:110) takes NO AuditRepo, whereas createTenantRepo(prisma, audit) (tenants.ts:112) audits all four write paths; the _actor params on create/update are underscore-prefixed and unused, and there is not one audit.record call in the entire affiliates repo. The only status test (affiliates.spec.ts:84-85) asserts pending→paid succeeds; nothing forbids a reversal.\n\nHowever, the claim is not fully CONFIRMED as a code-level correctness defect because the described HARM (double payout, un-void) depends on an out-of-band process that does not exist in the reviewed code. A repo-wide search found NO automated payout run — the only reference is the comment 'payout review' (crud.ts:1294), implying a human reviews the pending list and pays externally. So the double-pay scenario materializes only through a human/external workflow, not through any deterministic code path. Additionally, free reversibility is plausibly an intended platform_admin correction affordance (a strict state machine would block legitimate fix-ups), and I cannot prove from the code that it is unintended. It is above REJECTED — no guard or test constrains transitions, and the money-mutation audit gap is a concrete present omission — but below CONFIRMED because the exploit path is external and product intent on reversibility is unproven."
        ]
      },
      {
        "severity": "low",
        "category": "correctness",
        "file": "packages/shared/src/entities.ts",
        "line": 245,
        "title": "commissionPct accepts >2 decimal places; DECIMAL(5,2) silently rounds, so the effective commission rate differs from what the admin set",
        "description": "commissionPctSchema = z.number().min(0).max(100) has no .multipleOf(0.01) constraint, so a value like 15.555 or 33.335 passes validation. The affiliates.commissionPct column is DECIMAL(5,2) (migration L16), so Postgres rounds the stored value (e.g. 15.555 → 15.56 / 15.55 depending on rounding). accrueForPaidInvoice then computes amountCents = floor(amountPaid * Number(commissionPct)/100) against the ROUNDED rate. The rate the partner is actually paid at is not the rate the admin entered, with no error or warning surfaced.",
        "failureScenario": "platform_admin creates/updates an affiliate with commissionPct=15.555 (accepted by zod, 200 OK). The DB stores 15.56. On a €100.00 invoice the partner accrues floor(10000*15.56/100)=1556 cents instead of the intended 1555.5→1555, a systematic per-invoice discrepancy the admin cannot see because the API/UI echo the rounded stored value while believing they configured 15.555%.",
        "suggestedFix": "Add `.multipleOf(0.01)` (and a matching integer-cent bound) to commissionPctSchema so a rate that DECIMAL(5,2) cannot represent is rejected at the edge rather than silently rounded.",
        "verifierNotes": [
          "CONFIRMED: Verified against actual code. entities.ts:245 defines commissionPctSchema = z.number().min(0).max(100) with no multipleOf/decimal-places constraint, used by affiliateCreate (line 252) and affiliateUpdate (line 259). The DB column is DECIMAL(5,2) (migration 20260803120000_affiliates/migration.sql L16; schema.prisma:132 @db.Decimal(5,2)). Postgres silently rounds scale overflow on NUMERIC (does not throw for fractional-digit overflow), so 15.555 is stored as 15.56. affiliates.ts:160 computes amountCents = Math.floor((invoice.amountPaidCents * Number(affiliate.commissionPct)) / 100) using the rounded stored value. Thus an admin entering 15.555 (accepted, 200 OK) results in an effective 15.56% rate with no error/warning; the API/UI echo the rounded value. Failure-scenario math is correct (1555 intended vs 1556 stored on a €100 invoice = 1-cent per-invoice divergence). I attempted refutation on three fronts — additional validation layer, Postgres erroring instead of rounding, and input pre-rounding — all failed. No test enforces a decimal-places constraint. This does NOT violate the money-integrity invariant (accrual stays deterministic/idempotent against the stored rate; no double/under/over-pay relative to what's stored); it is strictly an input-validation gap where admin intent diverges from the stored rate. Real, reproducible, low severity — as claimed.",
          "CONFIRMED: Every link in the claim is grounded in real code. (1) packages/shared/src/entities.ts:245 defines commissionPctSchema = z.number().min(0).max(100) with NO .multipleOf(0.01) or step constraint, so 15.555 / 33.335 pass validation. (2) migration 20260803120000_affiliates/migration.sql declares \"commissionPct\" DECIMAL(5,2) — Postgres silently ROUNDS a 3-decimal value to 2 places (e.g. 15.555 → 15.56) rather than rejecting it. (3) affiliates.ts:160 computes amountCents = Math.floor((invoice.amountPaidCents * Number(affiliate.commissionPct)) / 100) using the DB-rounded value, so the partner is paid at the rounded rate. (4) No compensating rounding or rejection exists at any layer: grep of crud.ts, affiliates.tsx, and entities.ts shows commissionPct is passed straight through (route uses schema only; web form sends Number(pct) directly at affiliates.tsx:198). No test guards this — affiliates.spec.ts:151 uses 33.33 (exactly 2 decimals, representable), so nothing asserts >2-decimal rejection or entered-vs-effective-rate equality. The scenario is reachable via both UI and direct API: e.g. 15.555 → stored 15.56 → on €100.00 invoice accrues floor(10000*15.56/100)=1556 cents vs intended 1555, silent because API/UI echo the rounded value. Real, reproducible, low-severity correctness gap exactly as described."
        ]
      },
      {
        "severity": "medium",
        "category": "anti-fraud",
        "file": "apps/api/src/routes/crud.ts",
        "line": 1207,
        "title": "Spec-mandated self-referral guard is entirely absent from ref→referredByAffiliateId resolution",
        "description": "PROJECT_PLAN §6.9 mandates, under \"Anti-fraud v1 floor\", a self-referral guard: \"affiliate cannot refer a tenant whose admin email domain matches the affiliate's.\" No such check exists anywhere in the money path. crud.ts POST /v1/tenants (L1207-1211) resolves `ref` via getActiveByCode and writes referredByAffiliateId with zero domain/self-dealing validation; tenants.ts create just spreads referredByAffiliateId (verified: no email/domain/selfRef logic). The affiliate's own referral code attributing the affiliate's own tenant is not blocked, so every invoice that tenant pays accrues a commission to the affiliate on their own spend.",
        "failureScenario": "An affiliate (or a partner-run reseller) gets a tenant provisioned under their own code — e.g. their lead carries ?ref=THEIRCODE and a platform_admin provisions it from the lead unaware of the domain match. The affiliate now harvests commissionPct% of their own subscription+usage payments for commissionMonths, indefinitely re-attributed. The v1 anti-fraud control that was supposed to stop exactly this is not implemented, and (because tenant admin email is not even available at tenant-create time in the current flow) cannot fire.",
        "suggestedFix": "Implement the §6.9 self-referral guard: when attributing referredByAffiliateId, refuse (or drop the attribution) if the tenant's admin email domain matches the affiliate's email domain; since admin email is not present at tenant create, enforce at the point admin users are added, or capture the intended admin email on the lead/tenant-create body and compare there.",
        "verifierNotes": [
          "CONFIRMED: The spec-mandated self-referral guard is genuinely absent from the money path, and the claim is grounded in the actual code.\n\nSPEC MANDATE (real): PROJECT_PLAN.md line 230 (§6.9 affiliate section) states verbatim: \"Self-referral guard: affiliate cannot refer a tenant whose admin email domain matches the affiliate's.\" This is normative. (Minor mischaracterization in the claim: the guard sentence lives in the \"Attribution\" paragraph, and line 235's \"Anti-fraud v1 floor:\" bullet does not itself re-list the self-referral guard — but the guard is still explicitly required by the spec, so this does not weaken the finding.)\n\nCODE — guard absent: apps/api/src/routes/crud.ts POST /v1/tenants (the lines shown, ~1203-1210) resolves the ref with a single line: `const referredByAffiliateId = ref !== undefined ? (await db.affiliates.getActiveByCode(ref))?.id ?? null : null` and passes it straight into `db.tenants.create`. There is zero domain/self-dealing validation. packages/db/src/repos/tenants.ts `create` just spreads `...(data.referredByAffiliateId != null ? { referredByAffiliateId } : {})` into `prisma.tenant.create` — no email/domain/self-ref logic. A repo-wide grep of source (apps/api/src, apps/web/src, packages/db/src, packages/shared/src) for self-referral/domain-match/emailDomain logic returns nothing but an unrelated import line; the only self-referral test hit is a domain-TXT branding test, unrelated. No test asserts self-referral is blocked.\n\nCANNOT-FIRE claim verified: tenantCreateSchema (packages/shared/src/entities.ts ~L358-368) has only name, branding, plan, ref — no admin email, and db.tenants.create does not create any admin user (unlike createSelfServeSignup, which does carry data.email). So even if someone wanted to check \"admin email domain matches affiliate's\" at platform_admin create time, the data needed does not exist in this path.\n\nMONEY IMPACT is real and reachable: packages/db/src/repos/affiliates.ts accrueForPaidInvoice (L149-152) selects tenant.referredByAffiliateId and accrues a commission for exactly that affiliate on every paid invoice within the window. A tenant self-referred under the affiliate's own code therefore harvests commissionPct% of its own subscription+usage spend for the commission window, re-attributed indefinitely. The billing webhook (invoice.payment_succeeded → accrueForPaidInvoice) makes this live (#137). The only mitigating control is that v1 tenants are created manually by platform_admin from leads — but the spec explicitly required the automated guard as an anti-fraud floor precisely because a human unaware of the domain match is insufficient. The scenario can actually occur; the mandated control does not exist and, in the current schema, could not fire. CONFIRMED.",
          "CONFIRMED: The spec-mandated self-referral guard is genuinely absent from the money path, and self-referred commission accrual can occur automatically.\n\nEVIDENCE FOR THE MANDATE. PROJECT_PLAN.md §6.9 line 230 states verbatim: \"Self-referral guard: affiliate cannot refer a tenant whose admin email domain matches the affiliate's.\" §6.9 is the v1 affiliate module spec (line 233 explicitly keeps it in v1: \"No self-serve tenant signup in v1 — tenants are created by platform_admin from pilot leads\"); the guard is NOT deferred to V2 (line 234 V2 = self-serve portal, Stripe payouts, multi-touch only). So the control is in-scope for the shipped affiliate epic.\n\nEVIDENCE OF ABSENCE. crud.ts POST /v1/tenants (L1204-1211) resolves ref via `db.affiliates.getActiveByCode(ref)` and writes `referredByAffiliateId` with zero domain/self validation. tenants.ts `create` (L156-165) merely spreads `referredByAffiliateId` into `prisma.tenant.create`. The live accrual path affiliates.ts `accrueForPaidInvoice` (L146-152) checks only `tenant.referredByAffiliateId != null` and the affiliate row — no self/domain check. A repo-wide grep for self-referral/self-deal/domain-match logic returns nothing, and neither packages/db/__tests__/affiliates.spec.ts nor apps/api/__tests__/affiliates.spec.ts contains any self-referral case. No TODO/deferral marker documents it.\n\nSCENARIO REALIZABLE. The accrual path is live (Stripe invoice.payment_succeeded → accrueForPaidInvoice, commit #137). A tenant provisioned under its own affiliate's code (e.g. a lead carrying ?ref=THEIRCODE that a platform_admin provisions) will accrue commission on its own paid invoices with nothing blocking it. The abuse is not prevented anywhere.\n\nTwo accuracy caveats that temper but do not defeat the finding: (1) the guard is stated under \"Attribution\" (L230), not the enumerated \"Anti-fraud v1 floor\" list (L235) as the claim says — a minor misattribution. (2) The claim's \"harvests ... indefinitely\" overstates real money movement: commissions land as `pending` and payout is manual (pending→approved→paid per §6.9 L231), so a human approval is a backstop before cash actually moves; and today tenant creation is platform_admin-only. But the specced control is an AUTOMATED guard precisely so the ledger is not polluted with self-dealing accruals, and the claim correctly notes the architecture cannot even evaluate it (tenantCreateSchema carries no admin email — only name/branding/plan/ref). The mandated control is absent, the pollution occurs automatically, and no test or code path handles it. That is a real defect, hence CONFIRMED rather than REJECTED, at roughly the claimed medium/anti-fraud severity."
        ]
      },
      {
        "severity": "low",
        "category": "session-management",
        "file": "apps/api/src/routes/partner.ts",
        "line": 58,
        "title": "Partner password reset does not revoke outstanding partner access tokens",
        "description": "The partner access token (mintPartnerToken, partnerJwt.ts L27-29) has a default 8h TTL, no refresh, and no revocation channel. partnerAuth (partner.ts L50-62) validates only signature/exp plus a live status==='active' recheck; it performs NO issued-at/revocation check. The set-password flow (partner.ts L109 -> affiliates.ts setPassword L183-185) simply overwrites passwordHash and writes no revocation marker. This is asymmetric with the tenant surface, whose own design doc (apps/api/src/auth/revoke.ts L4-13) states the invariant 'A password change or admin reset must revoke EVERY family of the user' and whose ws-ticket path enforces a revokedAfter(iat) marker (audit B1, app.ts L205-208). Note the partner surface DOES revoke on suspension (status recheck) but NOT on the compromise-recovery action a partner actually controls: resetting the password.",
        "failureScenario": "An attacker phishes/compromises a partner's password and logs in via POST /v1/partner/login, receiving an 8h Bearer token. The partner detects the compromise and uses the admin-conveyed reset link (POST /v1/partner/set-password) to change the password. Because setPassword writes no revocation marker and partnerAuth never checks token age against one, the attacker's already-minted token keeps authenticating GET /v1/partner/me and GET /v1/partner/commissions (exposing commission amounts, invoice ids, referral code, email) for the remainder of its up-to-8h lifetime, and there is no partner-facing logout to shorten that window.",
        "suggestedFix": "On setPassword, bump a per-affiliate revocation marker (e.g. a passwordChangedAt/tokenEpoch column or a Redis key keyed by affiliate id), and have partnerAuth reject any partner token whose iat predates it — mirroring the tenant revokedAfter(iat) check. Alternatively shorten partnerTtlS materially and/or add a partner logout that sets the marker.",
        "verifierNotes": [
          "CONFIRMED: The claimed mechanic is exactly what the code does. affiliates.ts:183-185 setPassword only overwrites passwordHash and writes no revocation marker; partner.ts:50-62 partnerAuth checks only signature/exp (partnerJwt.ts:31-38) plus status==='active', with no iat/revocation/password-generation check; the Affiliate model (schema.prisma:125-145) has no passwordChangedAt or tokenVersion field to check against; token TTL is 8h with no refresh and no partner logout (partner.ts:37). partner.ts:111 invalidatePwTokens burns only sibling set-password tokens, not access tokens. Tests confirm: partner.spec.ts:135 asserts revocation on suspension but there is NO test for revocation on password reset. So an access token minted before a reset keeps authenticating GET /v1/partner/me and /commissions for up to the remaining TTL. The scenario is reachable, so it does not meet the REJECTED bar (neither handled nor impossible). Hence CONFIRMED. Two caveats keep it at low severity and partially rebut the claim's framing: (1) The asymmetry with the tenant surface is overstated — revoke.ts:4-13 itself documents that the 'revoke every family' rule is an unimplemented TODO(db) and that an admin reset is currently a best-effort no-op; the partner reset is admin-conveyed (no partner self-service change; token issued by platform_admin), so the equivalent tenant action is also a no-op today. (2) Exposure is read-only, own-data (which the credential-holder already had), with a hard 8h cap and no refresh renewal, and the design explicitly documents this as an accepted low-sensitivity tradeoff (partnerJwt.ts:6-13). The gap objectively exists and is reachable, but it is genuinely low severity and not uniquely deficient versus the tenant admin-reset path.",
          "CONFIRMED: All factual claims verified against the actual code. The partner access token is a stateless HS256 JWT with an 8h default TTL and no refresh (partnerJwt.ts L27-29; partner.ts L37 DEFAULT_TTL_S). partnerAuth (partner.ts L50-62) validates only signature/exp plus a status==='active' recheck; it performs no iat/revocation check — verifyPartnerToken (partnerJwt.ts L31-38) does not even return iat, unlike the tenant verifyAccessToken (jwt.ts L48-61) which surfaces iat specifically for the ws-ticket revokedAfter(iat) marker (audit B1). The set-password flow (partner.ts L100-114) calls affiliates.setPassword (affiliates.ts L183-185) which only overwrites passwordHash and writes NO revocation marker/token-epoch; invalidatePwTokens (L111/L198-200) burns only sibling set-password tokens, not access tokens. No partner-facing logout exists. Therefore, after a partner (via the admin-conveyed reset link) changes their password, an attacker's already-minted token keeps authenticating GET /v1/partner/me and /v1/partner/commissions (exposing commission amounts, invoice ids, referral code, email — L121-134) for up to the remaining 8h.\n\nThe tests confirm rather than refute the gap: partner.spec.ts L135 explicitly tests that SUSPENSION revokes a live token, and L145 tests single-use set-password tokens, but there is NO test asserting a password reset invalidates an outstanding access token. Suspension (status recheck) is the only revocation channel — exactly as the claim states.\n\nRefutation failed on both fronts: the scenario can occur (reset flow exists; tokens are stateless with no revocation store), and neither code nor test handles access-token invalidation on password reset. This is a genuine, reproducible low-severity session-management weakness (password reset should invalidate live sessions — OWASP session mgmt).\n\nSeverity-bounding caveats (the claim already rates it low): the partner surface is read-only; the partner does not self-initiate reset — a platform_admin issues/conveys the token via issuePartnerSetPwToken; and the 'asymmetric with tenant' framing is slightly overstated, since tenant ACCESS tokens likewise survive their TTL on ordinary REST routes after a reset (only refresh families and ws-ticket issuance enforce revocation, per revoke.ts and jwt.ts). None of these make the scenario impossible or handled — they only cap impact — so the defect stands as CONFIRMED at low severity."
        ]
      },
      {
        "severity": "medium",
        "category": "ordering",
        "file": "packages/db/src/repos/tenants.ts",
        "line": 239,
        "title": "Per-subscription webhook guard only protects active/trialing tenants — a past_due (grace) subscription is clobbered by a late foreign-sub deletion",
        "description": "The audit-P4 per-subscription guard (L231-242) is meant to stop a late-delivered `customer.subscription.deleted` for an OLD sub from cancelling the tenant's CURRENT live subscription. But its notion of \"a live sub to protect\" is the clause at L239: `{ subscriptionStatus: { notIn: ['active','trialing'] } }`. That means the guard only fires when the tenant is currently active/trialing. Every other status that the rest of the system treats as a LIVE, billable subscription is left unprotected. billing.ts `hasLiveSubscription`/`RESUBSCRIBABLE` (L38-39) classifies past_due, unpaid, incomplete and paused as live (only canceled/incomplete_expired are re-subscribable), and effectiveEntitlements (plans.ts L84-88) grants the FULL entitlement matrix during `past_due` (documented grace) and `incomplete`. So the guard's definition of \"live\" is strictly narrower than the two modules that consume the state, and it under-protects exactly the grace states billing.ts calls live. When the tenant's current status is past_due, the OR clause at L239 matches, so a non-live incoming event for ANY (even superseded) subscription passes the per-sub guard and, if its eventAt is newer, passes the monotonic guard too — overwriting subscriptionStatus->canceled and stripeSubscriptionId->the dead sub id.",
        "failureScenario": "Tenant is on sub_B with subscriptionStatus='past_due' (renewal payment retrying; still fully entitled via grace). A superseded sub_A (cancelled at period-end, or a downgraded/migrated sub) emits `customer.subscription.deleted` whose event.created is later than the past_due transition. incomingLive=false. Monotonic guard at L230 passes (newer eventAt). Per-sub OR clause: current subscriptionStatus='past_due' is notIn ['active','trialing'] -> L239 matches -> updateMany applies. Tenant is flipped to subscriptionStatus='canceled', stripeSubscriptionId=sub_A. getEntitlements now returns FLOOR (canceled is in LAPSED_SUBSCRIPTION_STATUSES), so the tenant loses all paid features and drops off listActiveSubscribers, even though sub_B is a live, retrying, entitled subscription. The P4 test (billing.spec.ts L211) only proves protection when the current status is 'active'; swap it to 'past_due' and the protection is gone.",
        "suggestedFix": "Base the per-sub guard's \"is there a live sub to protect\" test on the SAME live-status set the rest of the system uses (billing.ts hasLiveSubscription / not-in-RESUBSCRIBABLE), not the narrow active/trialing pair. I.e. only allow a non-live incoming event to touch the tenant when stripeSubscriptionId is null, matches data.stripeSubscriptionId, or the current status is one of the re-subscribable/ended states (canceled/incomplete_expired) — so past_due/unpaid/incomplete/paused on a different sub id cannot clobber the current one.",
        "verifierNotes": [
          "CONFIRMED: Real, reachable defect. The per-subscription P4 guard in packages/db/src/repos/tenants.ts (L231-242) decides whether a tenant has \"a live sub to protect\" using the clause at L239: `{ subscriptionStatus: { notIn: ['active','trialing'] } }`. That set is strictly narrower than the two modules that consume subscription state: billing.ts `hasLiveSubscription`/`RESUBSCRIBABLE` (L38-39) treats past_due/unpaid/incomplete/paused as LIVE (only canceled/incomplete_expired are re-subscribable), and plans.ts `effectiveEntitlements` (L87-88, LAPSED set L64) grants the FULL entitlement matrix during past_due (documented grace, L60-61) and incomplete. So the guard under-protects exactly the grace states billing.ts/plans.ts call live.\n\nFailure path (all steps grounded in real code): tenant currently on sub_B with subscriptionStatus='past_due' (still fully entitled via grace). A superseded sub_A configured cancel_at_period_end fires customer.subscription.deleted when its period ends — after B's past_due transition — so event.created (eventAt) is newer. The webhook maps this to status='canceled', stripeSubscriptionId=sub_A, eventAt=event.created (billing.ts L184-190, L237). incomingLive=false. Monotonic guard L230 passes (newer eventAt). Per-sub OR: sub_A ≠ current sub_B and stripeSubscriptionId not null, but current subscriptionStatus='past_due' is notIn ['active','trialing'] → L239 matches → updateMany applies. Tenant flips to subscriptionStatus='canceled', stripeSubscriptionId=sub_A. getEntitlements (tenants.ts L128) then returns FLOOR (canceled ∈ LAPSED), the tenant loses all paid features, and it drops off listActiveSubscribers (L262 filters to active/trialing) — even though sub_B is a live, retrying, entitled subscription.\n\nMultiple subscriptions per customer with late foreign-sub deletion is precisely the scenario this guard was built for (comment L217-224), so the reachability premise is the code's own. The existing P4 test (billing.spec.ts L211-232) only covers current status 'active'; swapping the current sub_B to 'past_due' removes the protection. The guard's definition of \"live\" being narrower than both consumers is the concrete, verifiable defect.",
          "CONFIRMED: The per-subscription guard in packages/db/src/repos/tenants.ts L239 defines \"a live sub to protect\" as subscriptionStatus IN ['active','trialing'] (its complement, notIn, is the pass-clause). This is strictly narrower than the two modules that consume subscription state: plans.ts L64/L87-88 grants the FULL entitlement matrix for past_due (LAPSED_SUBSCRIPTION_STATUSES deliberately excludes past_due — grace window), and billing.ts L38-39 hasLiveSubscription treats past_due as an existing live subscription. So a past_due tenant is fully entitled and live everywhere EXCEPT this guard.\n\nFailure path is realizable and confirmed against the code: tenant on sub_B, subscriptionStatus='past_due' (lastBillingEventAt = t_pastdue). A superseded sub_A (same customerId; e.g. cancel-at-period-end whose period ends after B went past_due) emits customer.subscription.deleted with event.created = t_Adel > t_pastdue. In applySubscriptionEvent: (1) incomingLive=false (status 'canceled'); (2) monotonic guard L230 passes since t_pastdue < t_Adel; (3) inner OR L235-240: sub null? no; sub==sub_A? current is sub_B, no; status null? no; status notIn[active,trialing]? past_due → TRUE. updateMany applies, flipping subscriptionStatus→'canceled', stripeSubscriptionId→sub_A (dead id), currentPeriodEnd overwritten. getEntitlements (L128→effectiveEntitlements) then returns FLOOR because canceled∈LAPSED — the tenant loses grace entitlements prematurely while sub_B is a live, retrying subscription, and its stored subscription id is corrupted to a dead one.\n\nThe P4 test (billing.spec.ts L211-231) only proves protection when the current status is 'active' (sub_B active, L224-225); there is no coverage of the past_due-current case, and substituting past_due defeats the guard.\n\nTwo caveats that do not change the verdict: (a) the claim's 'drops off listActiveSubscribers' harm is moot because listActiveSubscribers (L262) already filters IN['active','trialing'] and past_due was never in it — but the entitlement-floor and state-corruption harms stand independently; (b) the exact timing (A deleted after B went past_due, with newer event.created) is uncommon, but genuinely occurs with cancel-at-period-end / migration flows. The defect is real and the guard under-protects exactly the grace states the rest of the system calls live."
        ]
      },
      {
        "severity": "low",
        "category": "ordering",
        "file": "packages/db/src/repos/tenants.ts",
        "line": 230,
        "title": "Monotonic webhook guard uses strict `lt` against second-granularity event.created — two distinct subscription events in the same second collapse to first-applied-wins, dropping the later authoritative state",
        "description": "applySubscriptionEvent orders writes by `lastBillingEventAt { lt: eventAt }` (L230) and writes eventAt (L255). eventAt is `new Date(event.created * 1000)` (billing.ts L237), and Stripe's `event.created` has ONE-SECOND granularity. The code comment (L213-214) explicitly treats an equal eventAt as \"a replay -> no-op\", conflating \"same Unix second\" with \"duplicate event\". But two DISTINCT subscription lifecycle events for the same customer routinely share a `created` second (e.g. `customer.subscription.created` + `customer.subscription.updated` emitted together at checkout completion, or two rapid `customer.subscription.updated` events during a plan change). Whichever is applied first advances lastBillingEventAt to T; the second, with eventAt==T, fails the strict `lt: T` predicate, matches zero rows, and is silently dropped. This is last-write-loses instead of last-write-wins, and it is nondeterministic because same-second delivery order is not guaranteed.",
        "failureScenario": "At checkout completion Stripe emits `customer.subscription.created` (status 'incomplete', sub id S) and `customer.subscription.updated` (status 'active', sub id S) both with created=T. The webhook applies 'created' first: lastBillingEventAt=T, subscriptionStatus='incomplete'. The 'updated'(active) event arrives with eventAt=T, fails `lt: T` at L230 -> 0 rows updated -> dropped. The tenant is left stuck at 'incomplete' (and, e.g., excluded from listActiveSubscribers which requires active/trialing) until some later out-of-second event happens to arrive. The same applies to any two genuinely-distinct same-second events where the second one carries the authoritative final status/plan.",
        "suggestedFix": "Do not rely on second-granularity event.created for ordering distinct events. Either use `lte` combined with an explicit event-id idempotency table (so true replays are caught by id, not by equal timestamp), or order by a finer/monotonic key (Stripe delivers events with strictly increasing ids you can persist and compare), so two distinct events in the same second are both applied in the correct order rather than the second being dropped.",
        "verifierNotes": [
          "PLAUSIBLE: The code property is exactly as described and is NOT handled anywhere. tenants.ts:230 gates the write with strict `lt` on `lastBillingEventAt` (no tiebreaker), tenants.ts:255 writes `lastBillingEventAt: eventAt`, and billing.ts:237 derives eventAt from `new Date(event.created * 1000)` where Stripe's `event.created` is one-second granularity. The comment at tenants.ts:212-214 explicitly equates an equal eventAt with a replay/no-op, so two DISTINCT same-second events for one customer collapse to first-applied-wins, with the second matching zero rows and being silently dropped. Delivery order for same-second events is not guaranteed by Stripe, so this is nondeterministic. No test covers it: all monotonic-guard tests (billing.spec.ts:196-251) use distinct created values. So the mechanism is genuinely present and unrefuted.\n\nHowever, I could not confirm the HARMFUL half of the claim. The specific asserted sequence — subscription.created(incomplete) + subscription.updated(active) both at the same second — is not a normal Stripe pattern: the incomplete→active transition is gated on payment/SCA confirmation and typically spans more than a second, and a synchronous successful Checkout subscription is usually born 'active' with no incomplete stage. The same-second pairs Stripe actually does emit (e.g. created+updated both already active) generally carry equivalent status/price, making the dropped write benign, and any genuinely divergent state is corrected by the next later-second lifecycle event. I could not construct a concrete, persistent, harmful mis-state that provably occurs. Thus the defect is a real unhandled code property but with speculative impact — short of CONFIRMED, not REJECTED. Severity low is appropriate.",
          "CONFIRMED: CONFIRMED. The code at tenants.ts:230 gates writes with strict `lt` (`lastBillingEventAt: { lt: eventAt }`) and writes `lastBillingEventAt: eventAt` (L255), where eventAt = new Date(event.created * 1000) (billing.ts:237). Stripe's event.created is second-granular and the code itself uses it as the sole ordering key (comment L228) with no secondary tiebreak. The guard's design explicitly equates equal-eventAt with a replay (comment L213), so two GENUINELY DISTINCT subscription events that share a `created` second are indistinguishable from a duplicate: whichever applies first advances lastBillingEventAt to T, the second matches zero rows (result.count > 0 → false, L258) and is silently dropped — last-write-loses.\n\nThe harm path is concrete. At checkout completion both created(incomplete) and updated(active) carry the same sub id; the active one is LIVE so incomingLive=true (L225) skips the per-subscription guard, leaving only the strict-`lt` monotonic guard. If created is applied first, the same-second updated(active) fails `lt: T`, is dropped, and the tenant is stuck at 'incomplete' — and excluded from listActiveSubscribers (L262 requires active/trialing) — until a later out-of-second event arrives. The same applies to two same-second customer.subscription.updated events during a plan change where the second carries the authoritative final price/period.\n\nNo test handles this: billing.spec.ts ordering tests (L196-250) always use distinct seconds for distinct events (100/200/300/400) and exercise equal-second only as a replay of the SAME event id (L206-207). So the suite bakes in the conflation rather than guarding against it.\n\nRefutation attempts failed: (a) the per-subscription guard does not help because a LIVE incoming event bypasses it; (b) same-second distinct Stripe events are a well-established real possibility, not hypothetical; (c) delivery/apply order is not guaranteed, so an unfavorable order that produces the wrong final state genuinely exists. Severity is correctly low: it self-heals on the next out-of-second event and does not touch the money invariant (commission accrual is a separate path idempotent on invoice.id, billing.ts:244-249). The defect — strict `lt` on a second-granular key conflating distinct same-second events with replays, causing last-write-loses drop of an authoritative subscription state — is real and can occur."
        ]
      },
      {
        "severity": "low",
        "category": "efficiency",
        "file": "packages/db/src/repos/affiliates.ts",
        "line": 157,
        "title": "No index supports the hot-path window-anchor lookup on commissions(affiliateId,tenantId) ordered by createdAt",
        "description": "The per-invoice accrual path runs prisma.commission.findFirst({ where: { affiliateId, tenantId }, orderBy: { createdAt: 'asc' } }) on every invoice.payment_succeeded webhook (affiliates.ts:157) to find the window anchor. The commissions table has exactly three indexes (migration 20260803120000_affiliates L42/45/48): unique(sourceInvoiceId), unique-none-else, and @@index([affiliateId, status]). None covers this query: the (affiliateId,status) index can seek the affiliateId prefix but status is not in the predicate, so Postgres must read every commission row for that affiliate, filter tenantId in memory, and sort createdAt to pick the earliest. For a high-volume affiliate with many referred-tenant payments this scan grows unboundedly on the synchronous webhook path.",
        "failureScenario": "A popular affiliate accumulates thousands of commission rows across many referred tenants. Each subsequent invoice.payment_succeeded webhook does a growing index-prefix scan + in-memory sort to recompute the anchor, adding latency to the Stripe-facing handler as the table grows.",
        "suggestedFix": "Add @@index([affiliateId, tenantId, createdAt]) (a new numbered/append-only migration) so the anchor findFirst is an index-only seek to the first row.",
        "verifierNotes": [
          "CONFIRMED: Verified against actual code. affiliates.ts:157 runs prisma.commission.findFirst({ where:{affiliateId,tenantId}, orderBy:{createdAt:'asc'} }) synchronously inside accrueForPaidInvoice, which billing.ts invokes on every invoice.payment_succeeded webhook. The only indexes on commissions (migration 20260803120000 L45/L48 and schema.prisma L189) are unique(sourceInvoiceId) and @@index([affiliateId, status]); the second migration adds none to commissions. The (affiliateId,status) index leads with affiliateId so it can seek that prefix, but tenantId is not in the index and createdAt gives no usable ordering from it — Postgres must scan all of the affiliate's index entries, filter tenantId, and sort createdAt to take the earliest. No index covers (affiliateId, tenantId, createdAt). Rows per affiliate grow as referred_tenants × ~commissionMonths and are never deleted, so the anchor scan grows on the synchronous webhook path. Every particular of the claim checks out. Impact is genuinely low (index-localized to one affiliate, not a full-table scan; webhook already does several comparable queries; realistic volumes are modest), consistent with the stated low/efficiency severity, but the missing-index defect and growing-scan behavior are provable from the code, so this is CONFIRMED rather than merely plausible.",
          "CONFIRMED: CONFIRMED. The query at affiliates.ts:157 (prisma.commission.findFirst where {affiliateId, tenantId} orderBy createdAt asc) runs on the synchronous invoice.payment_succeeded webhook via accrueForPaidInvoice. The commissions table (migration 20260803120000_affiliates) has only: PK(id), UNIQUE(sourceInvoiceId), and INDEX(affiliateId, status). Postgres does NOT auto-index FK columns, so there is no (affiliateId, tenantId) or (affiliateId, tenantId, createdAt) index. The only usable index for this query is (affiliateId, status): with no status predicate, the planner can seek only the affiliateId equality prefix, then must Filter tenantId and Sort createdAt in memory (LIMIT 1 can't short-circuit since the index provides no createdAt order within the tenant subset). Rows scanned scale with the affiliate's total commission count (≈ referred-tenant-count × commissionMonths), so a high-volume affiliate's monthly webhooks perform a widening prefix scan + sort on the Stripe-facing path. No test checks query plans and no code mitigates it. Adversarial refutations fail: FKs aren't auto-indexed, the sourceInvoiceId unique index is irrelevant to the predicate, and the scan is affiliate-scoped not pair-scoped. The finding is factually accurate; 'unbounded' is mild hyperbole (bounded by business volume) but the mechanism and missing index are real. Genuinely low severity, but a real, verifiable inefficiency."
        ]
      },
      {
        "severity": "medium",
        "category": "test-coverage",
        "file": "apps/api/src/routes/partner.ts",
        "line": 111,
        "title": "Sibling set-password-token invalidation has zero test coverage",
        "description": "set-password redemption calls invalidatePwTokens (partner.ts:111 → affiliates.ts:198-199) with the comment \"burn any sibling outstanding tokens so only the newest link ever worked.\" No test in partner.spec.ts or affiliates.spec.ts ever issues TWO outstanding set-password tokens for one affiliate and asserts that redeeming one kills the other. The single-use test (partner.spec.ts:145-151) only redeems the SAME token twice — it proves per-token single-use, not cross-token invalidation. consumePwToken already marks the redeemed token used, so the entire sibling-burn behavior (the WHERE affiliateId, usedAt:null updateMany) is exercised by nothing.",
        "failureScenario": "platform_admin issues set-password token A, then re-sends the invite issuing token B (both live, 24h TTL). Partner redeems B. If invalidatePwTokens were dropped, its WHERE clause regressed, or the call reordered, token A stays valid — a stale credential-setting link an attacker who intercepted the first invite email can still redeem to seize the partner account. This regression ships green because no test asserts A is dead after B is consumed.",
        "suggestedFix": "Add a test: create affiliate, createPwToken A and B, consumePwToken(B) via /v1/partner/set-password, then assert consumePwToken(A) → null (token A invalidated).",
        "verifierNotes": [
          "CONFIRMED: Verified by reading the actual code and both relevant test files. The sibling-burn behavior lives at partner.ts:111 (invalidatePwTokens call) and affiliates.ts:198-199 (updateMany over usedAt:null tokens for the affiliate). It is the ONLY mechanism that kills a still-outstanding sibling set-password token after another is redeemed; consumePwToken only single-uses the token actually redeemed.\n\nNo test covers it: (1) partner.spec.ts:145-151 issues one token and redeems the SAME token twice — the second 400 comes from consumePwToken's usedAt guard, proving per-token single-use only, not cross-token invalidation, and no sibling ever exists in that test. (2) affiliates.spec.ts:89-109 tests consumePwToken single-use + expiry and never calls invalidatePwTokens; the two tokens it creates (tokhash-1 consumed, tokhash-2 expired) are used to prove single-use and expiry, never to prove that consuming one live token invalidates another live sibling. (3) A repo-wide grep shows invalidatePwTokens referenced in test code only as a notImpl mock stub (helpers/auth.ts:57).\n\nRefutation attempts fail: deleting the invalidatePwTokens call at partner.ts:111 would leave every existing test green because single-use is enforced separately by consumePwToken. The failure scenario is reachable — the platform_admin set-password-token route can be invoked twice, yielding two live 24h tokens, and only invalidatePwTokens closes the first after the second is redeemed. The coverage gap and the ungated regression path are both real.",
          "CONFIRMED: The test-coverage gap is real and precisely as described. partner.ts:111 calls db.affiliates.invalidatePwTokens(affiliateId, now) — affiliates.ts:198-199 — to burn sibling outstanding set-password tokens. This is genuinely separate behavior from consumePwToken (affiliates.ts:189-196), which atomically claims ONLY the matching tokenHash and leaves other tokens (different hashes) untouched. Without invalidatePwTokens, a second live token stays usedAt:null and redeemable.\n\nNo test issues TWO outstanding set-password tokens for one affiliate and asserts that redeeming one kills the other: (1) partner.spec.ts:145-151 issues one token and redeems the SAME token twice — proves per-token single-use, not cross-token invalidation; (2) affiliates.spec.ts:89-109 calls consumePwToken directly (never invoking invalidatePwTokens) with tokhash-1 (consumed/reused) and tokhash-2 (created EXPIRED) — no two-live-token sibling case; (3) grep of invalidatePwTokens across all tests hits only the notImpl mock in helpers/auth.ts:57. Every API set-password call does invoke invalidatePwTokens, but always with zero sibling rows outstanding, so the updateMany affects nothing observable and no assertion depends on its effect.\n\nThe scenario is reachable: POST /v1/affiliates/:id/set-password-token can be called repeatedly (re-send invite), both tokens carry 24h TTL, and nothing dedups issuance, so two live tokens is a normal state. A regression that dropped the call, broke the WHERE affiliateId/usedAt:null clause, or reordered it would pass all existing tests while leaving a stale credential-setting link live — a real account-seizure vector. Refutation attempts failed: the burn is not redundant with consumePwToken (different hashes survive), the path is not covered indirectly (no test reaches it with a live sibling), and the scenario is genuinely reachable. This is a valid medium-severity test-coverage finding, not a code bug — the code itself is correct but its security-relevant sibling-burn behavior has zero assertions."
        ]
      },
      {
        "severity": "medium",
        "category": "test-coverage",
        "file": "apps/api/src/routes/partner.ts",
        "line": 79,
        "title": "Partner login brute-force lockout (429) is completely untested",
        "description": "The partner login limiter (partner.ts:78-92) has its OWN hardcoded threshold LOGIN_RL_MAX=10 per IP+email, separate from the tenant-login lockout config, plus a Retry-After header and the increment-on-failure/del-on-success logic. No test drives repeated failed logins to hit the 429 path. partner.spec.ts:122-133 asserts a single 401 for a wrong password but never exhausts the counter, and the app is built with lockout:{maxFails:100} which does not govern this limiter at all. The RL_SCRIPT eval, the >= LOGIN_RL_MAX gate, and the del(lockKey)-on-success reset are all uncovered. The set-password limiter (REDEEM_RL_MAX=30, partner.ts:101) is likewise untested.",
        "failureScenario": "If the lockKey composition, the LOGIN_RL_MAX comparison, or the RL_SCRIPT EXPIRE re-arm regressed (e.g. off-by-one, wrong key, TTL-less key), the partner surface loses brute-force protection silently — an attacker can grind passwords against a partner email indefinitely — and every existing test still passes because none of them makes the 11th attempt.",
        "suggestedFix": "Add a test that posts 10 wrong-password logins then asserts the 11th returns 429 with a Retry-After header, and that a subsequent correct login after del/reset still succeeds.",
        "verifierNotes": [
          "CONFIRMED: Verified against the actual code and the full test suite. partner.ts:78-93 implements a partner-specific brute-force limiter (hardcoded LOGIN_RL_MAX=10, lockKey partner:fail:${ip}:${sha256(email).slice(0,16)}, RL_SCRIPT INCR/EXPIRE on failure, Retry-After 429 gate, del(lockKey) reset on success) plus a second set-password limiter (REDEEM_RL_MAX=30, line 101). partner.spec.ts exercises exactly one wrong-password login (line 124) asserting a single 401 and never reaches the 11th attempt; distinct per-test emails plus the fixed getRemoteAddr keep counters from accumulating, so the >=LOGIN_RL_MAX gate, the 429/Retry-After response, the EXPIRE re-arm, and the del-on-success reset are all uncovered. The app's lockout:{maxFails:100} config governs only the tenant login limiter in login.ts (separately tested at auth.spec.ts:343), not this one. A whole-suite grep for 429/Retry-After/partner:fail/partner:redeem confirms no 429 test targets the partner surface. The set-password 429 limiter is also untested (only 2-3 redeem calls in existing tests). This is a genuine, real test-coverage gap on a public unauthenticated security-relevant endpoint: a regression in key composition, threshold, or TTL would pass all current tests silently. Every specific claim (file:line, thresholds, key names, the single-401 test) matches reality; I could not refute it.",
          "CONFIRMED: The partner login/set-password brute-force limiters in apps/api/src/routes/partner.ts are governed by their OWN hardcoded thresholds (LOGIN_RL_MAX=10 at line 79/43, REDEEM_RL_MAX=30 at line 101/44), independent of the app's lockout:{maxFails:100} config (that config drives the tenant login.ts limiter). partner.spec.ts is the only partner test file and it never exhausts the counter: the login-rejection test (lines 122-133) issues at most a few failed attempts across DIFFERENT emails and asserts only 401. No test reaches the 11th attempt against one IP+email, so the `>= LOGIN_RL_MAX` gate, the 429 response, the `Retry-After: ttl` header (lines 80-82), and the `redis.del(lockKey)` reset-on-success (line 93) are never asserted. A repo-wide grep confirms 429 lockout tests exist for reports, tenant auth, routing, branding, apiKeys, pilotRequest, and shares, but there is zero 429/Too-Many/partner:fail/partner:redeem/LOGIN_RL_MAX coverage in partner.spec.ts or the db tests. The increment eval (line 90) executes as a side effect of the existing wrong-password assertions, but the actual brute-force protection behavior is unverified — a regression in the lockKey composition, the threshold comparison, or the EXPIRE re-arm would keep every existing partner test green while silently disabling the lockout. The test-coverage gap is real and verifiable; I could not find any test that refutes it."
        ]
      },
      {
        "severity": "low",
        "category": "test-coverage",
        "file": "apps/api/src/routes/crud.ts",
        "line": 1301,
        "title": "Commission 'void' status transition is exercised by no test",
        "description": "commissionStatusSchema (entities.ts:243) and setCommissionStatus (affiliates.ts:176-179 via PATCH /v1/commissions/:id, crud.ts:1301) accept 'void', and it is wired through i18n (en/lt/pl/de.json:1049) and the web COMMISSION_TONE map (affiliates.tsx:22). But every test only ever sets 'paid' (db affiliates.spec.ts:84, api affiliates.spec.ts:138 only 404s an unknown id), and the web UI only ever emits 'paid' (affiliates.tsx:136 — there is no void button). 'void' is the money-reversal state (a commission on a refunded/clawed-back invoice), so a payout-integrity operation has no test and no UI path at all.",
        "failureScenario": "A regression tightening commissionStatusUpdateSchema, or a setCommissionStatus change that mishandles the 'void' value, would pass CI because no test ever PATCHes a commission to 'void'; separately, an admin cannot void a commission from the UI at all, so a refund clawback has no supported flow and no test guarding it.",
        "suggestedFix": "Add a test that accrues a commission and PATCHes it to 'void' (asserting the returned status), and expose a void action in CommissionsPanel to cover the refund-reversal path.",
        "verifierNotes": [
          "CONFIRMED: Every factual claim in the finding is verified against the real code. commissionStatusSchema (packages/shared/src/entities.ts:243) is z.enum(['pending','paid','void']), so 'void' is a valid API input to PATCH /v1/commissions/:id. setCommissionStatus (packages/db/src/repos/affiliates.ts:176-179) forwards any status value directly into prisma.commission.update({ data: { status } }) with no per-value handling, so 'void' is a fully supported transition. 'void' is also wired into the presentation layer: COMMISSION_TONE (apps/web/src/routes/app/affiliates.tsx:22) maps void→'neutral', and i18n exposes commissionStatus.void. Yet grepping all four spec files (packages/db/__tests__/affiliates.spec.ts, apps/api/__tests__/{affiliates,billing,partner}.spec.ts) for 'void' returns zero status-related hits: db spec:84 only sets 'paid'; api spec:138 only asserts a 404 for an unknown id using status:'paid'. The web UI likewise only ever emits 'paid' — markPaid (affiliates.tsx:135-136) hardcodes setCommissionStatus(id,'paid') and the sole action button (167-171) is the mark-paid button shown for pending rows; there is no void button anywhere. Thus the 'void' payout-reversal state is genuinely untested (a regression tightening the schema or mishandling 'void' in setCommissionStatus would pass CI) and has no admin UI path to trigger a refund clawback. This is a real, low-severity test-coverage plus missing-UI gap, grounded in the actual files. I tried to refute it by looking for a hidden void button in the commissions table or a void-path test, and found neither.",
          "CONFIRMED: Every factual assertion is grounded in the actual code. 'void' is a live, intended API capability: commissionStatusSchema (entities.ts:243) and commissionStatusUpdateSchema (263) accept it; setCommissionStatus (affiliates.ts:176-179) writes it verbatim; it is reachable via PATCH /v1/commissions/:id, whose own comment (crud.ts:1294) documents \"PATCH marks one paid/void\"; and it is fully wired into i18n (en/lt/de/pl.json:1049) and the web COMMISSION_TONE map + CommissionStatus type (affiliates.tsx:22, affiliates.ts:9). Yet no test ever exercises it: packages/db/__tests__/affiliates.spec.ts:84 only sets 'paid'; apps/api/__tests__/affiliates.spec.ts:138 only PATCHes 'paid' against an unknown id (404 path); grepping both spec files for 'void' yields nothing relevant. The web UI has no void affordance — the sole mutation is markPaid→'paid' (affiliates.tsx:136), rendered only for status==='pending' (167-171); the void tone is display-only. No backend refund/clawback path sets 'void' either, so the only way to reach it is the untested PATCH endpoint. Thus a regression tightening the schema or mishandling the 'void' value in setCommissionStatus would pass CI unnoticed. This is an accurate test-coverage gap on a money-reversal state; severity=low is appropriate. Not REJECTED because the capability is genuinely reachable and untested, not dead code; the finding proves out."
        ]
      },
      {
        "severity": "medium",
        "category": "correctness",
        "file": "packages/db/src/repos/affiliates.ts",
        "line": 159,
        "status": "FIXED (PR #162) — `Commission.ratePct` + `baseAmountCents` pin the rate and the base with the entry, and the WINDOW is pinned too: `tenants.commissionAnchorAt` + `commissionMonthsAtAnchor` are stamped at the tenant's first PAYMENT and never recomputed, so an admin edit can neither re-price history nor re-open a closed window. Verified in packages/db/__tests__/affiliates.spec.ts.",
        "title": "Commission terms are read live, not snapshotted — editing commissionMonths reopens a legitimately-closed commission window and over-pays",
        "description": "accrueForPaidInvoice computes the window boundary as addMonthsUtc(anchor, affiliate.commissionMonths) and the amount as floor(amountPaid * affiliate.commissionPct / 100), reading BOTH terms LIVE from the current Affiliate row (affiliates.ts L159-160). Neither the rate nor the month term is snapshotted anywhere: the Commission model (schema.prisma L175-190) stores only amountCents/currency — no rate_pct and no base amount — even though PROJECT_PLAN.md L232 explicitly mandates commission_entries carry (base_amount_cents, rate_pct, amount_cents) precisely so each entry pins the terms it was accrued under. Because commissionMonths (and commissionPct) are freely editable via PATCH /v1/affiliates/:id (affiliateUpdateSchema, entities.ts L255-262; crud.ts L1278-1284), a single admin edit silently re-prices and can REOPEN the commission window for EVERY tenant that affiliate already referred, retroactively — the anchor (first commission's createdAt) is unchanged, but the multiplier applied to it is mutated. This is a different root cause from the already-reported suspend/reactivate anchor drift (which moves the anchor); here the anchor is untouched and the boundary length itself is rewritten. Note the window is recomputed on EVERY invoice, so the change applies to future invoices of long-existing, past-window referrals, not just to new referrals.",
        "failureScenario": "Affiliate A is created with commissionMonths=6 and refers tenant T in 2026-01. T pays monthly; the first Commission row is inserted ~2026-01 (anchor). Payments through 2026-07 accrue; 2026-08+ invoices return null because paidAt > addMonthsUtc(2026-01, 6) — the window has legitimately closed. In 2027-03 a platform_admin edits A to commissionMonths=24 (to match a newer partner arrangement, a policy bump, or a fat-finger). On T's next renewal invoice (e.g. 2027-04), accrueForPaidInvoice recomputes boundary = addMonthsUtc(2026-01, 24) = 2028-01; 2027-04 < 2028-01, so it is judged IN-WINDOW and a fresh commission accrues on T again — paying the affiliate on a tenant whose contracted 6-month commission term ended nearly a year earlier. Symmetrically, raising commissionPct re-prices all still-in-window invoices of existing referrals at the higher rate with zero record of the originally agreed rate. Net: real over-pay / commission-revenue leak with no per-entry audit of the terms actually applied.",
        "suggestedFix": "Snapshot the commercial terms at attribution (or at first accrual) rather than reading them live: store rate_pct and commissionMonths (or a concrete windowEndsAt) on the Commission and/or on Tenant.referredBy* at the moment the referral is bound, and have accrueForPaidInvoice use those pinned values for both the amount and the window boundary. Minimally, add rate_pct and base_amount_cents columns to Commission per PROJECT_PLAN.md L232 so each entry records the rate it was accrued under, and freeze the window from a stored per-tenant anchor+term instead of the live affiliate row.",
        "verifierNotes": [
          "CONFIRMED: The defect is real and reproducible from the actual code. accrueForPaidInvoice reads BOTH commission terms live from the current Affiliate row: the window boundary is addMonthsUtc(anchor, affiliate.commissionMonths) (affiliates.ts:159) and the amount is floor(amountPaidCents * affiliate.commissionPct / 100) (affiliates.ts:160). The anchor is the FIRST existing commission's createdAt (L157-158), which editing the terms never touches — so an edit rewrites the boundary length / rate multiplier while leaving the anchor fixed. This is distinct from any suspend/reactivate anchor-drift issue.\n\nNo snapshot exists: the Commission model (schema.prisma L174-192) stores only amountCents + currency — there is NO rate_pct and NO base_amount_cents, in direct violation of PROJECT_PLAN.md §6.9 (L232) which mandates commission_entries carry (base_amount_cents, rate_pct, amount_cents) specifically to pin each entry's terms.\n\nThe terms are freely and retroactively editable: affiliateUpdateSchema is a .partial() exposing commissionPct and commissionMonths (entities.ts L255-262), and PATCH /v1/affiliates/:id (crud.ts:1278-1284) passes them straight into prisma.affiliate.update (affiliates.ts:135) with no restriction.\n\nThe window is recomputed on every invoice and idempotency is keyed only on the unique sourceInvoiceId, so a NEW invoice after a legitimately-closed window is not deduped. Worked scenario: anchor 2026-01 with commissionMonths=6 closes the window at 2026-07 (2026-08+ return null); a later PATCH to commissionMonths=24 makes boundary=2028-01, so the 2027-04 invoice is judged in-window and accrues a fresh commission on a tenant whose 6-month term ended ~9 months earlier — real over-pay. Symmetrically, raising commissionPct re-prices all still-in-window invoices with no record of the originally agreed rate.\n\nI attempted refutation by searching for a snapshot column, a guard blocking these fields in the update path, a once-closed-stays-closed flag, and re-accrual-blocking idempotency; none exist. Severity medium is appropriate: it requires a platform_admin edit (a normal operational action — policy bump or fat-finger) and produces a genuine, unauditable commission-revenue leak.",
          "CONFIRMED: Grounded in read code. accrueForPaidInvoice (packages/db/src/repos/affiliates.ts:159-160) reads BOTH commission terms live from the current Affiliate row every invoice: window boundary = addMonthsUtc(anchor, affiliate.commissionMonths) and amount = floor(amountPaidCents * commissionPct / 100). The anchor (L157-158) is the FIRST commission's createdAt and is never mutated by a term edit, so raising commissionMonths lengthens the boundary retroactively. The Commission model (schema.prisma, model Commission) stores only amountCents + currency — no rate_pct, no base_amount_cents — even though PROJECT_PLAN.md §6.9 (L232) explicitly specs commission_entries(... base_amount_cents, rate_pct, amount_cents ...) precisely to pin per-entry terms; the code diverges. Both terms are freely editable: affiliateUpdateSchema is a .partial() including commissionPct/commissionMonths (entities.ts:255-262); the PATCH /v1/affiliates/:id handler (crud.ts:1278-1284) passes data straight into prisma.affiliate.update with no filtering (affiliates.ts:135). Because the window is recomputed on every invoice, a single admin edit of commissionMonths (e.g. 6→24) makes a previously-closed window judge future invoices IN-window and accrue fresh commissions on tenants whose contracted term expired — a real over-pay — and raising commissionPct re-prices all still-in-window invoices with no record of the originally agreed rate. Refutation attempts fail: no snapshot column exists, no test guards term-pinning (affiliates.spec.ts windowing test never edits terms post-accrual), and the trigger is one ordinary platform_admin PATCH via an endpoint built for exactly that edit. Distinct root cause from any anchor-drift finding: here the anchor is untouched and the multiplier/boundary itself is mutated. Money-correctness defect that can actually occur; medium severity fits (requires a privileged edit but leaves no per-entry audit and causes genuine commission-revenue leak)."
        ]
      },
      {
        "severity": "low",
        "category": "auth-token-lifecycle",
        "file": "apps/api/src/routes/partner.ts",
        "line": 142,
        "title": "Set-password tokens are never revoked on re-issue or on suspension — superseded/post-offboarding links stay redeemable",
        "description": "Outstanding partner set-password tokens are only invalidated AFTER a successful redeem (partner.ts L111 invalidatePwTokens, called inside the set-password handler). Nothing invalidates prior unused tokens when (a) the admin issues a NEW token — issuePartnerSetPwToken (L142-146) only inserts via createPwToken, so multiple unused 24h tokens for one affiliate coexist and each remains independently redeemable; or (b) the admin SUSPENDS the affiliate — the set-password handler (L100-114) and consumePwToken/setPassword (affiliates.ts L183-197) perform NO status check, so a suspended/pending affiliate's password can still be set (HTTP 200). The partner.spec.ts makePartner('...','suspended') helper itself proves set-password returns 200 for a suspended affiliate. Net effect: an admin who re-issues a link (believing it rotates access) or who suspends a partner for fraud (believing that cuts off self-service) is wrong — an older/leaked link is still valid. If a superseded/suspended-era link is redeemed, whoever holds it sets the login password (there is no email-confirmation step, per the code's 'email wiring is a follow-up'); for a suspended affiliate the attacker-chosen password becomes live the moment any admin re-activates the affiliate.",
        "failureScenario": "Admin issues set-password link L1 for affiliate A and conveys it; it leaks. Admin later re-issues L2 (thinking L1 is now void) OR suspends A for fraud. L1 is still valid: attacker POSTs /v1/partner/set-password with L1 -> 200, password set. On A's next re-activation the attacker logs in to A's partner portal. Also: two concurrently-outstanding tokens (L1,L2) both redeem, last-writer sets the final password.",
        "suggestedFix": "On issuePartnerSetPwToken, first call invalidatePwTokens(affiliateId, now) so a fresh link supersedes all prior ones (single outstanding token invariant). Gate the set-password handler on affiliate.status !== 'suspended' (reject with 400/409). Optionally invalidate outstanding set-password tokens when an affiliate is suspended in affiliates.update.",
        "verifierNotes": [
          "CONFIRMED: Both factual pillars hold in the actual code. (a) issuePartnerSetPwToken (partner.ts L142-146) only INSERTs via createPwToken; nothing invalidates prior unused tokens on re-issue. invalidatePwTokens runs only AFTER a successful redeem (L111), so multiple 24h tokens coexist pre-redeem, each independently redeemable (consumePwToken matches tokenHash/usedAt:null/expiresAt>now only). (b) The set-password handler (L100-114) and consumePwToken/setPassword (affiliates.ts L183-197) perform NO affiliate-status check, unlike login (L89) and partnerAuth (L58), which are active-only. partner.spec.ts makePartner(...,'suspended') PATCHes to suspended FIRST (L48) then redeems set-password asserting 200 (L51), and L131 exercises it — so the suite itself proves set-password returns 200 for a suspended affiliate. update() (affiliates.ts L132-136) clears neither password nor tokens on re-activation, so an attacker-set password during suspension persists and goes live on any later re-activation without a reset; login/portal access is correctly blocked only WHILE suspended (the L135-143 test proves live-token revocation, which the claim already concedes). Refutation attempts fail on the core: the 'both-tokens-redeem race' is not a real escalation (first redeem burns siblings; needs attacker holding both), and suspension blocks live access but NOT the set-password write. The described behavior is definite and provable, and the superseded-link / post-suspension redeem scenario can actually occur — a genuine low-severity auth-token-lifecycle / defense-in-depth gap. The remedy is to invalidate outstanding tokens on re-issue and to gate consumePwToken/setPassword (and login already does) on a non-suspended status, plus invalidate tokens on suspension.",
          "CONFIRMED: Both mechanisms are directly evidenced in real code. (a) issuePartnerSetPwToken (partner.ts L142-146) only calls createPwToken and never invalidatePwTokens; the admin route (crud.ts L1287-1293) adds no pre-invalidate. consumePwToken (affiliates.ts L189-197) matches ANY unused/unexpired token, so multiple concurrently-issued tokens are each independently redeemable until 24h expiry or a redeem burns siblings. The code's own comment (partner.ts L110 'so only the newest link ever worked') overstates a guarantee it does not provide, since sibling-burn only fires at redeem time — an older leaked token can be redeemed first and win. (b) No status check exists in consumePwToken, setPassword (affiliates.ts L183-197), or the set-password handler (partner.ts L100-114); partner.spec.ts makePartner(...,'suspended') asserts set-password returns 200 for a suspended affiliate, proving the behavior. Mitigations exist and match the stated low severity: login (L89) and partnerAuth (L58) both require status==='active', so a suspended partner cannot authenticate and the attacker-chosen password only becomes usable on admin re-activation. My refutation attempts (searching for a pre-invalidate on re-issue, a status gate in the redeem path, and window-closing via expiry/sibling-burn) all failed. The described behaviors genuinely occur and the scenario is realistic, so the low-severity auth-token-lifecycle defect is real."
        ]
      },
      {
        "severity": "medium",
        "category": "data-leakage",
        "file": "apps/api/src/routes/crud.ts",
        "line": 1282,
        "title": "PATCH /v1/affiliates/:id serializes the raw Affiliate row, leaking passwordHash (distinct handler from the already-reported GET leak)",
        "description": "An earlier round flagged the GET affiliate routes (list / get) leaking the partner argon2id passwordHash. The same leak exists on a SEPARATE, untouched handler: the PATCH /v1/affiliates/:id update handler returns `json(c, row)` where `row` is the full Prisma Affiliate model returned by `AffiliateRepo.update` (packages/db/src/repos/affiliates.ts:132-136 → `prisma.affiliate.update({ where:{id}, data })`, which selects every column including `passwordHash`). Because the repo returns the raw model everywhere (list/get/update) and the leak was reported only for the GET routes, a narrow remediation that adds a field projection to the two GET handlers will leave this PATCH path exfiltrating the hash. Note POST /v1/affiliates (crud.ts:1264-1265) also returns the raw row but at create time passwordHash is always null, so only the PATCH path (invoked once a partner has set a password and the admin later edits name/status/commission terms) actually discloses a live credential hash. The fix must project the returned row (or add a repo-level DTO/select) on every affiliate-returning route, not just the GETs.",
        "failureScenario": "Partner sets a self-service password (passwordHash now populated). A platform_admin later PATCHes that affiliate (e.g. status pending→active, or edits commissionPct). The 200 response body includes `passwordHash` = the partner's argon2id hash, exposing it to anyone who can read that admin API response (browser devtools, proxy logs, SPA state). A GET-only fix for the previously-reported leak does not close this handler.",
        "suggestedFix": "Introduce a single serializer/DTO for the Affiliate entity (id, name, email, code, commissionPct, commissionMonths, status, createdAt — never passwordHash) and apply it to GET list, GET :id, POST, and PATCH handlers; or have the repo methods `select` the safe columns so no route can ever emit passwordHash.",
        "verifierNotes": [
          "CONFIRMED: The defect is real and reproducible.\n\nEvidence chain:\n1. PATCH handler (apps/api/src/routes/crud.ts:1278-1284): `const row = await db.affiliates.update({ userId: auth(c).userId }, id(c), data)` then `return ... json(c, row)`. This is a distinct handler from the GET list (1250-1251) and GET :id (1252-1256) handlers.\n2. Repo update (packages/db/src/repos/affiliates.ts:132-136): `return prisma.affiliate.update({ where: { id }, data })` — NO `select`/projection, so Prisma returns the full Affiliate model including every column.\n3. Schema (schema.prisma:125-145): the Affiliate model has `passwordHash String?` (argon2id hash for partner self-service login, line 138), populated once a partner sets a password.\n4. Serialization (crud.ts:173-181): `json()` -> `toJson()` only stringifies bigints; it performs ZERO field redaction. So the raw row, passwordHash included, is written verbatim to the 200 response body.\n\nNo compensating control exists: there is no outbound redaction middleware, no DTO/projection at the route, and no test asserting passwordHash absence (grep for passwordHash/redact/sanitize/omit in apps/api/__tests__/affiliates.spec.ts returned nothing).\n\nScenario is valid: a partner sets a password (passwordHash populated via setPassword at repo line 183-185), then a platform_admin PATCHes the affiliate (e.g. status pending->active, or edits commissionPct/commissionMonths — all fields in AffiliateUpdate). The 200 body then discloses the live argon2id hash to whoever reads that admin API response (devtools, proxy logs, SPA state). POST /v1/affiliates (1257-1277) also returns the raw row but passwordHash is always null at create time, so PATCH is the path that leaks a live credential hash.\n\nA narrow fix that only projects the two GET handlers leaves this PATCH path (and the POST/create path shape) exfiltrating the hash — matching the claim. Correct remediation is a repo-level select/DTO or per-route projection applied to every affiliate-returning handler. Medium severity is apt: it is a hash (not plaintext) exposed only within platform_admin-authorized responses, but it is a genuine credential-material leak with no guard.",
          "CONFIRMED: The PATCH /v1/affiliates/:id handler (apps/api/src/routes/crud.ts:1278-1284) returns json(c, row) where row is the raw Prisma Affiliate model from AffiliateRepo.update (packages/db/src/repos/affiliates.ts:132-136), which calls prisma.affiliate.update({where:{id},data}) with NO select — so every column is returned, including passwordHash (confirmed as a column on model Affiliate, schema.prisma:138, `passwordHash String?`). The serialization path json()/toJson() (crud.ts:173-181) only stringifies BigInts and applies zero field redaction, so the row is emitted verbatim. passwordHash is genuinely populated in the F5 partner self-service auth flow via setPassword (repo:183-185), so after a partner sets a password and an admin later PATCHes name/status/commissionPct, the 200 response body discloses the partner's argon2id hash. No output-schema validation or DTO strips it. The existing test (affiliates.spec.ts:124-127) asserts only the status field and never checks passwordHash absence, and its fixture affiliate has no password set — so the suite does not catch the leak. This is a real, separately reachable leak distinct from the GET handlers; a GET-only fix does not close it. Only mitigation is that it requires platform_admin auth to trigger, consistent with the medium severity; but the credential hash is exposed in the admin API response (devtools/proxy logs/SPA state), so the defect is real."
        ]
      },
      {
        "severity": "low",
        "category": "concurrency-atomicity",
        "file": "apps/api/src/routes/partner.ts",
        "line": 107,
        "title": "Partner set-password sequence (consume → setPassword → invalidate siblings) is not transactional; a partial failure leaves older invite tokens live",
        "description": "The set-password handler performs three independent, separately-awaited repo calls with no wrapping transaction: consumePwToken (L107, atomically burns the redeemed token), setPassword (L109, commits the new argon2 hash), then invalidatePwTokens (L111, burns any sibling unused tokens). Each is its own auto-committed statement. The comment at L110 asserts the invariant \"only the newest link ever worked,\" but that invariant is enforced only if L111 actually runs and commits after L109. Because setPassword commits before invalidatePwTokens, any fault between them (transient DB error, connection drop, pod kill, or the request simply erroring out on L111) leaves the password changed AND every other still-unused set/reset token for that affiliate with usedAt=null and valid until its 24h expiry. The repo exposes these as three separate methods (affiliates.ts L101/105/107) and offers no combined transactional method, so the route cannot make them atomic.",
        "failureScenario": "A platform_admin issues two set-password invite links for one partner (e.g. the first link sat unused in an inbox, so a second is issued). The partner redeems link-2: consumePwToken(token2) succeeds, setPassword(newHash) commits at L109, then invalidatePwTokens throws (DB blip / process killed). token1 keeps usedAt=null and remains redeemable until its TTL. Whoever holds link-1 (an older, possibly-forwarded or leaked invite) can later POST /v1/partner/set-password with token1 and overwrite the partner's password — a partner-account takeover — despite the code's stated single-newest-link guarantee. The endpoint also returns 500 to the legitimate partner even though their password was in fact changed, a confusing half-success.",
        "suggestedFix": "Add a single repo method that runs consumePwToken + setPassword + invalidatePwTokens inside one prisma.$transaction (claim the token, set the hash, and burn all sibling unused tokens for that affiliate atomically), and have the route call that one method. Then the sibling-invalidation can never be skipped after the password write, and a fault rolls the whole redemption back (the token stays unused and retryable) instead of leaving old links live.",
        "verifierNotes": [
          "CONFIRMED: The code matches the claim exactly. In apps/api/src/routes/partner.ts:100-114 the set-password handler runs three independent, separately-awaited repo calls with no wrapping transaction: consumePwToken (L107), setPassword (L109), invalidatePwTokens (L111). In packages/db/src/repos/affiliates.ts these map to three distinct auto-committed Prisma statements (setPassword=affiliate.update L183-185; invalidatePwTokens=affiliatePasswordToken.updateMany L198-200); there is no prisma.$transaction wrapper and no combined method exposed, so the route cannot make them atomic. setPassword (L109) commits BEFORE invalidatePwTokens (L111), so the invariant asserted by the L110 comment (\"only the newest link ever worked\") holds only if L111 actually commits. A fault between L109 and L111 (transient DB error, connection drop, pod kill, or invalidatePwTokens throwing) leaves the password changed while sibling tokens keep usedAt=null and stay redeemable until their 24h TTL — consumePwToken at L189-197 only requires usedAt:null and expiresAt>now, which an untouched sibling still satisfies. That enables a later redemption of an older leaked/forwarded invite link to overwrite the partner's password (partner-account takeover), and the endpoint returns 500 to the legitimate partner despite the password having been persisted (a confusing half-success). Every element of the description is verifiable in the real code; the failure can actually occur. Severity is correctly rated low (it also requires an attacker to hold a valid unexpired CSPRNG invite token, and even absent the bug multiple issued tokens are concurrently live until one is redeemed — invalidatePwTokens is best-effort/defense-in-depth), but the atomicity defect itself is real and confirmed.",
          "PLAUSIBLE: The mechanical claim is accurate and verifiable in the real code. In apps/api/src/routes/partner.ts the set-password handler makes three independent, separately-awaited repo calls with no wrapping transaction: consumePwToken (L107), setPassword (L109), invalidatePwTokens (L111). In packages/db/src/repos/affiliates.ts these map to three distinct Prisma statements (setPassword L183-185 → affiliate.update; invalidatePwTokens L198-200 → updateMany), each auto-committed, and the repo interface exposes no combined transactional method (L101/106/107). So setPassword genuinely commits before invalidatePwTokens; a fault (DB blip, connection drop, pod kill) between L109 and L111 leaves the password changed AND any sibling unused tokens with usedAt=null until their 24h expiry, and returns 500 to the legitimate partner despite a committed change. The comment at L110 (\"only the newest link ever worked\") is only enforced if L111 commits, so relative to the code's own stated invariant the gap is real and reachable, and no test guards transactionality — hence not REJECTED.\n\nHowever, the severity/impact (\"partner-account takeover\") is materially overstated, which is why this is not CONFIRMED. A one-time set/reset token remaining live is within its documented 24h lifetime — consumePwToken still gates on expiresAt: { gt: now } (affiliates.ts L192), and issuing two invite links deliberately creates two valid credentials for the full TTL by design. invalidatePwTokens is proactive early-revocation, explicitly flagged as a \"review LOW\" defense-in-depth nicety, not a hard security boundary. The fault grants no NEW capability: an attacker holding the older token could have redeemed it at any point in its normal 24h life regardless of this gap; the hardening only helps in the narrow race where the legitimate partner redeems first, and only there does the partial failure matter. The atomic single-use consume (the real security boundary) is intact. The exploit needs a compound precondition (≥2 issued links + an older link in a hostile/leaked party's hands + legitimate redemption landing first + a transient fault in the millisecond window between L109 and L111). The atomicity gap and the cosmetic 500 are real, but the takeover consequence cannot be proven and is inflated — it degrades a best-effort cleanup within an already-TTL-bounded token, not a novel takeover primitive. Real mechanism, unprovable/overstated impact → PLAUSIBLE."
        ]
      },
      {
        "severity": "medium",
        "category": "data-model",
        "file": "packages/db/src/repos/affiliates.ts",
        "line": 115,
        "title": "Referral code lookup treats the user-supplied `_` as a SQL LIKE wildcard → mis-attribution to the wrong affiliate",
        "description": "`getActiveByCode` resolves an inbound ?ref value with Prisma `{ equals: code, mode: 'insensitive' }`. I verified against the live tika Postgres (Prisma 6.19.3, query event log) that this compiles to `WHERE \"affiliates\".\"code\" ILIKE $1` with the value bound UNESCAPED (params: [\"PROMO_\"]), not to `LOWER(code)=LOWER($1)` and with no metacharacter escaping. In Postgres `ILIKE` an underscore matches any single character (confirmed: `SELECT 'PROMO1' ILIKE 'PROMO_'` → true). Every ref-attribution path validates the code with `/^[a-zA-Z0-9_-]{...}$/` (entities.ts:233 tenantCreate, :275 signup, :367; pilotRequest), which explicitly ALLOWS `_`. So a ref value containing `_` is interpreted as a wildcard pattern against stored codes. `%` is blocked by the regex, but `_` is not. Compounding it, `getActiveByCode` has no `orderBy`, so when the pattern matches more than one active affiliate `findFirst` returns a nondeterministic row. This is separate from the already-reported case-insensitive-vs-case-sensitive-unique-index issue: that one is about two STORED codes differing only by case; this is LIKE-metacharacter injection in the LOOKUP INPUT that broadens matching across otherwise-distinct codes and misroutes recurring commission money to a partner the tenant never referenced.",
        "failureScenario": "Affiliate A (active) has code AUTUMN1; Affiliate B (active) has code AUTUMNX. A tenant is created (crud.ts:1210) with ?ref=AUTUMN_ (a legal value under the [a-zA-Z0-9_-] regex). getActiveByCode runs `code ILIKE 'AUTUMN_'`, which matches BOTH rows; findFirst (no orderBy) returns whichever the planner yields → tenant.referredByAffiliateId is set to the wrong affiliate, and every subsequent invoice.payment_succeeded accrues that tenant's recurring commission to the wrong partner for commissionMonths. An actor who only knows a code prefix can also attribute to a code they don't fully know (?ref=SUMMER_ matches SUMMER1). Verified: Prisma emits ILIKE with unescaped param, and 'AUTUMN1' ILIKE 'AUTUMN_' is true in the live DB.",
        "suggestedFix": "Do case-insensitive EQUALITY, not pattern matching: either look up via raw SQL `WHERE lower(code) = lower($1)` backed by a `CREATE UNIQUE INDEX ... ON affiliates (lower(code))` (which also closes the case-duplicate-code gap), or escape LIKE metacharacters (`_`,`%`,`\\`) before the insensitive `equals`, and add an explicit `orderBy` so multi-match is at least deterministic. Also drop `_` from the ref regex if underscores are not intended in codes (auto-generated codes already use an alnum alphabet only).",
        "verifierNotes": [
          "CONFIRMED: I reproduced the defect end-to-end against a live Postgres (timescaledb-ha:pg16) with the repo's real Prisma 6.19.3 client and applied migrations, capturing the emitted SQL.\n\nEvery link in the chain holds:\n\n1. Input is unescaped and allows `_`. All ref-attribution schemas in packages/shared/src/entities.ts permit underscore: pilotRequest `ref` = `/^[a-zA-Z0-9_-]{1,64}$/` (line 233), and `affiliateCodeSchema` = `/^[a-zA-Z0-9_-]{3,64}$/` (line 241) used by tenantCreate `ref` (275) and signup `ref` (367). `%` is NOT in the charset (blocked), but `_` IS allowed. So `AUTUMN_` / `SUMMER_` are legal ref values.\n\n2. Prisma compiles the lookup to unescaped ILIKE. packages/db/src/repos/affiliates.ts:115 `getActiveByCode` uses `{ code: { equals: code, mode: 'insensitive' }, status: 'active' }` via findFirst. Live query-event log captured exactly: `WHERE (\"affiliates\".\"code\" ILIKE $1 AND ...status...) LIMIT $3 OFFSET $4` with `PARAMS: [\"AUTUMN_\",\"active\",1,0]`. The value is bound raw — NOT `LOWER(code)=LOWER($1)`, no metacharacter escaping. This confirms the claim's central technical premise, which I did not take on trust.\n\n3. `_` behaves as a single-char wildcard and broadens the match. With two seeded active affiliates AUTUMN1 and AUTUMNX, `getActiveByCode('AUTUMN_')` returned AUTUMN1 — a stored code that is NOT equal to the input. Both rows match the pattern; findFirst has no orderBy, so the returned row is planner-dependent (nondeterministic wrong-affiliate selection). Even a single-match case (`SUMMER_` → SUMMER1) attributes to a code the tenant never typed.\n\n4. The wrong id is persisted and drives recurring money. apps/api/src/routes/crud.ts:1210 passes the raw validated ref straight in: `ref !== undefined ? (await db.affiliates.getActiveByCode(ref))?.id ?? null : null`, writing tenant.referredByAffiliateId. accrueForPaidInvoice (affiliates.ts:146) then accrues every invoice.payment_succeeded to that (wrong) affiliate for commissionMonths, misrouting recurring commission money.\n\nNo test covers `_`/wildcard input; the only case-insensitivity test (packages/db/__tests__/affiliates.spec.ts:143) uses plain alphanumerics (whook1/WHOOK1) and does not exercise metacharacters. Nothing in the code escapes LIKE metacharacters or constrains the match to an exact string. The defect is real, reachable via a legal public ?ref value, and financially material. This is distinct from the separately-reported case-sensitive-unique-index issue: here the flaw is LIKE-metacharacter interpretation of the lookup INPUT broadening matches across otherwise-distinct stored codes.",
          "CONFIRMED: Empirically reproduced against a freshly-migrated Postgres using the repo's own Prisma client (6.19.3). The filter at packages/db/src/repos/affiliates.ts:115 (`{ code: { equals: code, mode: 'insensitive' } }`) compiles to `WHERE \"affiliates\".\"code\" ILIKE $1` with the value bound UNESCAPED (captured query log: PARAMS [\"AUTUMN_\",\"active\",1,0]) — not LOWER()=LOWER(). With two active rows AUTUMN1 and AUTUMNX stored, getActiveByCode('AUTUMN_') returned AUTUMN1, i.e. the `_` acted as a single-char wildcard and matched a code NOT equal to the input. The input path is reachable with `_`: the zod validators (entities.ts:233 tenantCreate ref, and affiliateCodeSchema at :241 used by :275/:367) use /^[a-zA-Z0-9_-]{...}$/, which explicitly permits `_` (only `%` is excluded); crud.ts:1210 passes ref straight into getActiveByCode and assigns the matched id to referredByAffiliateId, after which accrueForPaidInvoice (affiliates.ts:146-172) pays that affiliate on every invoice.payment_succeeded. getActiveByCode also has no orderBy, so with multiple ILIKE matches findFirst returns a nondeterministic row. Auto-generated codes use CODE_ALPHABET without `_`, but the flaw is on the lookup-input side and is independent of stored-code content. This is distinct from the previously-reported case-folding/unique-index issue. All four load-bearing claims (ILIKE compilation, unescaped param, cross-code wildcard match, reachable `_` input) verified in real code and a live DB run."
        ]
      },
      {
        "severity": "medium",
        "category": "test-coverage",
        "file": "packages/db/__tests__/affiliates.spec.ts",
        "line": 129,
        "title": "Recurring in-window accrual (months 2..N) is never tested — every accrual test uses the FIRST payment only",
        "description": "The whole point of the commissionMonths window anchored on the tenant's first payment (affiliates.ts L154-159) is that the 2nd..Nth invoice inside the window ALSO accrues a fresh commission — that is the recurring partner payout. But no test exercises a SECOND, DISTINCT, in-window invoice for the same (affiliate,tenant). Grepping every accrueForPaidInvoice/accrueCommission/invoiceEvent call: each is either the first commission for the pair (affiliates.spec L120,136,156,164; billing.spec L353), a DUPLICATE of that same invoice id (affiliates.spec L125; billing.spec L360 uses the same 'in_whook_1'), or an OUT-OF-window payment (affiliates.spec L140). No test asserts that a distinct in-window invoice produces a second Commission row and a summed payout.",
        "failureScenario": "A regression that makes the anchor/window math reject any invoice after the first (e.g. flipping the boundary compare, or treating the existence of a prior commission as 'already accrued for this tenant') would silently zero out every month-2..12 commission — the core recurring payout — while the entire affiliate + billing suite stays green.",
        "suggestedFix": "Add a case: active affiliate, first invoice accrues, then a SECOND distinct invoiceId with a paidAt still inside commissionMonths accrues a second Commission; assert listCommissions length 2 and the summed amountCents. Mirror it once at the webhook level in billing.spec with a second distinct invoice id.",
        "verifierNotes": [
          "CONFIRMED: CONFIRMED as a real test-coverage gap. The recurring accrual feature genuinely exists: accrueForPaidInvoice (packages/db/src/repos/affiliates.ts L146-173) anchors the commissionMonths window on the FIRST commission's createdAt (L157-159), and the commissions table's only unique constraint is on sourceInvoiceId (migration 20260803120000_affiliates L45; schema.prisma L185 `sourceInvoiceId String @unique`) — NOT on (affiliateId, tenantId). Therefore a distinct in-window invoice for the same pair is by design supposed to create a second Commission row; this is the core recurring partner payout for months 2..N.\n\nI enumerated every accrual call in the suite. In affiliates.spec.ts: L76 accrueCommission in_123 (first), L80 duplicate in_123, L120 accrueForPaidInvoice in_win_1 (first), L125 duplicate in_win_1, L136 in_exp_1 (first), L140 in_exp_2 (explicitly OUT of window → expects null), L156 in_frac_1 (first), L164/166 unreferred/unknown, L173 suspended. In billing.spec.ts: L353 in_whook_1 (first), L360 duplicate in_whook_1, L367 unreferred. None create a SECOND, DISTINCT, in-window invoice for an already-accrued (affiliate,tenant) pair and assert a second Commission row / summed payout.\n\nI tried to refute by finding a test that would catch the regression. The closest, the out-of-window test (L136-140), ALSO expects null for its second invoice, so a regression that made every post-first invoice return null (a window-boundary flip toward exclusion, or treating the existence of a prior commission as 'already accrued for this tenant') would keep that test — and every first-payment and duplicate test — green while silently zeroing all month-2..N commissions. No existing test resists that regression. The claim's file/line references, the OUT-OF-window vs first/duplicate classification, and the failure scenario are all accurate against the real code.",
          "CONFIRMED: Confirmed real test-coverage gap. I enumerated every accrueForPaidInvoice/accrueCommission caller in apps/ and packages/. Each is either (a) the FIRST commission for the (affiliate,tenant) pair (affiliates.spec L76,120,136,156; billing.spec L353; partner.spec L113), (b) a byte-identical DUPLICATE of that same invoice id (affiliates.spec L80 in_123, L125 in_win_1; billing.spec L360 in_whook_1), or (c) an OUT-OF-window / unreferred / suspended / unknown case expecting null (affiliates.spec L140 in_exp_2 at 7mo, L164/166/173). No test creates a SECOND, DISTINCT, IN-window invoice for the same pair and asserts a second Commission row plus a summed payout.\n\nThe production code (affiliates.ts L146-173) actually supports recurring accrual correctly: it anchors the window on the earliest existing commission (L157-158, anchor = first?.createdAt ?? invoice.paidAt) and creates a fresh row per distinct sourceInvoiceId while invoice.paidAt <= addMonthsUtc(anchor, commissionMonths). So months 2..N do accrue in reality — but that behavior, the entire economic point of commissionMonths, is unproven.\n\nThe claimed regression is genuinely uncaught: if a change made the function treat 'a prior commission exists for this tenant' as 'already accrued → return null', the whole suite still passes — L120/125 (single+dup, length 1), L129 (first accrues, second is out-of-window and expects null regardless), and billing (first+dup) all stay green. I could not find any assertion that would fail under that regression, nor any existing test exercising a distinct in-window recurring invoice. I actively tried to refute by grepping the full test tree for a second in-window invoice; none exists. Medium severity is appropriate: money-critical recurring payout path with no test guard."
        ]
      },
      {
        "severity": "low",
        "category": "test-coverage",
        "file": "packages/db/__tests__/affiliates.spec.ts",
        "line": 150,
        "title": "The 'floors a fractional commission rate' test is vacuous — its input yields an exact integer",
        "description": "The test claims to prove floor() rounds a fractional commission in the platform's favour, but the arithmetic has no fractional part: floor(10000 * 33.33 / 100) = floor(3333.0) = 3333. With amountPaidCents=10000, any 2-decimal commissionPct times 100 is always an exact integer, so floor, round, ceil and trunc are indistinguishable here. The rounding DIRECTION (the money-safety property the test name asserts) is not actually exercised.",
        "failureScenario": "A regression changing Math.floor to Math.round or Math.ceil in accrueForPaidInvoice (affiliates.ts L160) — which would over-pay affiliates by a cent on genuinely fractional products — passes this test unchanged, because 3333.0 has nothing to round.",
        "suggestedFix": "Use an amountPaidCents not divisible by 100 so the product is fractional, e.g. amountPaidCents=10_050, commissionPct=33.33 → 3349.665, and assert exactly 3349 (proving floor, not round/ceil which would give 3350).",
        "verifierNotes": [
          "CONFIRMED: CONFIRMED. I ran the exact expression from affiliates.ts:160 with the test's inputs (amountPaidCents=10000, commissionPct=33.33): 10000*33.33/100 evaluates to exactly 3333.0 (toPrecision(20) = 3333.0000000000000000, zero floating-point residual), so Math.floor, Math.round, Math.ceil and Math.trunc all return 3333. The test at affiliates.spec.ts:150 titled 'floors a fractional commission rate in the platform's favour' therefore does not exercise the rounding DIRECTION at all — a regression changing Math.floor to Math.round/ceil (which would over-pay affiliates by a cent on genuinely fractional products) would leave expect(c?.amountCents).toBe(3_333) green. The claim's structural argument also holds: with amountPaidCents=10000, the result is 100*pct, and any 2-decimal pct*100 is an exact integer, so a fractional cent is arithmetically impossible with this amount. I grepped every amountCents assertion in affiliates.spec.ts and the api specs; all commission tests use amountPaidCents=10_000, so no test anywhere feeds an amount yielding a fractional cent (e.g. amountPaidCents=999 would give 332.9667 where floor=332 vs round=333). The finding is an accurate, real test-coverage gap. Severity=low and category=test-coverage are appropriate — the production code is correct (uses Math.floor), only the test's asserted guarantee is unproven.",
          "CONFIRMED: The claim is accurate and verified against real code.\n\nProduction code (packages/db/src/repos/affiliates.ts:160):\n`const amountCents = Math.floor((invoice.amountPaidCents * Number(affiliate.commissionPct)) / 100)`\n\nTest (packages/db/__tests__/affiliates.spec.ts:150-158) named \"floors a fractional commission rate in the platform's favour\" feeds commissionPct=33.33 and amountPaidCents=10_000, asserting amountCents === 3_333.\n\nI evaluated the exact JS expression the code runs: `10000 * 33.33 / 100`. In IEEE-754 the intermediate `10000 * 33.33` collapses to exactly 333300, and `/100` to exactly 3333.0. Empirically Math.floor, Math.round, Math.ceil, and Math.trunc all return 3333. The value has no fractional part, so the rounding DIRECTION — the money-safety property the test title claims to prove — is not exercised at all. A regression swapping Math.floor for Math.round or Math.ceil in accrueForPaidInvoice passes this test unchanged.\n\nThis is systemic to the input choice: with amountPaidCents=10_000, any 2-decimal percentage times 100 is an exact integer of cents. I checked every other accrual assertion in the affiliates and billing spec files — all use amountPaidCents=10_000 (yielding exact 2_000 at 20%) or pass amountCents explicitly (accrueCommission with 300). No test anywhere exercises a genuinely fractional cents result (e.g. amountPaidCents=999 at 33.33% → 332.9667, where floor=332 but round/ceil=333 would diverge). So nothing in the suite catches a floor→round/ceil change that would over-pay affiliates by a cent.\n\nSeverity low / test-coverage is correct: the production floor() logic is right; the defect is purely that the test asserting it is vacuous."
        ]
      },
      {
        "severity": "low",
        "category": "test-coverage",
        "file": "apps/api/__tests__/billing.spec.ts",
        "line": 85,
        "title": "paidInvoiceFrom null/zero branches are untested at the webhook boundary",
        "description": "The webhook extractor billing.ts::paidInvoiceFrom (L197-204) returns null — skipping accrual and acking 200 — when amount_paid <= 0, when customer is missing/empty, or when the invoice id is missing/empty, and it defaults currency to 'eur' when absent. The test helper invoiceEvent (billing.spec L85-88) always emits a positive amount plus a complete customer/id/currency, so none of those null-return branches nor the currency default are ever exercised through the webhook. The repo-level $0 gap was noted before; this is the separate HTTP/extractor function.",
        "failureScenario": "A regression to the guard (e.g. changing amount_paid<=0 to <0, or dropping the customer!=='' check) would let a $0 or malformed invoice.payment_succeeded reach accrual / mis-resolve a tenant, and no billing test would catch it — the suite only ever feeds well-formed positive invoices.",
        "suggestedFix": "Add webhook cases: invoiceEvent with amount_paid=0 and with a negative amount → 200 and zero commissions; an event whose data.object omits customer or id → 200, no accrual; and an event omitting currency → asserts the stored commission currency is 'eur'.",
        "verifierNotes": [
          "CONFIRMED: Confirmed by reading the actual code. billing.ts::paidInvoiceFrom (L197-204) has three null-return branches (empty/missing customer, empty/missing invoice id, amount_paid<=0) and a currency default ('eur'). The function is private (grep shows it is only referenced in billing.ts, never exported), so it is reachable only through POST /v1/webhooks/stripe. The single webhook-invoice test helper invoiceEvent (billing.spec.ts:85-88) always emits a complete customer, complete invoice id, an explicit currency 'eur', and positive amount_paid; its only two callers (L353/360, L367) pass 5_000. Therefore none of the null-return branches nor the currency default are ever exercised at the HTTP/extractor boundary. The db-level affiliates.spec tests hit accrueForPaidInvoice directly but always with positive amounts and complete fields, and never touch paidInvoiceFrom (the claim explicitly scopes to the extractor, not the repo). The failure scenario is valid: a regression loosening the guard would let a $0/malformed invoice reach accrual with no failing test. This is an accurate low-severity test-coverage gap.",
          "CONFIRMED: Verified against the real code. billing.ts::paidInvoiceFrom (L197-204) has null-return guards for missing/empty customer (L198), missing/empty invoice id (L199), and amount_paid<=0 (L200,L202), plus a currency default to 'eur' (L201). The webhook test helper invoiceEvent (billing.spec L85-88) hardcodes a complete object and every call site (L353, L360, L367) passes amount_paid=5_000 with a valid customer, valid id, and explicit currency 'eur'. A grep of the whole apps/api test tree shows these are the ONLY invoice-webhook invocations and paidInvoiceFrom has no direct unit test, so none of the four null/default branches is ever exercised through the HTTP boundary. The claim correctly separates this from the prior repo-level $0 gap (accrueForPaidInvoice tested in packages/db). The branches are reachable in production (Stripe emits amount_paid:0 for trial/100%-coupon invoices), so a regression loosening the guard would silently escape the suite. Genuine, accurately scoped test-coverage gap; severity low is appropriate since it is missing coverage over otherwise-correct guard code."
        ]
      }
    ]
  },
  "workflowProgress": [
    {
      "type": "workflow_phase",
      "index": 1,
      "title": "Map"
    },
    {
      "type": "workflow_phase",
      "index": 2,
      "title": "Round 1"
    },
    {
      "type": "workflow_phase",
      "index": 3,
      "title": "Round 2"
    },
    {
      "type": "workflow_phase",
      "index": 4,
      "title": "Round 3"
    },
    {
      "type": "workflow_phase",
      "index": 5,
      "title": "Synthesize"
    },
    {
      "type": "workflow_agent",
      "index": 1,
      "label": "map-surface",
      "phaseIndex": 1,
      "phaseTitle": "Map",
      "agentId": "a959cf3e101d87af7",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1785780203647,
      "queuedAt": 1785780203646,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "grep -rn \"audit\\|Actor\\|actor\" /Users/ernis/Sites/gps_track…",
      "promptPreview": "You are mapping the affiliate platform of a GPS SaaS before a deep audit. \nAFFILIATE PLATFORM SURFACE (Orbetra, a multi-tenant white-label GPS SaaS). Review THESE files/areas:\n- packages/db/prisma/schema.prisma — models Affiliate, Commission, AffiliatePasswordToken; Tenant.referredByAffiliateId\n- packages/db/prisma/migrations/20260803120000_affiliates/migration.sql\n- packages/db/prisma/migrations/…",
      "lastProgressAt": 1785780388875,
      "tokens": 90622,
      "toolCalls": 21,
      "durationMs": 185228,
      "resultPreview": "I have everything needed. Here is the structural map.\n\n---\n\n# Affiliate Platform — Structural Map (for finder agents)\n\nGround truth: PRs #135–#140 on `main`. All line refs verified against files as read.\n\n## 1. Money flow: referral → attribution → accrual → payout\n\n**Stage 1 — Referral code exists.** `platform_admin` creates an `Affiliate` (crud.ts POST `/v1/affiliates`, L1257) with a `code` (admi…"
    },
    {
      "type": "workflow_agent",
      "index": 2,
      "label": "find:money-correctness:r1",
      "phaseIndex": 2,
      "phaseTitle": "Round 1",
      "agentId": "a4a9cdafa3229aeb3",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1785780388937,
      "queuedAt": 1785780388881,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "promptPreview": "You are a hostile, skeptical senior code auditor. \nAFFILIATE PLATFORM SURFACE (Orbetra, a multi-tenant white-label GPS SaaS). Review THESE files/areas:\n- packages/db/prisma/schema.prisma — models Affiliate, Commission, AffiliatePasswordToken; Tenant.referredByAffiliateId\n- packages/db/prisma/migrations/20260803120000_affiliates/migration.sql\n- packages/db/prisma/migrations/20260803160000_affiliate…",
      "lastProgressAt": 1785780536615,
      "tokens": 49000,
      "toolCalls": 8,
      "durationMs": 147678,
      "resultPreview": "{\"findings\":[{\"severity\":\"high\",\"category\":\"money-correctness\",\"file\":\"apps/api/src/routes/billing.ts\",\"line\":239,\"title\":\"No refund/chargeback clawback — commission paid on money the platform gave back\",\"description\":\"The Stripe webhook accrues a commission on invoice.payment_succeeded (billing.ts:239 -> accrueForPaidInvoice, affiliates.ts:146) but subscribes to NO reversal event. A grep across a…"
    },
    {
      "type": "workflow_agent",
      "index": 3,
      "label": "find:auth-security:r1",
      "phaseIndex": 2,
      "phaseTitle": "Round 1",
      "agentId": "a747fcd956c76ec2e",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1785780388937,
      "queuedAt": 1785780388881,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "promptPreview": "You are a hostile, skeptical senior code auditor. \nAFFILIATE PLATFORM SURFACE (Orbetra, a multi-tenant white-label GPS SaaS). Review THESE files/areas:\n- packages/db/prisma/schema.prisma — models Affiliate, Commission, AffiliatePasswordToken; Tenant.referredByAffiliateId\n- packages/db/prisma/migrations/20260803120000_affiliates/migration.sql\n- packages/db/prisma/migrations/20260803160000_affiliate…",
      "lastProgressAt": 1785780564268,
      "tokens": 65756,
      "toolCalls": 19,
      "durationMs": 175331,
      "resultPreview": "{\"findings\":[{\"severity\":\"medium\",\"category\":\"auth-correctness\",\"file\":\"packages/db/src/repos/affiliates.ts\",\"line\":121,\"title\":\"Affiliate email stored verbatim but partner login lowercases + looks up case-sensitively → mixed-case affiliates can never log in\",\"description\":\"The partner login path normalizes the submitted email to lowercase (apps/api/src/routes/partner.ts:76, `parsed.data.email.tri…"
    },
    {
      "type": "workflow_agent",
      "index": 4,
      "label": "find:isolation-leak:r1",
      "phaseIndex": 2,
      "phaseTitle": "Round 1",
      "agentId": "aeb17fdc3d664334c",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1785780388937,
      "queuedAt": 1785780388882,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "promptPreview": "You are a hostile, skeptical senior code auditor. \nAFFILIATE PLATFORM SURFACE (Orbetra, a multi-tenant white-label GPS SaaS). Review THESE files/areas:\n- packages/db/prisma/schema.prisma — models Affiliate, Commission, AffiliatePasswordToken; Tenant.referredByAffiliateId\n- packages/db/prisma/migrations/20260803120000_affiliates/migration.sql\n- packages/db/prisma/migrations/20260803160000_affiliate…",
      "lastProgressAt": 1785780478658,
      "tokens": 45996,
      "toolCalls": 13,
      "durationMs": 89721,
      "resultPreview": "{\"findings\":[{\"severity\":\"medium\",\"category\":\"sensitive-data-exposure\",\"file\":\"apps/api/src/routes/crud.ts\",\"line\":1251,\"title\":\"Platform affiliate GET routes leak partner argon2id passwordHash over the API\",\"description\":\"The `Affiliate` Prisma model carries `passwordHash String?` (packages/db/prisma/schema.prisma:138), the argon2id hash for partner self-service login. The platform routes return …"
    },
    {
      "type": "workflow_agent",
      "index": 5,
      "label": "find:concurrency-idempotency:r1",
      "phaseIndex": 2,
      "phaseTitle": "Round 1",
      "agentId": "a66cd9d06bb4fa188",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1785780388937,
      "queuedAt": 1785780388882,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "promptPreview": "You are a hostile, skeptical senior code auditor. \nAFFILIATE PLATFORM SURFACE (Orbetra, a multi-tenant white-label GPS SaaS). Review THESE files/areas:\n- packages/db/prisma/schema.prisma — models Affiliate, Commission, AffiliatePasswordToken; Tenant.referredByAffiliateId\n- packages/db/prisma/migrations/20260803120000_affiliates/migration.sql\n- packages/db/prisma/migrations/20260803160000_affiliate…",
      "lastProgressAt": 1785780626684,
      "tokens": 63233,
      "toolCalls": 10,
      "durationMs": 237747,
      "resultPreview": "{\"findings\":[{\"severity\":\"medium\",\"category\":\"ordering-idempotency\",\"file\":\"packages/db/src/repos/affiliates.ts\",\"line\":158,\"title\":\"Commission window anchored on DB insert clock (createdAt), not payment time — out-of-order / delayed delivery drifts the anchor later and over-pays\",\"description\":\"accrueForPaidInvoice computes the 12-month window from `first?.createdAt ?? invoice.paidAt` (L157-159):…"
    },
    {
      "type": "workflow_agent",
      "index": 6,
      "label": "find:datamodel-migration:r1",
      "phaseIndex": 2,
      "phaseTitle": "Round 1",
      "agentId": "ae2d696d2b7185917",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1785780388937,
      "queuedAt": 1785780388882,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "promptPreview": "You are a hostile, skeptical senior code auditor. \nAFFILIATE PLATFORM SURFACE (Orbetra, a multi-tenant white-label GPS SaaS). Review THESE files/areas:\n- packages/db/prisma/schema.prisma — models Affiliate, Commission, AffiliatePasswordToken; Tenant.referredByAffiliateId\n- packages/db/prisma/migrations/20260803120000_affiliates/migration.sql\n- packages/db/prisma/migrations/20260803160000_affiliate…",
      "lastProgressAt": 1785780491231,
      "tokens": 41602,
      "toolCalls": 10,
      "durationMs": 102294,
      "resultPreview": "{\"findings\":[{\"severity\":\"high\",\"category\":\"data-model\",\"file\":\"packages/db/prisma/migrations/20260803120000_affiliates/migration.sql\",\"line\":42,\"title\":\"Case-insensitive code lookup over a case-SENSITIVE unique index → duplicate effective codes + nondeterministic (mis-routed) attribution\",\"description\":\"`affiliates_code_key` is a plain case-sensitive btree unique index (migration L42; schema.pris…"
    },
    {
      "type": "workflow_agent",
      "index": 7,
      "label": "find:coverage-edgecases:r1",
      "phaseIndex": 2,
      "phaseTitle": "Round 1",
      "agentId": "a15c3812712e3b9a4",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1785780388937,
      "queuedAt": 1785780388882,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "promptPreview": "You are a hostile, skeptical senior code auditor. \nAFFILIATE PLATFORM SURFACE (Orbetra, a multi-tenant white-label GPS SaaS). Review THESE files/areas:\n- packages/db/prisma/schema.prisma — models Affiliate, Commission, AffiliatePasswordToken; Tenant.referredByAffiliateId\n- packages/db/prisma/migrations/20260803120000_affiliates/migration.sql\n- packages/db/prisma/migrations/20260803160000_affiliate…",
      "lastProgressAt": 1785780524868,
      "tokens": 68203,
      "toolCalls": 11,
      "durationMs": 135930,
      "resultPreview": "{\"findings\":[{\"severity\":\"medium\",\"category\":\"test-coverage\",\"file\":\"apps/api/__tests__/billing.spec.ts\",\"line\":346,\"title\":\"The genuine-DB-fault → HTTP 500 → Stripe-retry accrual path has zero test coverage\",\"description\":\"The whole design rationale of the F4 webhook accrual is the distinction between a benign null (dedupe / nothing-owed → 200 ack) and a genuine fault (throw → return 500 so Strip…"
    },
    {
      "type": "workflow_agent",
      "index": 8,
      "label": "verify:apps/api/src/routes/billing.ts::no refun:1",
      "phaseIndex": 2,
      "phaseTitle": "Round 1",
      "agentId": "acb1ba31e0cea7c91",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1785780626711,
      "queuedAt": 1785780626693,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "PLAUSIBLE",
      "promptPreview": "You are an adversarial verifier trying HARD to REFUTE a claimed defect. \nAFFILIATE PLATFORM SURFACE (Orbetra, a multi-tenant white-label GPS SaaS). Review THESE files/areas:\n- packages/db/prisma/schema.prisma — models Affiliate, Commission, AffiliatePasswordToken; Tenant.referredByAffiliateId\n- packages/db/prisma/migrations/20260803120000_affiliates/migration.sql\n- packages/db/prisma/migrations/20…",
      "lastProgressAt": 1785780731218,
      "tokens": 29974,
      "toolCalls": 7,
      "durationMs": 104507,
      "resultPreview": "{\"verdict\":\"PLAUSIBLE\",\"reasoning\":\"All factual premises verified in real code. billing.ts (mountStripeWebhook) handles only SUBSCRIPTION_EVENTS and invoice.payment_succeeded; a grep for charge.refunded|charge.dispute|invoice.voided|credit_note|refund|clawback across apps/api/src and packages/db/src returns zero hits. accrueForPaidInvoice (affiliates.ts:146) creates a Commission defaulting to stat…"
    },
    {
      "type": "workflow_agent",
      "index": 9,
      "label": "verify:apps/api/src/routes/billing.ts::no refun:2",
      "phaseIndex": 2,
      "phaseTitle": "Round 1",
      "agentId": "a22fccf2847ee64ff",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1785780626711,
      "queuedAt": 1785780626693,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "PLAUSIBLE",
      "promptPreview": "You are an adversarial verifier trying HARD to REFUTE a claimed defect. \nAFFILIATE PLATFORM SURFACE (Orbetra, a multi-tenant white-label GPS SaaS). Review THESE files/areas:\n- packages/db/prisma/schema.prisma — models Affiliate, Commission, AffiliatePasswordToken; Tenant.referredByAffiliateId\n- packages/db/prisma/migrations/20260803120000_affiliates/migration.sql\n- packages/db/prisma/migrations/20…",
      "lastProgressAt": 1785780773984,
      "tokens": 34732,
      "toolCalls": 15,
      "durationMs": 147273,
      "resultPreview": "{\"verdict\":\"PLAUSIBLE\",\"reasoning\":\"The factual claim is verified against the code: no Stripe reversal event is consumed anywhere. Grep across apps/api/src and packages/db/src returns zero hits for charge.refunded, charge.dispute, invoice.voided, credit_note, or refund. The webhook (billing.ts:214-262) handles only SUBSCRIPTION_EVENTS and invoice.payment_succeeded->accrueForPaidInvoice. Commission…"
    },
    {
      "type": "workflow_agent",
      "index": 10,
      "label": "verify:packages/db/src/repos/affiliates.ts::com:1",
      "phaseIndex": 2,
      "phaseTitle": "Round 1",
      "agentId": "a6fb69b7de3d82598",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1785780626717,
      "queuedAt": 1785780626717,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Confirmed against real code in packages/db/src/repos/affili…",
      "promptPreview": "You are an adversarial verifier trying HARD to REFUTE a claimed defect. \nAFFILIATE PLATFORM SURFACE (Orbetra, a multi-tenant white-label GPS SaaS). Review THESE files/areas:\n- packages/db/prisma/schema.prisma — models Affiliate, Commission, AffiliatePasswordToken; Tenant.referredByAffiliateId\n- packages/db/prisma/migrations/20260803120000_affiliates/migration.sql\n- packages/db/prisma/migrations/20…",
      "lastProgressAt": 1785780709008,
      "tokens": 28472,
      "toolCalls": 4,
      "durationMs": 82291,
      "resultPreview": "{\"reasoning\":\"Confirmed against real code in packages/db/src/repos/affiliates.ts. The commission window anchor at L157-159 is `first?.createdAt ?? invoice.paidAt`, where `first` is the earliest EXISTING Commission row for (affiliate, tenant). The suspended-affiliate bail at L153 returns null BEFORE writing a row, so payments received while the affiliate is suspended leave no row. If an affiliate i…"
    },
    {
      "type": "workflow_agent",
      "index": 11,
      "label": "verify:packages/db/src/repos/affiliates.ts::com:2",
      "phaseIndex": 2,
      "phaseTitle": "Round 1",
      "agentId": "afefd8aedd830331d",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1785780626717,
      "queuedAt": 1785780626717,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "REJECTED",
      "promptPreview": "You are an adversarial verifier trying HARD to REFUTE a claimed defect. \nAFFILIATE PLATFORM SURFACE (Orbetra, a multi-tenant white-label GPS SaaS). Review THESE files/areas:\n- packages/db/prisma/schema.prisma — models Affiliate, Commission, AffiliatePasswordToken; Tenant.referredByAffiliateId\n- packages/db/prisma/migrations/20260803120000_affiliates/migration.sql\n- packages/db/prisma/migrations/20…",
      "lastProgressAt": 1785780760328,
      "tokens": 31977,
      "toolCalls": 4,
      "durationMs": 133611,
      "resultPreview": "{\"verdict\":\"REJECTED\",\"reasoning\":\"The anchor-drift behavior is real (window anchors on the earliest EXISTING commission row's createdAt, and suspend/pending across the tenant's first paid invoices writes no rows, moving the anchor later on reactivation — affiliates.ts:157-159). However, the claimed money-correctness harm (\\\"overpay past commissionMonths / 3-4 extra commissions the platform never …"
    },
    {
      "type": "workflow_agent",
      "index": 12,
      "label": "verify:packages/db/src/repos/affiliates.ts::win:1",
      "phaseIndex": 2,
      "phaseTitle": "Round 1",
      "agentId": "a10f0a497b6d85223",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1785780626719,
      "queuedAt": 1785780626719,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "REJECTED",
      "promptPreview": "You are an adversarial verifier trying HARD to REFUTE a claimed defect. \nAFFILIATE PLATFORM SURFACE (Orbetra, a multi-tenant white-label GPS SaaS). Review THESE files/areas:\n- packages/db/prisma/schema.prisma — models Affiliate, Commission, AffiliatePasswordToken; Tenant.referredByAffiliateId\n- packages/db/prisma/migrations/20260803120000_affiliates/migration.sql\n- packages/db/prisma/migrations/20…",
      "lastProgressAt": 1785780767965,
      "tokens": 30286,
      "toolCalls": 7,
      "durationMs": 141246,
      "resultPreview": "{\"verdict\":\"REJECTED\",\"reasoning\":\"Grounded in the actual code (packages/db/src/repos/affiliates.ts:146-159, schema.prisma:174-192 Commission.createdAt @default(now()), and the window test affiliates.spec.ts:129-141), the claim's mechanical premise is true — the anchor is `first.createdAt` (server insert clock) while the comparand is `invoice.paidAt` (Stripe clock) — but the \\\"defect\\\" is not real…"
    },
    {
      "type": "workflow_agent",
      "index": 13,
      "label": "verify:packages/db/src/repos/affiliates.ts::win:2",
      "phaseIndex": 2,
      "phaseTitle": "Round 1",
      "agentId": "ab85ac51fb95102bc",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1785780626719,
      "queuedAt": 1785780626719,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "PLAUSIBLE",
      "promptPreview": "You are an adversarial verifier trying HARD to REFUTE a claimed defect. \nAFFILIATE PLATFORM SURFACE (Orbetra, a multi-tenant white-label GPS SaaS). Review THESE files/areas:\n- packages/db/prisma/schema.prisma — models Affiliate, Commission, AffiliatePasswordToken; Tenant.referredByAffiliateId\n- packages/db/prisma/migrations/20260803120000_affiliates/migration.sql\n- packages/db/prisma/migrations/20…",
      "lastProgressAt": 1785780765332,
      "tokens": 27829,
      "toolCalls": 4,
      "durationMs": 138613,
      "resultPreview": "{\"verdict\":\"PLAUSIBLE\",\"reasoning\":\"The factual mechanism is correctly read and real. At affiliates.ts:158 the subsequent-payment window anchor is `first.createdAt` (Commission.createdAt = schema `@default(now())`, i.e. DB server wall-clock at webhook-insert time), while the boundary at L159 tests `invoice.paidAt` (Stripe event time) against `addMonthsUtc(anchor, commissionMonths)`. These are two …"
    },
    {
      "type": "workflow_agent",
      "index": 14,
      "label": "verify:packages/db/src/repos/affiliates.ts::aff:1",
      "phaseIndex": 2,
      "phaseTitle": "Round 1",
      "agentId": "a9f85bd23b1917246",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1785780626721,
      "queuedAt": 1785780626721,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "CONFIRMED. Every link in the claimed chain is real code: 1.…",
      "promptPreview": "You are an adversarial verifier trying HARD to REFUTE a claimed defect. \nAFFILIATE PLATFORM SURFACE (Orbetra, a multi-tenant white-label GPS SaaS). Review THESE files/areas:\n- packages/db/prisma/schema.prisma — models Affiliate, Commission, AffiliatePasswordToken; Tenant.referredByAffiliateId\n- packages/db/prisma/migrations/20260803120000_affiliates/migration.sql\n- packages/db/prisma/migrations/20…",
      "lastProgressAt": 1785780675845,
      "tokens": 30412,
      "toolCalls": 10,
      "durationMs": 49124,
      "resultPreview": "{\"reasoning\":\"CONFIRMED. Every link in the claimed chain is real code:\\n\\n1. Store verbatim — affiliates.ts:121 `create` sets `email: data.email` with no normalization. The crud.ts POST /v1/affiliates handler passes `...data` straight through (spreads the parsed body), doing no lowercasing. And `affiliateCreateSchema` (entities.ts:250) is `email: z.string().email().max(320)` — no `.transform`/`.to…"
    },
    {
      "type": "workflow_agent",
      "index": 15,
      "label": "verify:packages/db/src/repos/affiliates.ts::aff:2",
      "phaseIndex": 2,
      "phaseTitle": "Round 1",
      "agentId": "aa5a9313e4b4a7a0c",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1785780626721,
      "queuedAt": 1785780626721,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "CONFIRMED",
      "promptPreview": "You are an adversarial verifier trying HARD to REFUTE a claimed defect. \nAFFILIATE PLATFORM SURFACE (Orbetra, a multi-tenant white-label GPS SaaS). Review THESE files/areas:\n- packages/db/prisma/schema.prisma — models Affiliate, Commission, AffiliatePasswordToken; Tenant.referredByAffiliateId\n- packages/db/prisma/migrations/20260803120000_affiliates/migration.sql\n- packages/db/prisma/migrations/20…",
      "lastProgressAt": 1785780681797,
      "tokens": 32749,
      "toolCalls": 9,
      "durationMs": 55076,
      "resultPreview": "{\"verdict\":\"CONFIRMED\",\"reasoning\":\"Every link in the claimed chain is verified in real code. Login normalizes to lowercase (apps/api/src/routes/partner.ts:76) then looks up via findByEmailForAuth, which is a case-SENSITIVE Prisma findUnique on the email column (packages/db/src/repos/affiliates.ts:182). The create path stores the email verbatim (affiliates.ts:121, `email: data.email`); the crud.ts…"
    },
    {
      "type": "workflow_agent",
      "index": 16,
      "label": "verify:apps/api/src/routes/crud.ts::platform af:1",
      "phaseIndex": 2,
      "phaseTitle": "Round 1",
      "agentId": "abaff995454fc285a",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1785780675846,
      "queuedAt": 1785780626723,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Verified against actual code. schema.prisma:138 defines Aff…",
      "promptPreview": "You are an adversarial verifier trying HARD to REFUTE a claimed defect. \nAFFILIATE PLATFORM SURFACE (Orbetra, a multi-tenant white-label GPS SaaS). Review THESE files/areas:\n- packages/db/prisma/schema.prisma — models Affiliate, Commission, AffiliatePasswordToken; Tenant.referredByAffiliateId\n- packages/db/prisma/migrations/20260803120000_affiliates/migration.sql\n- packages/db/prisma/migrations/20…",
      "lastProgressAt": 1785780739695,
      "tokens": 36103,
      "toolCalls": 10,
      "durationMs": 63849,
      "resultPreview": "{\"reasoning\":\"Verified against actual code. schema.prisma:138 defines Affiliate.passwordHash String? (argon2id partner login hash). affiliates.ts:112-113 list()/get() call prisma.affiliate.findMany/findUnique with NO select, returning the full row including passwordHash; create (118) and update (135) likewise. crud.ts:1251/1254-1255/1265/1283 serialize the raw row via json→toJson (crud.ts:173-181)…"
    },
    {
      "type": "workflow_agent",
      "index": 17,
      "label": "verify:apps/api/src/routes/crud.ts::platform af:2",
      "phaseIndex": 2,
      "phaseTitle": "Round 1",
      "agentId": "a42fe00ca07fcfa2e",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1785780681798,
      "queuedAt": 1785780626724,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "CONFIRMED",
      "promptPreview": "You are an adversarial verifier trying HARD to REFUTE a claimed defect. \nAFFILIATE PLATFORM SURFACE (Orbetra, a multi-tenant white-label GPS SaaS). Review THESE files/areas:\n- packages/db/prisma/schema.prisma — models Affiliate, Commission, AffiliatePasswordToken; Tenant.referredByAffiliateId\n- packages/db/prisma/migrations/20260803120000_affiliates/migration.sql\n- packages/db/prisma/migrations/20…",
      "lastProgressAt": 1785780726717,
      "tokens": 29040,
      "toolCalls": 9,
      "durationMs": 44919,
      "resultPreview": "{\"verdict\":\"CONFIRMED\",\"reasoning\":\"The defect is real and reachable.\\n\\nEvidence chain:\\n1. Schema: `Affiliate.passwordHash String?` is the argon2id hash for partner self-service login (packages/db/prisma/schema.prisma:138).\\n2. Repo: `list: () => prisma.affiliate.findMany({ orderBy: { createdAt: 'desc' } })` and `get: (id) => prisma.affiliate.findUnique({ where: { id } })` (affiliates.ts:112-113…"
    },
    {
      "type": "workflow_agent",
      "index": 18,
      "label": "verify:packages/db/src/repos/affiliates.ts::com:1",
      "phaseIndex": 2,
      "phaseTitle": "Round 1",
      "agentId": "a572eea5645b188e5",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1785780709009,
      "queuedAt": 1785780626724,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Every factual pillar of the claim is confirmed in real code…",
      "promptPreview": "You are an adversarial verifier trying HARD to REFUTE a claimed defect. \nAFFILIATE PLATFORM SURFACE (Orbetra, a multi-tenant white-label GPS SaaS). Review THESE files/areas:\n- packages/db/prisma/schema.prisma — models Affiliate, Commission, AffiliatePasswordToken; Tenant.referredByAffiliateId\n- packages/db/prisma/migrations/20260803120000_affiliates/migration.sql\n- packages/db/prisma/migrations/20…",
      "lastProgressAt": 1785780820902,
      "tokens": 28856,
      "toolCalls": 5,
      "durationMs": 111893,
      "resultPreview": "{\"reasoning\":\"Every factual pillar of the claim is confirmed in real code.\\n\\n1. Two clocks. In `accrueForPaidInvoice` (packages/db/src/repos/affiliates.ts ~L155-160): `const first = await prisma.commission.findFirst({ ... orderBy: { createdAt: 'asc' }, select: { createdAt: true } })`, then `const anchor = first?.createdAt ?? invoice.paidAt`, and the window check `if (invoice.paidAt > addMonthsUtc…"
    },
    {
      "type": "workflow_agent",
      "index": 19,
      "label": "verify:packages/db/src/repos/affiliates.ts::com:2",
      "phaseIndex": 2,
      "phaseTitle": "Round 1",
      "agentId": "a85def7e5255b00ba",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1785780726718,
      "queuedAt": 1785780626724,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "CONFIRMED",
      "promptPreview": "You are an adversarial verifier trying HARD to REFUTE a claimed defect. \nAFFILIATE PLATFORM SURFACE (Orbetra, a multi-tenant white-label GPS SaaS). Review THESE files/areas:\n- packages/db/prisma/schema.prisma — models Affiliate, Commission, AffiliatePasswordToken; Tenant.referredByAffiliateId\n- packages/db/prisma/migrations/20260803120000_affiliates/migration.sql\n- packages/db/prisma/migrations/20…",
      "lastProgressAt": 1785780863709,
      "tokens": 35130,
      "toolCalls": 8,
      "durationMs": 136991,
      "resultPreview": "{\"verdict\":\"CONFIRMED\",\"reasoning\":\"Verified against source, schema, migration, and test. affiliates.ts:157-159 anchors the commission window on the earliest existing commission's `createdAt`, compared against the incoming Stripe `invoice.paidAt` — two different clocks, exactly as claimed. schema.prisma (Commission) and 20260803120000_affiliates/migration.sql confirm `createdAt TIMESTAMPTZ DEFAULT…"
    },
    {
      "type": "workflow_agent",
      "index": 20,
      "label": "verify:packages/db/src/repos/affiliates.ts::\"no:1",
      "phaseIndex": 2,
      "phaseTitle": "Round 1",
      "agentId": "aafd0ce8bac56ab24",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1785780731219,
      "queuedAt": 1785780626724,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "CONFIRMED mechanism, grounded in code. In packages/db/src/r…",
      "promptPreview": "You are an adversarial verifier trying HARD to REFUTE a claimed defect. \nAFFILIATE PLATFORM SURFACE (Orbetra, a multi-tenant white-label GPS SaaS). Review THESE files/areas:\n- packages/db/prisma/schema.prisma — models Affiliate, Commission, AffiliatePasswordToken; Tenant.referredByAffiliateId\n- packages/db/prisma/migrations/20260803120000_affiliates/migration.sql\n- packages/db/prisma/migrations/20…",
      "lastProgressAt": 1785780868754,
      "tokens": 36846,
      "toolCalls": 6,
      "durationMs": 137535,
      "resultPreview": "{\"reasoning\":\"CONFIRMED mechanism, grounded in code. In packages/db/src/repos/affiliates.ts:146-173, accrueForPaidInvoice's three \\\"nothing owed\\\" exits (L151 not-referred, L153 status!=='active', L159 out-of-window, L161 amount<=0) return null and persist NO row; the unique-sourceInvoiceId idempotency marker is written only when a commission is actually created (L166-168). apps/api/src/routes/bil…"
    },
    {
      "type": "workflow_agent",
      "index": 21,
      "label": "verify:packages/db/src/repos/affiliates.ts::\"no:2",
      "phaseIndex": 2,
      "phaseTitle": "Round 1",
      "agentId": "a42df8b035ee57964",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1785780739696,
      "queuedAt": 1785780626724,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "PLAUSIBLE",
      "promptPreview": "You are an adversarial verifier trying HARD to REFUTE a claimed defect. \nAFFILIATE PLATFORM SURFACE (Orbetra, a multi-tenant white-label GPS SaaS). Review THESE files/areas:\n- packages/db/prisma/schema.prisma — models Affiliate, Commission, AffiliatePasswordToken; Tenant.referredByAffiliateId\n- packages/db/prisma/migrations/20260803120000_affiliates/migration.sql\n- packages/db/prisma/migrations/20…",
      "lastProgressAt": 1785780864935,
      "tokens": 39634,
      "toolCalls": 6,
      "durationMs": 125239,
      "resultPreview": "{\"verdict\":\"PLAUSIBLE\",\"reasoning\":\"MECHANISM IS REAL (verified in code). In accrueForPaidInvoice (packages/db/src/repos/affiliates.ts:146-173) every \\\"nothing owed\\\" exit — not referred (L151), affiliate not active (L153), out-of-window (L159), floored amount <=0 (L161) — returns null and persists NO row. The only durable marker is the Commission row itself, whose sourceInvoiceId unique index (L1…"
    },
    {
      "type": "workflow_agent",
      "index": 22,
      "label": "verify:packages/db/prisma/migrations/2026080312:1",
      "phaseIndex": 2,
      "phaseTitle": "Round 1",
      "agentId": "a036700b9393e5f36",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1785780760330,
      "queuedAt": 1785780626724,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "CONFIRMED",
      "promptPreview": "You are an adversarial verifier trying HARD to REFUTE a claimed defect. \nAFFILIATE PLATFORM SURFACE (Orbetra, a multi-tenant white-label GPS SaaS). Review THESE files/areas:\n- packages/db/prisma/schema.prisma — models Affiliate, Commission, AffiliatePasswordToken; Tenant.referredByAffiliateId\n- packages/db/prisma/migrations/20260803120000_affiliates/migration.sql\n- packages/db/prisma/migrations/20…",
      "lastProgressAt": 1785780827151,
      "tokens": 32854,
      "toolCalls": 12,
      "durationMs": 66821,
      "resultPreview": "{\"verdict\":\"CONFIRMED\",\"reasoning\":\"Real, reachable data-model defect. The DB unique index on affiliates.code is a plain case-SENSITIVE btree (migration 20260803120000_affiliates/migration.sql:42; schema.prisma:130 `code String @unique`, whose \\\"case-insensitive by convention\\\" comment is not DB-enforced — no citext, no lower(code) functional index). Nothing normalizes the code on write: crud.ts:1…"
    },
    {
      "type": "workflow_agent",
      "index": 23,
      "label": "verify:packages/db/prisma/migrations/2026080312:2",
      "phaseIndex": 2,
      "phaseTitle": "Round 1",
      "agentId": "a905077c721e0dc57",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1785780765333,
      "queuedAt": 1785780626724,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "CONFIRMED",
      "promptPreview": "You are an adversarial verifier trying HARD to REFUTE a claimed defect. \nAFFILIATE PLATFORM SURFACE (Orbetra, a multi-tenant white-label GPS SaaS). Review THESE files/areas:\n- packages/db/prisma/schema.prisma — models Affiliate, Commission, AffiliatePasswordToken; Tenant.referredByAffiliateId\n- packages/db/prisma/migrations/20260803120000_affiliates/migration.sql\n- packages/db/prisma/migrations/20…",
      "lastProgressAt": 1785780822630,
      "tokens": 31905,
      "toolCalls": 8,
      "durationMs": 57297,
      "resultPreview": "{\"verdict\":\"CONFIRMED\",\"reasoning\":\"Every load-bearing claim checks out against the actual code, and I could not refute the scenario.\\n\\n1. The unique index IS case-sensitive. migration.sql:42 is a plain btree `CREATE UNIQUE INDEX \\\"affiliates_code_key\\\" ON \\\"affiliates\\\"(\\\"code\\\")` — no `lower(code)` expression index, no citext type, no trigger. schema.prisma:130 is `code String @unique` with a c…"
    },
    {
      "type": "workflow_agent",
      "index": 24,
      "label": "verify:packages/db/prisma/migrations/2026080312:1",
      "phaseIndex": 2,
      "phaseTitle": "Round 1",
      "agentId": "a231db4ac2498a99e",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1785780767966,
      "queuedAt": 1785780626724,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "CONFIRMED for the tenant-delete path. Evidence: (1) migrati…",
      "promptPreview": "You are an adversarial verifier trying HARD to REFUTE a claimed defect. \nAFFILIATE PLATFORM SURFACE (Orbetra, a multi-tenant white-label GPS SaaS). Review THESE files/areas:\n- packages/db/prisma/schema.prisma — models Affiliate, Commission, AffiliatePasswordToken; Tenant.referredByAffiliateId\n- packages/db/prisma/migrations/20260803120000_affiliates/migration.sql\n- packages/db/prisma/migrations/20…",
      "lastProgressAt": 1785780827819,
      "tokens": 30591,
      "toolCalls": 8,
      "durationMs": 59853,
      "resultPreview": "{\"reasoning\":\"CONFIRMED for the tenant-delete path. Evidence: (1) migration 20260803120000_affiliates/migration.sql L54 and L57 declare both commissions FKs as ON DELETE CASCADE, matched by schema.prisma:177,180 (onDelete: Cascade on Commission.affiliate and Commission.tenant). (2) A hard tenant delete is reachable: crud.ts:1220-1224 exposes DELETE /v1/tenants/:id (platform_admin) → db.tenants.rem…"
    },
    {
      "type": "workflow_agent",
      "index": 25,
      "label": "verify:packages/db/prisma/migrations/2026080312:2",
      "phaseIndex": 2,
      "phaseTitle": "Round 1",
      "agentId": "aa7e01e58c4f31715",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1785780773985,
      "queuedAt": 1785780626724,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "CONFIRMED",
      "promptPreview": "You are an adversarial verifier trying HARD to REFUTE a claimed defect. \nAFFILIATE PLATFORM SURFACE (Orbetra, a multi-tenant white-label GPS SaaS). Review THESE files/areas:\n- packages/db/prisma/schema.prisma — models Affiliate, Commission, AffiliatePasswordToken; Tenant.referredByAffiliateId\n- packages/db/prisma/migrations/20260803120000_affiliates/migration.sql\n- packages/db/prisma/migrations/20…",
      "lastProgressAt": 1785780830385,
      "tokens": 28833,
      "toolCalls": 7,
      "durationMs": 56399,
      "resultPreview": "{\"verdict\":\"CONFIRMED\",\"reasoning\":\"All links in the chain are verified against real code. (1) The commissions FKs are ON DELETE CASCADE: migration 20260803120000_affiliates/migration.sql L54 (affiliateId) and L57 (tenantId). The tenants.referredByAffiliateId FK at L51 is deliberately ON DELETE SET NULL, confirming the noted asymmetry and that CASCADE on the ledger is an oversight. (2) A hard tena…"
    },
    {
      "type": "workflow_agent",
      "index": 26,
      "label": "verify:packages/db/prisma/migrations/2026080312:1",
      "phaseIndex": 2,
      "phaseTitle": "Round 1",
      "agentId": "a7ca26b7dd7c96c67",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1785780820903,
      "queuedAt": 1785780626724,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "CONFIRMED",
      "promptPreview": "You are an adversarial verifier trying HARD to REFUTE a claimed defect. \nAFFILIATE PLATFORM SURFACE (Orbetra, a multi-tenant white-label GPS SaaS). Review THESE files/areas:\n- packages/db/prisma/schema.prisma — models Affiliate, Commission, AffiliatePasswordToken; Tenant.referredByAffiliateId\n- packages/db/prisma/migrations/20260803120000_affiliates/migration.sql\n- packages/db/prisma/migrations/20…",
      "lastProgressAt": 1785780916474,
      "tokens": 41232,
      "toolCalls": 10,
      "durationMs": 95571,
      "resultPreview": "{\"verdict\":\"CONFIRMED\",\"reasoning\":\"Confirmed via direct code reading. (1) affiliates_email_key is a plain case-sensitive btree (migration.sql:39); no citext/lower() index anywhere, and the second migration does not add normalization. (2) The create path stores email verbatim: crud.ts:1264 spreads ...data and affiliates.ts create (116-126) writes email: data.email unchanged; zod schema entities.ts…"
    },
    {
      "type": "workflow_agent",
      "index": 27,
      "label": "verify:packages/db/prisma/migrations/2026080312:2",
      "phaseIndex": 2,
      "phaseTitle": "Round 1",
      "agentId": "a8675d03744f985ce",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1785780822631,
      "queuedAt": 1785780626724,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "CONFIRMED",
      "promptPreview": "You are an adversarial verifier trying HARD to REFUTE a claimed defect. \nAFFILIATE PLATFORM SURFACE (Orbetra, a multi-tenant white-label GPS SaaS). Review THESE files/areas:\n- packages/db/prisma/schema.prisma — models Affiliate, Commission, AffiliatePasswordToken; Tenant.referredByAffiliateId\n- packages/db/prisma/migrations/20260803120000_affiliates/migration.sql\n- packages/db/prisma/migrations/20…",
      "lastProgressAt": 1785780890986,
      "tokens": 33920,
      "toolCalls": 10,
      "durationMs": 68355,
      "resultPreview": "{\"verdict\":\"CONFIRMED\",\"reasoning\":\"The defect is real and demonstrable. (1) Duplicate rows: affiliates_email_key is a plain case-sensitive btree unique index (migration L39; schema.prisma:128 email String @unique, no citext/functional index). The create path stores email verbatim — crud.ts:1264 spreads ...data into affiliates.ts:119 create, and affiliateCreateSchema (entities.ts:248-254) validate…"
    },
    {
      "type": "workflow_agent",
      "index": 28,
      "label": "verify:apps/api/__tests__/billing.spec.ts::the :1",
      "phaseIndex": 2,
      "phaseTitle": "Round 1",
      "agentId": "aa59d87409ba1b698",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1785780827151,
      "queuedAt": 1785780626724,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Verified against the real code. The webhook's genuine-fault…",
      "promptPreview": "You are an adversarial verifier trying HARD to REFUTE a claimed defect. \nAFFILIATE PLATFORM SURFACE (Orbetra, a multi-tenant white-label GPS SaaS). Review THESE files/areas:\n- packages/db/prisma/schema.prisma — models Affiliate, Commission, AffiliatePasswordToken; Tenant.referredByAffiliateId\n- packages/db/prisma/migrations/20260803120000_affiliates/migration.sql\n- packages/db/prisma/migrations/20…",
      "lastProgressAt": 1785780877796,
      "tokens": 46766,
      "toolCalls": 6,
      "durationMs": 50645,
      "resultPreview": "{\"reasoning\":\"Verified against the real code. The webhook's genuine-fault path (billing.ts L250-256: catch → console.error → return 500) and the repo's rethrow semantics (affiliates.ts L169-172, with the L162-164 comment documenting the deliberate reversal away from swallowing) both exist and are money-critical. Grepping apps/api/__tests__ and packages/db/__tests__/affiliates.spec.ts for 500/accru…"
    },
    {
      "type": "workflow_agent",
      "index": 29,
      "label": "verify:apps/api/__tests__/billing.spec.ts::the :2",
      "phaseIndex": 2,
      "phaseTitle": "Round 1",
      "agentId": "ac9dce151608843c7",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1785780827820,
      "queuedAt": 1785780626724,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Verified against the actual code. billing.ts L246-256 imple…",
      "promptPreview": "You are an adversarial verifier trying HARD to REFUTE a claimed defect. \nAFFILIATE PLATFORM SURFACE (Orbetra, a multi-tenant white-label GPS SaaS). Review THESE files/areas:\n- packages/db/prisma/schema.prisma — models Affiliate, Commission, AffiliatePasswordToken; Tenant.referredByAffiliateId\n- packages/db/prisma/migrations/20260803120000_affiliates/migration.sql\n- packages/db/prisma/migrations/20…",
      "lastProgressAt": 1785780878592,
      "tokens": 44638,
      "toolCalls": 6,
      "durationMs": 50772,
      "resultPreview": "{\"reasoning\":\"Verified against the actual code. billing.ts L246-256 implements the claimed null→200 vs throw→500 split (catch → console.error → c.text('accrual failed', 500) for Stripe retry). affiliates.ts L162-172 deliberately rethrows non-dedupe errors, with a comment stating the repo 'no longer swallows non-dedupe errors' — a documented behavior reversal. The regression risk is concrete: accru…"
    },
    {
      "type": "workflow_agent",
      "index": 30,
      "label": "verify:packages/db/__tests__/affiliates.spec.ts:1",
      "phaseIndex": 2,
      "phaseTitle": "Round 1",
      "agentId": "afbbffa68bb473e23",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1785780830386,
      "queuedAt": 1785780626724,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "REJECTED. Premise is built on a misquote. The claim states …",
      "promptPreview": "You are an adversarial verifier trying HARD to REFUTE a claimed defect. \nAFFILIATE PLATFORM SURFACE (Orbetra, a multi-tenant white-label GPS SaaS). Review THESE files/areas:\n- packages/db/prisma/schema.prisma — models Affiliate, Commission, AffiliatePasswordToken; Tenant.referredByAffiliateId\n- packages/db/prisma/migrations/20260803120000_affiliates/migration.sql\n- packages/db/prisma/migrations/20…",
      "lastProgressAt": 1785780917851,
      "tokens": 29471,
      "toolCalls": 4,
      "durationMs": 87465,
      "resultPreview": "{\"reasoning\":\"REJECTED.\\n\\nPremise is built on a misquote. The claim states L154 \\\"comments the window is measured 'from the tenant's FIRST payment'.\\\" The actual L154 comment reads \\\"commissionMonths from the tenant's FIRST **accrued** payment,\\\" and L155-156 explicitly spell out the mechanism: \\\"The earliest existing commission is that anchor; if there is none yet, THIS invoice IS the first paym…"
    },
    {
      "type": "workflow_agent",
      "index": 31,
      "label": "verify:packages/db/__tests__/affiliates.spec.ts:2",
      "phaseIndex": 2,
      "phaseTitle": "Round 1",
      "agentId": "acddec0b4677c7e03",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1785780863711,
      "queuedAt": 1785780626724,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "CONFIRMED. The claim accurately describes real, reachable c…",
      "promptPreview": "You are an adversarial verifier trying HARD to REFUTE a claimed defect. \nAFFILIATE PLATFORM SURFACE (Orbetra, a multi-tenant white-label GPS SaaS). Review THESE files/areas:\n- packages/db/prisma/schema.prisma — models Affiliate, Commission, AffiliatePasswordToken; Tenant.referredByAffiliateId\n- packages/db/prisma/migrations/20260803120000_affiliates/migration.sql\n- packages/db/prisma/migrations/20…",
      "lastProgressAt": 1785780969269,
      "tokens": 35264,
      "toolCalls": 5,
      "durationMs": 105558,
      "resultPreview": "{\"reasoning\":\"CONFIRMED. The claim accurately describes real, reachable code behavior and a genuine test gap. affiliates.ts L157-158 anchors the commission window on the earliest existing COMMISSION row (`findFirst ... orderBy createdAt asc`), with the only fallback being the current invoice's paidAt — there is no true-first-payment timestamp (tenant select at L147-150 fetches only id + referredBy…"
    },
    {
      "type": "workflow_agent",
      "index": 32,
      "label": "verify:packages/db/__tests__/affiliates.spec.ts:1",
      "phaseIndex": 2,
      "phaseTitle": "Round 1",
      "agentId": "a08e1ac0f3dc674ef",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1785780864935,
      "queuedAt": 1785780626724,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "CONFIRMED. affiliates.spec.ts:160-174 is titled \"...or a $0…",
      "promptPreview": "You are an adversarial verifier trying HARD to REFUTE a claimed defect. \nAFFILIATE PLATFORM SURFACE (Orbetra, a multi-tenant white-label GPS SaaS). Review THESE files/areas:\n- packages/db/prisma/schema.prisma — models Affiliate, Commission, AffiliatePasswordToken; Tenant.referredByAffiliateId\n- packages/db/prisma/migrations/20260803120000_affiliates/migration.sql\n- packages/db/prisma/migrations/20…",
      "lastProgressAt": 1785780915757,
      "tokens": 24063,
      "toolCalls": 7,
      "durationMs": 50822,
      "resultPreview": "{\"reasoning\":\"CONFIRMED. affiliates.spec.ts:160-174 is titled \\\"...or a $0 invoice\\\" but its body has three accrueForPaidInvoice assertions (L164 unreferred, L166 unknown customer, L173 suspended affiliate), each passing amountPaidCents: 10_000. No $0 or floored-to-zero invoice is exercised. A grep of the entire suite shows every amountPaidCents value in affiliates.spec.ts is 10_000 (and the floor…"
    },
    {
      "type": "workflow_agent",
      "index": 33,
      "label": "verify:packages/db/__tests__/affiliates.spec.ts:2",
      "phaseIndex": 2,
      "phaseTitle": "Round 1",
      "agentId": "ac9ed90ed2bd45861",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1785780868754,
      "queuedAt": 1785780626724,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "CONFIRMED",
      "promptPreview": "You are an adversarial verifier trying HARD to REFUTE a claimed defect. \nAFFILIATE PLATFORM SURFACE (Orbetra, a multi-tenant white-label GPS SaaS). Review THESE files/areas:\n- packages/db/prisma/schema.prisma — models Affiliate, Commission, AffiliatePasswordToken; Tenant.referredByAffiliateId\n- packages/db/prisma/migrations/20260803120000_affiliates/migration.sql\n- packages/db/prisma/migrations/20…",
      "lastProgressAt": 1785780912299,
      "tokens": 25744,
      "toolCalls": 8,
      "durationMs": 43545,
      "resultPreview": "{\"verdict\":\"CONFIRMED\",\"reasoning\":\"The claim is accurate. The test at packages/db/__tests__/affiliates.spec.ts:160 is titled \\\"...or a $0 invoice\\\" but its body (L162-173) makes only three assertions — unreferred tenant, unknown customer, suspended affiliate — each passing amountPaidCents: 10_000. There is no $0 or floored-to-zero invoice in the body. Verified by grep: every accrueForPaidInvoice …"
    },
    {
      "type": "workflow_agent",
      "index": 34,
      "label": "find:money-correctness:r2",
      "phaseIndex": 3,
      "phaseTitle": "Round 2",
      "agentId": "ac4dbb5823606eac4",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1785780969295,
      "queuedAt": 1785780969291,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "promptPreview": "You are a hostile, skeptical senior code auditor. \nAFFILIATE PLATFORM SURFACE (Orbetra, a multi-tenant white-label GPS SaaS). Review THESE files/areas:\n- packages/db/prisma/schema.prisma — models Affiliate, Commission, AffiliatePasswordToken; Tenant.referredByAffiliateId\n- packages/db/prisma/migrations/20260803120000_affiliates/migration.sql\n- packages/db/prisma/migrations/20260803160000_affiliate…",
      "lastProgressAt": 1785781198868,
      "tokens": 70159,
      "toolCalls": 12,
      "durationMs": 229573,
      "resultPreview": "{\"findings\":[{\"severity\":\"medium\",\"category\":\"correctness\",\"file\":\"packages/db/src/repos/affiliates.ts\",\"line\":176,\"title\":\"setCommissionStatus has no state-machine or idempotency guard — payout status freely reversible, enabling double payout / un-void\",\"description\":\"setCommissionStatus (L176-180) blindly does `prisma.commission.update({ where:{id}, data:{ status } })` for any target status, wit…"
    },
    {
      "type": "workflow_agent",
      "index": 35,
      "label": "find:auth-security:r2",
      "phaseIndex": 3,
      "phaseTitle": "Round 2",
      "agentId": "aed41dfb16790a23e",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1785780969296,
      "queuedAt": 1785780969291,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "promptPreview": "You are a hostile, skeptical senior code auditor. \nAFFILIATE PLATFORM SURFACE (Orbetra, a multi-tenant white-label GPS SaaS). Review THESE files/areas:\n- packages/db/prisma/schema.prisma — models Affiliate, Commission, AffiliatePasswordToken; Tenant.referredByAffiliateId\n- packages/db/prisma/migrations/20260803120000_affiliates/migration.sql\n- packages/db/prisma/migrations/20260803160000_affiliate…",
      "lastProgressAt": 1785781251536,
      "tokens": 65934,
      "toolCalls": 17,
      "durationMs": 282240,
      "resultPreview": "{\"findings\":[{\"severity\":\"low\",\"category\":\"session-management\",\"file\":\"apps/api/src/routes/partner.ts\",\"line\":58,\"title\":\"Partner password reset does not revoke outstanding partner access tokens\",\"description\":\"The partner access token (mintPartnerToken, partnerJwt.ts L27-29) has a default 8h TTL, no refresh, and no revocation channel. partnerAuth (partner.ts L50-62) validates only signature/exp p…"
    },
    {
      "type": "workflow_agent",
      "index": 36,
      "label": "find:isolation-leak:r2",
      "phaseIndex": 3,
      "phaseTitle": "Round 2",
      "agentId": "a48c27ee50fa9b283",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1785780969296,
      "queuedAt": 1785780969292,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "promptPreview": "You are a hostile, skeptical senior code auditor. \nAFFILIATE PLATFORM SURFACE (Orbetra, a multi-tenant white-label GPS SaaS). Review THESE files/areas:\n- packages/db/prisma/schema.prisma — models Affiliate, Commission, AffiliatePasswordToken; Tenant.referredByAffiliateId\n- packages/db/prisma/migrations/20260803120000_affiliates/migration.sql\n- packages/db/prisma/migrations/20260803160000_affiliate…",
      "lastProgressAt": 1785781197404,
      "tokens": 64844,
      "toolCalls": 19,
      "durationMs": 228108,
      "resultPreview": "{\"findings\":[]}"
    },
    {
      "type": "workflow_agent",
      "index": 37,
      "label": "find:concurrency-idempotency:r2",
      "phaseIndex": 3,
      "phaseTitle": "Round 2",
      "agentId": "adcef02098a374645",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1785780969296,
      "queuedAt": 1785780969292,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "promptPreview": "You are a hostile, skeptical senior code auditor. \nAFFILIATE PLATFORM SURFACE (Orbetra, a multi-tenant white-label GPS SaaS). Review THESE files/areas:\n- packages/db/prisma/schema.prisma — models Affiliate, Commission, AffiliatePasswordToken; Tenant.referredByAffiliateId\n- packages/db/prisma/migrations/20260803120000_affiliates/migration.sql\n- packages/db/prisma/migrations/20260803160000_affiliate…",
      "lastProgressAt": 1785781283868,
      "tokens": 84117,
      "toolCalls": 19,
      "durationMs": 314572,
      "resultPreview": "{\"findings\":[{\"severity\":\"medium\",\"category\":\"ordering\",\"file\":\"packages/db/src/repos/tenants.ts\",\"line\":239,\"title\":\"Per-subscription webhook guard only protects active/trialing tenants — a past_due (grace) subscription is clobbered by a late foreign-sub deletion\",\"description\":\"The audit-P4 per-subscription guard (L231-242) is meant to stop a late-delivered `customer.subscription.deleted` for an…"
    },
    {
      "type": "workflow_agent",
      "index": 38,
      "label": "find:datamodel-migration:r2",
      "phaseIndex": 3,
      "phaseTitle": "Round 2",
      "agentId": "ae94901ebc3098827",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1785780969296,
      "queuedAt": 1785780969292,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "promptPreview": "You are a hostile, skeptical senior code auditor. \nAFFILIATE PLATFORM SURFACE (Orbetra, a multi-tenant white-label GPS SaaS). Review THESE files/areas:\n- packages/db/prisma/schema.prisma — models Affiliate, Commission, AffiliatePasswordToken; Tenant.referredByAffiliateId\n- packages/db/prisma/migrations/20260803120000_affiliates/migration.sql\n- packages/db/prisma/migrations/20260803160000_affiliate…",
      "lastProgressAt": 1785781138069,
      "tokens": 44143,
      "toolCalls": 14,
      "durationMs": 168773,
      "resultPreview": "{\"findings\":[{\"severity\":\"low\",\"category\":\"data-integrity\",\"file\":\"packages/db/src/repos/affiliates.ts\",\"line\":142,\"title\":\"accrueCommission bare catch {} masks any DB fault as a dedupe -> silent underpay\",\"description\":\"accrueCommission (affiliates.ts:141-144) wraps prisma.commission.create in `try { ... } catch { return null }` with a bare catch that swallows EVERY error, not just the unique-vio…"
    },
    {
      "type": "workflow_agent",
      "index": 39,
      "label": "find:coverage-edgecases:r2",
      "phaseIndex": 3,
      "phaseTitle": "Round 2",
      "agentId": "a5223e76a94067769",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1785780969296,
      "queuedAt": 1785780969292,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "promptPreview": "You are a hostile, skeptical senior code auditor. \nAFFILIATE PLATFORM SURFACE (Orbetra, a multi-tenant white-label GPS SaaS). Review THESE files/areas:\n- packages/db/prisma/schema.prisma — models Affiliate, Commission, AffiliatePasswordToken; Tenant.referredByAffiliateId\n- packages/db/prisma/migrations/20260803120000_affiliates/migration.sql\n- packages/db/prisma/migrations/20260803160000_affiliate…",
      "lastProgressAt": 1785781136833,
      "tokens": 79355,
      "toolCalls": 17,
      "durationMs": 167537,
      "resultPreview": "{\"findings\":[{\"severity\":\"medium\",\"category\":\"test-coverage\",\"file\":\"apps/api/src/routes/partner.ts\",\"line\":111,\"title\":\"Sibling set-password-token invalidation has zero test coverage\",\"description\":\"set-password redemption calls invalidatePwTokens (partner.ts:111 → affiliates.ts:198-199) with the comment \\\"burn any sibling outstanding tokens so only the newest link ever worked.\\\" No test in partn…"
    },
    {
      "type": "workflow_agent",
      "index": 40,
      "label": "verify:packages/db/src/repos/affiliates.ts::set:1",
      "phaseIndex": 3,
      "phaseTitle": "Round 2",
      "agentId": "a3c3c745f50847f2b",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1785781283876,
      "queuedAt": 1785781283875,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "CONFIRMED",
      "promptPreview": "You are an adversarial verifier trying HARD to REFUTE a claimed defect. \nAFFILIATE PLATFORM SURFACE (Orbetra, a multi-tenant white-label GPS SaaS). Review THESE files/areas:\n- packages/db/prisma/schema.prisma — models Affiliate, Commission, AffiliatePasswordToken; Tenant.referredByAffiliateId\n- packages/db/prisma/migrations/20260803120000_affiliates/migration.sql\n- packages/db/prisma/migrations/20…",
      "lastProgressAt": 1785781368486,
      "tokens": 30112,
      "toolCalls": 9,
      "durationMs": 84610,
      "resultPreview": "{\"verdict\":\"CONFIRMED\",\"reasoning\":\"All factual assertions verified against real code. affiliates.ts setCommissionStatus (L176-180) does only an existence check then a blind update({data:{status}}) — no current-status check, no allowed-transition table, no per-transition idempotency guard; so pending↔paid and void→paid are all permitted. The route PATCH /v1/commissions/:id (crud.ts L1297-1303) and…"
    },
    {
      "type": "workflow_agent",
      "index": 41,
      "label": "verify:packages/db/src/repos/affiliates.ts::set:2",
      "phaseIndex": 3,
      "phaseTitle": "Round 2",
      "agentId": "af3b711b0737d1405",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1785781283877,
      "queuedAt": 1785781283875,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "PLAUSIBLE",
      "promptPreview": "You are an adversarial verifier trying HARD to REFUTE a claimed defect. \nAFFILIATE PLATFORM SURFACE (Orbetra, a multi-tenant white-label GPS SaaS). Review THESE files/areas:\n- packages/db/prisma/schema.prisma — models Affiliate, Commission, AffiliatePasswordToken; Tenant.referredByAffiliateId\n- packages/db/prisma/migrations/20260803120000_affiliates/migration.sql\n- packages/db/prisma/migrations/20…",
      "lastProgressAt": 1785781381093,
      "tokens": 29534,
      "toolCalls": 11,
      "durationMs": 97216,
      "resultPreview": "{\"verdict\":\"PLAUSIBLE\",\"reasoning\":\"Every factual assertion in the claim is confirmed against the code. affiliates.ts:176-180 setCommissionStatus does an unconditional prisma.commission.update to any target status after only a null-existence check — no current-status read, no transition table. The API layer adds no guard: commissionStatusUpdateSchema (entities.ts:263) only validates the enum, and …"
    },
    {
      "type": "workflow_agent",
      "index": 42,
      "label": "verify:packages/shared/src/entities.ts::commiss:1",
      "phaseIndex": 3,
      "phaseTitle": "Round 2",
      "agentId": "a581870d8012cb154",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1785781283883,
      "queuedAt": 1785781283883,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Verified against actual code. entities.ts:245 defines commi…",
      "promptPreview": "You are an adversarial verifier trying HARD to REFUTE a claimed defect. \nAFFILIATE PLATFORM SURFACE (Orbetra, a multi-tenant white-label GPS SaaS). Review THESE files/areas:\n- packages/db/prisma/schema.prisma — models Affiliate, Commission, AffiliatePasswordToken; Tenant.referredByAffiliateId\n- packages/db/prisma/migrations/20260803120000_affiliates/migration.sql\n- packages/db/prisma/migrations/20…",
      "lastProgressAt": 1785781337513,
      "tokens": 34469,
      "toolCalls": 5,
      "durationMs": 53630,
      "resultPreview": "{\"reasoning\":\"Verified against actual code. entities.ts:245 defines commissionPctSchema = z.number().min(0).max(100) with no multipleOf/decimal-places constraint, used by affiliateCreate (line 252) and affiliateUpdate (line 259). The DB column is DECIMAL(5,2) (migration 20260803120000_affiliates/migration.sql L16; schema.prisma:132 @db.Decimal(5,2)). Postgres silently rounds scale overflow on NUME…"
    },
    {
      "type": "workflow_agent",
      "index": 43,
      "label": "verify:packages/shared/src/entities.ts::commiss:2",
      "phaseIndex": 3,
      "phaseTitle": "Round 2",
      "agentId": "ad04d8a33d8ee4f06",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1785781283883,
      "queuedAt": 1785781283883,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "CONFIRMED",
      "promptPreview": "You are an adversarial verifier trying HARD to REFUTE a claimed defect. \nAFFILIATE PLATFORM SURFACE (Orbetra, a multi-tenant white-label GPS SaaS). Review THESE files/areas:\n- packages/db/prisma/schema.prisma — models Affiliate, Commission, AffiliatePasswordToken; Tenant.referredByAffiliateId\n- packages/db/prisma/migrations/20260803120000_affiliates/migration.sql\n- packages/db/prisma/migrations/20…",
      "lastProgressAt": 1785781346309,
      "tokens": 26587,
      "toolCalls": 7,
      "durationMs": 62426,
      "resultPreview": "{\"verdict\":\"CONFIRMED\",\"reasoning\":\"Every link in the claim is grounded in real code. (1) packages/shared/src/entities.ts:245 defines commissionPctSchema = z.number().min(0).max(100) with NO .multipleOf(0.01) or step constraint, so 15.555 / 33.335 pass validation. (2) migration 20260803120000_affiliates/migration.sql declares \\\"commissionPct\\\" DECIMAL(5,2) — Postgres silently ROUNDS a 3-decimal va…"
    },
    {
      "type": "workflow_agent",
      "index": 44,
      "label": "verify:apps/api/src/routes/crud.ts::spec-mandat:1",
      "phaseIndex": 3,
      "phaseTitle": "Round 2",
      "agentId": "a825fa736925b9aab",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1785781283886,
      "queuedAt": 1785781283886,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "CONFIRMED",
      "promptPreview": "You are an adversarial verifier trying HARD to REFUTE a claimed defect. \nAFFILIATE PLATFORM SURFACE (Orbetra, a multi-tenant white-label GPS SaaS). Review THESE files/areas:\n- packages/db/prisma/schema.prisma — models Affiliate, Commission, AffiliatePasswordToken; Tenant.referredByAffiliateId\n- packages/db/prisma/migrations/20260803120000_affiliates/migration.sql\n- packages/db/prisma/migrations/20…",
      "lastProgressAt": 1785781367780,
      "tokens": 30115,
      "toolCalls": 14,
      "durationMs": 83894,
      "resultPreview": "{\"verdict\":\"CONFIRMED\",\"reasoning\":\"The spec-mandated self-referral guard is genuinely absent from the money path, and the claim is grounded in the actual code.\\n\\nSPEC MANDATE (real): PROJECT_PLAN.md line 230 (§6.9 affiliate section) states verbatim: \\\"Self-referral guard: affiliate cannot refer a tenant whose admin email domain matches the affiliate's.\\\" This is normative. (Minor mischaracteriza…"
    },
    {
      "type": "workflow_agent",
      "index": 45,
      "label": "verify:apps/api/src/routes/crud.ts::spec-mandat:2",
      "phaseIndex": 3,
      "phaseTitle": "Round 2",
      "agentId": "a92357065dc610e25",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1785781283886,
      "queuedAt": 1785781283886,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "CONFIRMED",
      "promptPreview": "You are an adversarial verifier trying HARD to REFUTE a claimed defect. \nAFFILIATE PLATFORM SURFACE (Orbetra, a multi-tenant white-label GPS SaaS). Review THESE files/areas:\n- packages/db/prisma/schema.prisma — models Affiliate, Commission, AffiliatePasswordToken; Tenant.referredByAffiliateId\n- packages/db/prisma/migrations/20260803120000_affiliates/migration.sql\n- packages/db/prisma/migrations/20…",
      "lastProgressAt": 1785781422936,
      "tokens": 35506,
      "toolCalls": 15,
      "durationMs": 139050,
      "resultPreview": "{\"verdict\":\"CONFIRMED\",\"reasoning\":\"The spec-mandated self-referral guard is genuinely absent from the money path, and self-referred commission accrual can occur automatically.\\n\\nEVIDENCE FOR THE MANDATE. PROJECT_PLAN.md §6.9 line 230 states verbatim: \\\"Self-referral guard: affiliate cannot refer a tenant whose admin email domain matches the affiliate's.\\\" §6.9 is the v1 affiliate module spec (li…"
    },
    {
      "type": "workflow_agent",
      "index": 46,
      "label": "verify:apps/api/src/routes/partner.ts::partner :1",
      "phaseIndex": 3,
      "phaseTitle": "Round 2",
      "agentId": "ad0bdeb38cf07cfeb",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1785781283888,
      "queuedAt": 1785781283888,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "CONFIRMED",
      "promptPreview": "You are an adversarial verifier trying HARD to REFUTE a claimed defect. \nAFFILIATE PLATFORM SURFACE (Orbetra, a multi-tenant white-label GPS SaaS). Review THESE files/areas:\n- packages/db/prisma/schema.prisma — models Affiliate, Commission, AffiliatePasswordToken; Tenant.referredByAffiliateId\n- packages/db/prisma/migrations/20260803120000_affiliates/migration.sql\n- packages/db/prisma/migrations/20…",
      "lastProgressAt": 1785781381824,
      "tokens": 35257,
      "toolCalls": 8,
      "durationMs": 97936,
      "resultPreview": "{\"verdict\":\"CONFIRMED\",\"reasoning\":\"The claimed mechanic is exactly what the code does. affiliates.ts:183-185 setPassword only overwrites passwordHash and writes no revocation marker; partner.ts:50-62 partnerAuth checks only signature/exp (partnerJwt.ts:31-38) plus status==='active', with no iat/revocation/password-generation check; the Affiliate model (schema.prisma:125-145) has no passwordChange…"
    },
    {
      "type": "workflow_agent",
      "index": 47,
      "label": "verify:apps/api/src/routes/partner.ts::partner :2",
      "phaseIndex": 3,
      "phaseTitle": "Round 2",
      "agentId": "a727bc4f55678b4f0",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1785781283888,
      "queuedAt": 1785781283888,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "CONFIRMED",
      "promptPreview": "You are an adversarial verifier trying HARD to REFUTE a claimed defect. \nAFFILIATE PLATFORM SURFACE (Orbetra, a multi-tenant white-label GPS SaaS). Review THESE files/areas:\n- packages/db/prisma/schema.prisma — models Affiliate, Commission, AffiliatePasswordToken; Tenant.referredByAffiliateId\n- packages/db/prisma/migrations/20260803120000_affiliates/migration.sql\n- packages/db/prisma/migrations/20…",
      "lastProgressAt": 1785781389690,
      "tokens": 36362,
      "toolCalls": 8,
      "durationMs": 105802,
      "resultPreview": "{\"verdict\":\"CONFIRMED\",\"reasoning\":\"All factual claims verified against the actual code. The partner access token is a stateless HS256 JWT with an 8h default TTL and no refresh (partnerJwt.ts L27-29; partner.ts L37 DEFAULT_TTL_S). partnerAuth (partner.ts L50-62) validates only signature/exp plus a status==='active' recheck; it performs no iat/revocation check — verifyPartnerToken (partnerJwt.ts L3…"
    },
    {
      "type": "workflow_agent",
      "index": 48,
      "label": "verify:packages/db/src/repos/tenants.ts::per-su:1",
      "phaseIndex": 3,
      "phaseTitle": "Round 2",
      "agentId": "a2be4a0f96d371513",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1785781337513,
      "queuedAt": 1785781283889,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "CONFIRMED",
      "promptPreview": "You are an adversarial verifier trying HARD to REFUTE a claimed defect. \nAFFILIATE PLATFORM SURFACE (Orbetra, a multi-tenant white-label GPS SaaS). Review THESE files/areas:\n- packages/db/prisma/schema.prisma — models Affiliate, Commission, AffiliatePasswordToken; Tenant.referredByAffiliateId\n- packages/db/prisma/migrations/20260803120000_affiliates/migration.sql\n- packages/db/prisma/migrations/20…",
      "lastProgressAt": 1785781448026,
      "tokens": 33240,
      "toolCalls": 10,
      "durationMs": 110512,
      "resultPreview": "{\"verdict\":\"CONFIRMED\",\"reasoning\":\"Real, reachable defect. The per-subscription P4 guard in packages/db/src/repos/tenants.ts (L231-242) decides whether a tenant has \\\"a live sub to protect\\\" using the clause at L239: `{ subscriptionStatus: { notIn: ['active','trialing'] } }`. That set is strictly narrower than the two modules that consume subscription state: billing.ts `hasLiveSubscription`/`RESU…"
    },
    {
      "type": "workflow_agent",
      "index": 49,
      "label": "verify:packages/db/src/repos/tenants.ts::per-su:2",
      "phaseIndex": 3,
      "phaseTitle": "Round 2",
      "agentId": "a9dfc836833f6a886",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1785781346309,
      "queuedAt": 1785781283889,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "CONFIRMED",
      "promptPreview": "You are an adversarial verifier trying HARD to REFUTE a claimed defect. \nAFFILIATE PLATFORM SURFACE (Orbetra, a multi-tenant white-label GPS SaaS). Review THESE files/areas:\n- packages/db/prisma/schema.prisma — models Affiliate, Commission, AffiliatePasswordToken; Tenant.referredByAffiliateId\n- packages/db/prisma/migrations/20260803120000_affiliates/migration.sql\n- packages/db/prisma/migrations/20…",
      "lastProgressAt": 1785781447684,
      "tokens": 39474,
      "toolCalls": 7,
      "durationMs": 101374,
      "resultPreview": "{\"verdict\":\"CONFIRMED\",\"reasoning\":\"The per-subscription guard in packages/db/src/repos/tenants.ts L239 defines \\\"a live sub to protect\\\" as subscriptionStatus IN ['active','trialing'] (its complement, notIn, is the pass-clause). This is strictly narrower than the two modules that consume subscription state: plans.ts L64/L87-88 grants the FULL entitlement matrix for past_due (LAPSED_SUBSCRIPTION_S…"
    },
    {
      "type": "workflow_agent",
      "index": 50,
      "label": "verify:packages/db/src/repos/tenants.ts::monoto:1",
      "phaseIndex": 3,
      "phaseTitle": "Round 2",
      "agentId": "a98cef2714f87e71a",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1785781367780,
      "queuedAt": 1785781283889,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "The code property is exactly as described and is NOT handle…",
      "promptPreview": "You are an adversarial verifier trying HARD to REFUTE a claimed defect. \nAFFILIATE PLATFORM SURFACE (Orbetra, a multi-tenant white-label GPS SaaS). Review THESE files/areas:\n- packages/db/prisma/schema.prisma — models Affiliate, Commission, AffiliatePasswordToken; Tenant.referredByAffiliateId\n- packages/db/prisma/migrations/20260803120000_affiliates/migration.sql\n- packages/db/prisma/migrations/20…",
      "lastProgressAt": 1785781467590,
      "tokens": 38326,
      "toolCalls": 8,
      "durationMs": 99810,
      "resultPreview": "{\"reasoning\":\"The code property is exactly as described and is NOT handled anywhere. tenants.ts:230 gates the write with strict `lt` on `lastBillingEventAt` (no tiebreaker), tenants.ts:255 writes `lastBillingEventAt: eventAt`, and billing.ts:237 derives eventAt from `new Date(event.created * 1000)` where Stripe's `event.created` is one-second granularity. The comment at tenants.ts:212-214 explicit…"
    },
    {
      "type": "workflow_agent",
      "index": 51,
      "label": "verify:packages/db/src/repos/tenants.ts::monoto:2",
      "phaseIndex": 3,
      "phaseTitle": "Round 2",
      "agentId": "a48028f6130a5066c",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1785781368487,
      "queuedAt": 1785781283889,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "CONFIRMED. The code at tenants.ts:230 gates writes with str…",
      "promptPreview": "You are an adversarial verifier trying HARD to REFUTE a claimed defect. \nAFFILIATE PLATFORM SURFACE (Orbetra, a multi-tenant white-label GPS SaaS). Review THESE files/areas:\n- packages/db/prisma/schema.prisma — models Affiliate, Commission, AffiliatePasswordToken; Tenant.referredByAffiliateId\n- packages/db/prisma/migrations/20260803120000_affiliates/migration.sql\n- packages/db/prisma/migrations/20…",
      "lastProgressAt": 1785781490182,
      "tokens": 39345,
      "toolCalls": 7,
      "durationMs": 121694,
      "resultPreview": "{\"reasoning\":\"CONFIRMED. The code at tenants.ts:230 gates writes with strict `lt` (`lastBillingEventAt: { lt: eventAt }`) and writes `lastBillingEventAt: eventAt` (L255), where eventAt = new Date(event.created * 1000) (billing.ts:237). Stripe's event.created is second-granular and the code itself uses it as the sole ordering key (comment L228) with no secondary tiebreak. The guard's design explici…"
    },
    {
      "type": "workflow_agent",
      "index": 52,
      "label": "verify:packages/db/src/repos/affiliates.ts::acc:1",
      "phaseIndex": 3,
      "phaseTitle": "Round 2",
      "agentId": "ae45fdd71dc529341",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1785781381098,
      "queuedAt": 1785781283889,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "PLAUSIBLE",
      "promptPreview": "You are an adversarial verifier trying HARD to REFUTE a claimed defect. \nAFFILIATE PLATFORM SURFACE (Orbetra, a multi-tenant white-label GPS SaaS). Review THESE files/areas:\n- packages/db/prisma/schema.prisma — models Affiliate, Commission, AffiliatePasswordToken; Tenant.referredByAffiliateId\n- packages/db/prisma/migrations/20260803120000_affiliates/migration.sql\n- packages/db/prisma/migrations/20…",
      "lastProgressAt": 1785781437742,
      "tokens": 21453,
      "toolCalls": 5,
      "durationMs": 56642,
      "resultPreview": "{\"verdict\":\"PLAUSIBLE\",\"reasoning\":\"The bare catch is real and verified: accrueCommission (packages/db/src/repos/affiliates.ts:141-144) wraps prisma.commission.create in `try { ... } catch { return null }`, swallowing EVERY error, not just the unique violation on sourceInvoiceId. The sibling accrueForPaidInvoice (L165-172) does handle this correctly, rethrowing non-unique-violation errors (`if (is…"
    },
    {
      "type": "workflow_agent",
      "index": 53,
      "label": "verify:packages/db/src/repos/affiliates.ts::acc:2",
      "phaseIndex": 3,
      "phaseTitle": "Round 2",
      "agentId": "aa5f6e81f4a52f2c6",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1785781381825,
      "queuedAt": 1785781283889,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "PLAUSIBLE",
      "promptPreview": "You are an adversarial verifier trying HARD to REFUTE a claimed defect. \nAFFILIATE PLATFORM SURFACE (Orbetra, a multi-tenant white-label GPS SaaS). Review THESE files/areas:\n- packages/db/prisma/schema.prisma — models Affiliate, Commission, AffiliatePasswordToken; Tenant.referredByAffiliateId\n- packages/db/prisma/migrations/20260803120000_affiliates/migration.sql\n- packages/db/prisma/migrations/20…",
      "lastProgressAt": 1785781438068,
      "tokens": 22906,
      "toolCalls": 5,
      "durationMs": 56243,
      "resultPreview": "{\"verdict\":\"PLAUSIBLE\",\"reasoning\":\"The code fact is verified and real: accrueCommission (packages/db/src/repos/affiliates.ts:137-145) wraps prisma.commission.create in `try { ... } catch { return null }` — a bare catch that masks EVERY error (deadlock, connection reset, any constraint fault) as an idempotent dedupe. This is objectively inferior to the sibling accrueForPaidInvoice (L165-172), whic…"
    },
    {
      "type": "workflow_agent",
      "index": 54,
      "label": "verify:packages/db/src/repos/affiliates.ts::no :1",
      "phaseIndex": 3,
      "phaseTitle": "Round 2",
      "agentId": "ae9a0eb0b1566f64f",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1785781389691,
      "queuedAt": 1785781283889,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Verified against actual code. affiliates.ts:157 runs prisma…",
      "promptPreview": "You are an adversarial verifier trying HARD to REFUTE a claimed defect. \nAFFILIATE PLATFORM SURFACE (Orbetra, a multi-tenant white-label GPS SaaS). Review THESE files/areas:\n- packages/db/prisma/schema.prisma — models Affiliate, Commission, AffiliatePasswordToken; Tenant.referredByAffiliateId\n- packages/db/prisma/migrations/20260803120000_affiliates/migration.sql\n- packages/db/prisma/migrations/20…",
      "lastProgressAt": 1785781488609,
      "tokens": 28266,
      "toolCalls": 6,
      "durationMs": 98918,
      "resultPreview": "{\"reasoning\":\"Verified against actual code. affiliates.ts:157 runs prisma.commission.findFirst({ where:{affiliateId,tenantId}, orderBy:{createdAt:'asc'} }) synchronously inside accrueForPaidInvoice, which billing.ts invokes on every invoice.payment_succeeded webhook. The only indexes on commissions (migration 20260803120000 L45/L48 and schema.prisma L189) are unique(sourceInvoiceId) and @@index([a…"
    },
    {
      "type": "workflow_agent",
      "index": 55,
      "label": "verify:packages/db/src/repos/affiliates.ts::no :2",
      "phaseIndex": 3,
      "phaseTitle": "Round 2",
      "agentId": "a21569ee49f1c3cd9",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1785781422936,
      "queuedAt": 1785781283889,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "CONFIRMED. The query at affiliates.ts:157 (prisma.commissio…",
      "promptPreview": "You are an adversarial verifier trying HARD to REFUTE a claimed defect. \nAFFILIATE PLATFORM SURFACE (Orbetra, a multi-tenant white-label GPS SaaS). Review THESE files/areas:\n- packages/db/prisma/schema.prisma — models Affiliate, Commission, AffiliatePasswordToken; Tenant.referredByAffiliateId\n- packages/db/prisma/migrations/20260803120000_affiliates/migration.sql\n- packages/db/prisma/migrations/20…",
      "lastProgressAt": 1785781490877,
      "tokens": 25454,
      "toolCalls": 4,
      "durationMs": 67940,
      "resultPreview": "{\"reasoning\":\"CONFIRMED. The query at affiliates.ts:157 (prisma.commission.findFirst where {affiliateId, tenantId} orderBy createdAt asc) runs on the synchronous invoice.payment_succeeded webhook via accrueForPaidInvoice. The commissions table (migration 20260803120000_affiliates) has only: PK(id), UNIQUE(sourceInvoiceId), and INDEX(affiliateId, status). Postgres does NOT auto-index FK columns, so…"
    },
    {
      "type": "workflow_agent",
      "index": 56,
      "label": "verify:apps/api/src/routes/partner.ts::sibling :1",
      "phaseIndex": 3,
      "phaseTitle": "Round 2",
      "agentId": "aa964992f9968d311",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1785781437743,
      "queuedAt": 1785781283889,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Verified by reading the actual code and both relevant test …",
      "promptPreview": "You are an adversarial verifier trying HARD to REFUTE a claimed defect. \nAFFILIATE PLATFORM SURFACE (Orbetra, a multi-tenant white-label GPS SaaS). Review THESE files/areas:\n- packages/db/prisma/schema.prisma — models Affiliate, Commission, AffiliatePasswordToken; Tenant.referredByAffiliateId\n- packages/db/prisma/migrations/20260803120000_affiliates/migration.sql\n- packages/db/prisma/migrations/20…",
      "lastProgressAt": 1785781491528,
      "tokens": 40978,
      "toolCalls": 8,
      "durationMs": 53785,
      "resultPreview": "{\"reasoning\":\"Verified by reading the actual code and both relevant test files. The sibling-burn behavior lives at partner.ts:111 (invalidatePwTokens call) and affiliates.ts:198-199 (updateMany over usedAt:null tokens for the affiliate). It is the ONLY mechanism that kills a still-outstanding sibling set-password token after another is redeemed; consumePwToken only single-uses the token actually r…"
    },
    {
      "type": "workflow_agent",
      "index": 57,
      "label": "verify:apps/api/src/routes/partner.ts::sibling :2",
      "phaseIndex": 3,
      "phaseTitle": "Round 2",
      "agentId": "afba93e3198fad222",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1785781438068,
      "queuedAt": 1785781283890,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "CONFIRMED",
      "promptPreview": "You are an adversarial verifier trying HARD to REFUTE a claimed defect. \nAFFILIATE PLATFORM SURFACE (Orbetra, a multi-tenant white-label GPS SaaS). Review THESE files/areas:\n- packages/db/prisma/schema.prisma — models Affiliate, Commission, AffiliatePasswordToken; Tenant.referredByAffiliateId\n- packages/db/prisma/migrations/20260803120000_affiliates/migration.sql\n- packages/db/prisma/migrations/20…",
      "lastProgressAt": 1785781502198,
      "tokens": 31582,
      "toolCalls": 8,
      "durationMs": 64130,
      "resultPreview": "{\"verdict\":\"CONFIRMED\",\"reasoning\":\"The test-coverage gap is real and precisely as described. partner.ts:111 calls db.affiliates.invalidatePwTokens(affiliateId, now) — affiliates.ts:198-199 — to burn sibling outstanding set-password tokens. This is genuinely separate behavior from consumePwToken (affiliates.ts:189-196), which atomically claims ONLY the matching tokenHash and leaves other tokens (d…"
    },
    {
      "type": "workflow_agent",
      "index": 58,
      "label": "verify:apps/api/src/routes/partner.ts::partner :1",
      "phaseIndex": 3,
      "phaseTitle": "Round 2",
      "agentId": "aeaa0516969255ed9",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1785781447685,
      "queuedAt": 1785781283890,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Verified against the actual code and the full test suite. p…",
      "promptPreview": "You are an adversarial verifier trying HARD to REFUTE a claimed defect. \nAFFILIATE PLATFORM SURFACE (Orbetra, a multi-tenant white-label GPS SaaS). Review THESE files/areas:\n- packages/db/prisma/schema.prisma — models Affiliate, Commission, AffiliatePasswordToken; Tenant.referredByAffiliateId\n- packages/db/prisma/migrations/20260803120000_affiliates/migration.sql\n- packages/db/prisma/migrations/20…",
      "lastProgressAt": 1785781492255,
      "tokens": 29134,
      "toolCalls": 5,
      "durationMs": 44570,
      "resultPreview": "{\"reasoning\":\"Verified against the actual code and the full test suite. partner.ts:78-93 implements a partner-specific brute-force limiter (hardcoded LOGIN_RL_MAX=10, lockKey partner:fail:${ip}:${sha256(email).slice(0,16)}, RL_SCRIPT INCR/EXPIRE on failure, Retry-After 429 gate, del(lockKey) reset on success) plus a second set-password limiter (REDEEM_RL_MAX=30, line 101). partner.spec.ts exercise…"
    },
    {
      "type": "workflow_agent",
      "index": 59,
      "label": "verify:apps/api/src/routes/partner.ts::partner :2",
      "phaseIndex": 3,
      "phaseTitle": "Round 2",
      "agentId": "a84c95abdaa039c07",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1785781448027,
      "queuedAt": 1785781283890,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "CONFIRMED",
      "promptPreview": "You are an adversarial verifier trying HARD to REFUTE a claimed defect. \nAFFILIATE PLATFORM SURFACE (Orbetra, a multi-tenant white-label GPS SaaS). Review THESE files/areas:\n- packages/db/prisma/schema.prisma — models Affiliate, Commission, AffiliatePasswordToken; Tenant.referredByAffiliateId\n- packages/db/prisma/migrations/20260803120000_affiliates/migration.sql\n- packages/db/prisma/migrations/20…",
      "lastProgressAt": 1785781493644,
      "tokens": 29371,
      "toolCalls": 5,
      "durationMs": 45617,
      "resultPreview": "{\"verdict\":\"CONFIRMED\",\"reasoning\":\"The partner login/set-password brute-force limiters in apps/api/src/routes/partner.ts are governed by their OWN hardcoded thresholds (LOGIN_RL_MAX=10 at line 79/43, REDEEM_RL_MAX=30 at line 101/44), independent of the app's lockout:{maxFails:100} config (that config drives the tenant login.ts limiter). partner.spec.ts is the only partner test file and it never e…"
    },
    {
      "type": "workflow_agent",
      "index": 60,
      "label": "verify:apps/api/src/routes/crud.ts::commission :1",
      "phaseIndex": 3,
      "phaseTitle": "Round 2",
      "agentId": "a67b73ddf0674ab87",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1785781467591,
      "queuedAt": 1785781283890,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "CONFIRMED",
      "promptPreview": "You are an adversarial verifier trying HARD to REFUTE a claimed defect. \nAFFILIATE PLATFORM SURFACE (Orbetra, a multi-tenant white-label GPS SaaS). Review THESE files/areas:\n- packages/db/prisma/schema.prisma — models Affiliate, Commission, AffiliatePasswordToken; Tenant.referredByAffiliateId\n- packages/db/prisma/migrations/20260803120000_affiliates/migration.sql\n- packages/db/prisma/migrations/20…",
      "lastProgressAt": 1785781522331,
      "tokens": 25478,
      "toolCalls": 8,
      "durationMs": 54740,
      "resultPreview": "{\"verdict\":\"CONFIRMED\",\"reasoning\":\"Every factual claim in the finding is verified against the real code. commissionStatusSchema (packages/shared/src/entities.ts:243) is z.enum(['pending','paid','void']), so 'void' is a valid API input to PATCH /v1/commissions/:id. setCommissionStatus (packages/db/src/repos/affiliates.ts:176-179) forwards any status value directly into prisma.commission.update({ d…"
    },
    {
      "type": "workflow_agent",
      "index": 61,
      "label": "verify:apps/api/src/routes/crud.ts::commission :2",
      "phaseIndex": 3,
      "phaseTitle": "Round 2",
      "agentId": "a4c28b26c4d8030af",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1785781488610,
      "queuedAt": 1785781283890,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "CONFIRMED",
      "promptPreview": "You are an adversarial verifier trying HARD to REFUTE a claimed defect. \nAFFILIATE PLATFORM SURFACE (Orbetra, a multi-tenant white-label GPS SaaS). Review THESE files/areas:\n- packages/db/prisma/schema.prisma — models Affiliate, Commission, AffiliatePasswordToken; Tenant.referredByAffiliateId\n- packages/db/prisma/migrations/20260803120000_affiliates/migration.sql\n- packages/db/prisma/migrations/20…",
      "lastProgressAt": 1785781555093,
      "tokens": 25333,
      "toolCalls": 10,
      "durationMs": 66483,
      "resultPreview": "{\"verdict\":\"CONFIRMED\",\"reasoning\":\"Every factual assertion is grounded in the actual code. 'void' is a live, intended API capability: commissionStatusSchema (entities.ts:243) and commissionStatusUpdateSchema (263) accept it; setCommissionStatus (affiliates.ts:176-179) writes it verbatim; it is reachable via PATCH /v1/commissions/:id, whose own comment (crud.ts:1294) documents \\\"PATCH marks one pa…"
    },
    {
      "type": "workflow_agent",
      "index": 62,
      "label": "find:money-correctness:r3",
      "phaseIndex": 4,
      "phaseTitle": "Round 3",
      "agentId": "a3aa6780fecdf0c8c",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1785781555097,
      "queuedAt": 1785781555096,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "promptPreview": "You are a hostile, skeptical senior code auditor. \nAFFILIATE PLATFORM SURFACE (Orbetra, a multi-tenant white-label GPS SaaS). Review THESE files/areas:\n- packages/db/prisma/schema.prisma — models Affiliate, Commission, AffiliatePasswordToken; Tenant.referredByAffiliateId\n- packages/db/prisma/migrations/20260803120000_affiliates/migration.sql\n- packages/db/prisma/migrations/20260803160000_affiliate…",
      "lastProgressAt": 1785781919497,
      "tokens": 88770,
      "toolCalls": 17,
      "durationMs": 364400,
      "resultPreview": "{\"findings\":[{\"severity\":\"medium\",\"category\":\"correctness\",\"file\":\"packages/db/src/repos/affiliates.ts\",\"line\":159,\"title\":\"Commission terms are read live, not snapshotted — editing commissionMonths reopens a legitimately-closed commission window and over-pays\",\"description\":\"accrueForPaidInvoice computes the window boundary as addMonthsUtc(anchor, affiliate.commissionMonths) and the amount as flo…"
    },
    {
      "type": "workflow_agent",
      "index": 63,
      "label": "find:auth-security:r3",
      "phaseIndex": 4,
      "phaseTitle": "Round 3",
      "agentId": "abaab596c56671d7f",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1785781555098,
      "queuedAt": 1785781555096,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "promptPreview": "You are a hostile, skeptical senior code auditor. \nAFFILIATE PLATFORM SURFACE (Orbetra, a multi-tenant white-label GPS SaaS). Review THESE files/areas:\n- packages/db/prisma/schema.prisma — models Affiliate, Commission, AffiliatePasswordToken; Tenant.referredByAffiliateId\n- packages/db/prisma/migrations/20260803120000_affiliates/migration.sql\n- packages/db/prisma/migrations/20260803160000_affiliate…",
      "lastProgressAt": 1785781956092,
      "tokens": 83568,
      "toolCalls": 18,
      "durationMs": 400994,
      "resultPreview": "{\"findings\":[{\"severity\":\"low\",\"category\":\"auth-token-lifecycle\",\"file\":\"apps/api/src/routes/partner.ts\",\"line\":142,\"title\":\"Set-password tokens are never revoked on re-issue or on suspension — superseded/post-offboarding links stay redeemable\",\"description\":\"Outstanding partner set-password tokens are only invalidated AFTER a successful redeem (partner.ts L111 invalidatePwTokens, called inside th…"
    },
    {
      "type": "workflow_agent",
      "index": 64,
      "label": "find:isolation-leak:r3",
      "phaseIndex": 4,
      "phaseTitle": "Round 3",
      "agentId": "a51b02c5950837334",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1785781555098,
      "queuedAt": 1785781555096,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "promptPreview": "You are a hostile, skeptical senior code auditor. \nAFFILIATE PLATFORM SURFACE (Orbetra, a multi-tenant white-label GPS SaaS). Review THESE files/areas:\n- packages/db/prisma/schema.prisma — models Affiliate, Commission, AffiliatePasswordToken; Tenant.referredByAffiliateId\n- packages/db/prisma/migrations/20260803120000_affiliates/migration.sql\n- packages/db/prisma/migrations/20260803160000_affiliate…",
      "lastProgressAt": 1785781853340,
      "tokens": 76703,
      "toolCalls": 22,
      "durationMs": 298242,
      "resultPreview": "{\"findings\":[{\"severity\":\"medium\",\"category\":\"data-leakage\",\"file\":\"apps/api/src/routes/crud.ts\",\"line\":1282,\"title\":\"PATCH /v1/affiliates/:id serializes the raw Affiliate row, leaking passwordHash (distinct handler from the already-reported GET leak)\",\"description\":\"An earlier round flagged the GET affiliate routes (list / get) leaking the partner argon2id passwordHash. The same leak exists on a …"
    },
    {
      "type": "workflow_agent",
      "index": 65,
      "label": "find:concurrency-idempotency:r3",
      "phaseIndex": 4,
      "phaseTitle": "Round 3",
      "agentId": "a033a4f1f927d6fa8",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1785781555098,
      "queuedAt": 1785781555096,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "promptPreview": "You are a hostile, skeptical senior code auditor. \nAFFILIATE PLATFORM SURFACE (Orbetra, a multi-tenant white-label GPS SaaS). Review THESE files/areas:\n- packages/db/prisma/schema.prisma — models Affiliate, Commission, AffiliatePasswordToken; Tenant.referredByAffiliateId\n- packages/db/prisma/migrations/20260803120000_affiliates/migration.sql\n- packages/db/prisma/migrations/20260803160000_affiliate…",
      "lastProgressAt": 1785781971103,
      "tokens": 79793,
      "toolCalls": 14,
      "durationMs": 416005,
      "resultPreview": "{\"findings\":[{\"severity\":\"low\",\"category\":\"concurrency-atomicity\",\"file\":\"apps/api/src/routes/partner.ts\",\"line\":107,\"title\":\"Partner set-password sequence (consume → setPassword → invalidate siblings) is not transactional; a partial failure leaves older invite tokens live\",\"description\":\"The set-password handler performs three independent, separately-awaited repo calls with no wrapping transactio…"
    },
    {
      "type": "workflow_agent",
      "index": 66,
      "label": "find:datamodel-migration:r3",
      "phaseIndex": 4,
      "phaseTitle": "Round 3",
      "agentId": "a2b7e9cca30c1ec21",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1785781555098,
      "queuedAt": 1785781555096,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "promptPreview": "You are a hostile, skeptical senior code auditor. \nAFFILIATE PLATFORM SURFACE (Orbetra, a multi-tenant white-label GPS SaaS). Review THESE files/areas:\n- packages/db/prisma/schema.prisma — models Affiliate, Commission, AffiliatePasswordToken; Tenant.referredByAffiliateId\n- packages/db/prisma/migrations/20260803120000_affiliates/migration.sql\n- packages/db/prisma/migrations/20260803160000_affiliate…",
      "lastProgressAt": 1785781766708,
      "tokens": 47949,
      "toolCalls": 19,
      "durationMs": 211610,
      "resultPreview": "{\"findings\":[{\"severity\":\"medium\",\"category\":\"data-model\",\"file\":\"packages/db/src/repos/affiliates.ts\",\"line\":115,\"title\":\"Referral code lookup treats the user-supplied `_` as a SQL LIKE wildcard → mis-attribution to the wrong affiliate\",\"description\":\"`getActiveByCode` resolves an inbound ?ref value with Prisma `{ equals: code, mode: 'insensitive' }`. I verified against the live tika Postgres (Pr…"
    },
    {
      "type": "workflow_agent",
      "index": 67,
      "label": "find:coverage-edgecases:r3",
      "phaseIndex": 4,
      "phaseTitle": "Round 3",
      "agentId": "a556f4df3f4d6d4a6",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1785781555099,
      "queuedAt": 1785781555096,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "promptPreview": "You are a hostile, skeptical senior code auditor. \nAFFILIATE PLATFORM SURFACE (Orbetra, a multi-tenant white-label GPS SaaS). Review THESE files/areas:\n- packages/db/prisma/schema.prisma — models Affiliate, Commission, AffiliatePasswordToken; Tenant.referredByAffiliateId\n- packages/db/prisma/migrations/20260803120000_affiliates/migration.sql\n- packages/db/prisma/migrations/20260803160000_affiliate…",
      "lastProgressAt": 1785781770003,
      "tokens": 89457,
      "toolCalls": 17,
      "durationMs": 214904,
      "resultPreview": "{\"findings\":[{\"severity\":\"medium\",\"category\":\"test-coverage\",\"file\":\"packages/db/__tests__/affiliates.spec.ts\",\"line\":129,\"title\":\"Recurring in-window accrual (months 2..N) is never tested — every accrual test uses the FIRST payment only\",\"description\":\"The whole point of the commissionMonths window anchored on the tenant's first payment (affiliates.ts L154-159) is that the 2nd..Nth invoice inside…"
    },
    {
      "type": "workflow_agent",
      "index": 68,
      "label": "verify:packages/db/src/repos/affiliates.ts::com:1",
      "phaseIndex": 4,
      "phaseTitle": "Round 3",
      "agentId": "addd824c4ed6748d0",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1785781971107,
      "queuedAt": 1785781971107,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "CONFIRMED",
      "promptPreview": "You are an adversarial verifier trying HARD to REFUTE a claimed defect. \nAFFILIATE PLATFORM SURFACE (Orbetra, a multi-tenant white-label GPS SaaS). Review THESE files/areas:\n- packages/db/prisma/schema.prisma — models Affiliate, Commission, AffiliatePasswordToken; Tenant.referredByAffiliateId\n- packages/db/prisma/migrations/20260803120000_affiliates/migration.sql\n- packages/db/prisma/migrations/20…",
      "lastProgressAt": 1785782029293,
      "tokens": 28772,
      "toolCalls": 6,
      "durationMs": 58185,
      "resultPreview": "{\"verdict\":\"CONFIRMED\",\"reasoning\":\"The defect is real and reproducible from the actual code. accrueForPaidInvoice reads BOTH commission terms live from the current Affiliate row: the window boundary is addMonthsUtc(anchor, affiliate.commissionMonths) (affiliates.ts:159) and the amount is floor(amountPaidCents * affiliate.commissionPct / 100) (affiliates.ts:160). The anchor is the FIRST existing c…"
    },
    {
      "type": "workflow_agent",
      "index": 69,
      "label": "verify:packages/db/src/repos/affiliates.ts::com:2",
      "phaseIndex": 4,
      "phaseTitle": "Round 3",
      "agentId": "a6f84bb5a117d798d",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1785781971108,
      "queuedAt": 1785781971107,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "CONFIRMED",
      "promptPreview": "You are an adversarial verifier trying HARD to REFUTE a claimed defect. \nAFFILIATE PLATFORM SURFACE (Orbetra, a multi-tenant white-label GPS SaaS). Review THESE files/areas:\n- packages/db/prisma/schema.prisma — models Affiliate, Commission, AffiliatePasswordToken; Tenant.referredByAffiliateId\n- packages/db/prisma/migrations/20260803120000_affiliates/migration.sql\n- packages/db/prisma/migrations/20…",
      "lastProgressAt": 1785782028770,
      "tokens": 28721,
      "toolCalls": 5,
      "durationMs": 57662,
      "resultPreview": "{\"verdict\":\"CONFIRMED\",\"reasoning\":\"Grounded in read code. accrueForPaidInvoice (packages/db/src/repos/affiliates.ts:159-160) reads BOTH commission terms live from the current Affiliate row every invoice: window boundary = addMonthsUtc(anchor, affiliate.commissionMonths) and amount = floor(amountPaidCents * commissionPct / 100). The anchor (L157-158) is the FIRST commission's createdAt and is neve…"
    },
    {
      "type": "workflow_agent",
      "index": 70,
      "label": "verify:apps/api/src/routes/partner.ts::set-pass:1",
      "phaseIndex": 4,
      "phaseTitle": "Round 3",
      "agentId": "ad9ca63c6e65209c3",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1785781971112,
      "queuedAt": 1785781971112,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "CONFIRMED",
      "promptPreview": "You are an adversarial verifier trying HARD to REFUTE a claimed defect. \nAFFILIATE PLATFORM SURFACE (Orbetra, a multi-tenant white-label GPS SaaS). Review THESE files/areas:\n- packages/db/prisma/schema.prisma — models Affiliate, Commission, AffiliatePasswordToken; Tenant.referredByAffiliateId\n- packages/db/prisma/migrations/20260803120000_affiliates/migration.sql\n- packages/db/prisma/migrations/20…",
      "lastProgressAt": 1785782092738,
      "tokens": 33567,
      "toolCalls": 5,
      "durationMs": 121626,
      "resultPreview": "{\"verdict\":\"CONFIRMED\",\"reasoning\":\"Both factual pillars hold in the actual code. (a) issuePartnerSetPwToken (partner.ts L142-146) only INSERTs via createPwToken; nothing invalidates prior unused tokens on re-issue. invalidatePwTokens runs only AFTER a successful redeem (L111), so multiple 24h tokens coexist pre-redeem, each independently redeemable (consumePwToken matches tokenHash/usedAt:null/ex…"
    },
    {
      "type": "workflow_agent",
      "index": 71,
      "label": "verify:apps/api/src/routes/partner.ts::set-pass:2",
      "phaseIndex": 4,
      "phaseTitle": "Round 3",
      "agentId": "a1d3820810275e4c2",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1785781971112,
      "queuedAt": 1785781971112,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Both mechanisms are directly evidenced in real code. (a) is…",
      "promptPreview": "You are an adversarial verifier trying HARD to REFUTE a claimed defect. \nAFFILIATE PLATFORM SURFACE (Orbetra, a multi-tenant white-label GPS SaaS). Review THESE files/areas:\n- packages/db/prisma/schema.prisma — models Affiliate, Commission, AffiliatePasswordToken; Tenant.referredByAffiliateId\n- packages/db/prisma/migrations/20260803120000_affiliates/migration.sql\n- packages/db/prisma/migrations/20…",
      "lastProgressAt": 1785782050389,
      "tokens": 35000,
      "toolCalls": 7,
      "durationMs": 79277,
      "resultPreview": "{\"reasoning\":\"Both mechanisms are directly evidenced in real code. (a) issuePartnerSetPwToken (partner.ts L142-146) only calls createPwToken and never invalidatePwTokens; the admin route (crud.ts L1287-1293) adds no pre-invalidate. consumePwToken (affiliates.ts L189-197) matches ANY unused/unexpired token, so multiple concurrently-issued tokens are each independently redeemable until 24h expiry or…"
    },
    {
      "type": "workflow_agent",
      "index": 72,
      "label": "verify:apps/api/src/routes/crud.ts::patch /v1/a:1",
      "phaseIndex": 4,
      "phaseTitle": "Round 3",
      "agentId": "ad324412ee83a01cb",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1785781971115,
      "queuedAt": 1785781971114,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "CONFIRMED",
      "promptPreview": "You are an adversarial verifier trying HARD to REFUTE a claimed defect. \nAFFILIATE PLATFORM SURFACE (Orbetra, a multi-tenant white-label GPS SaaS). Review THESE files/areas:\n- packages/db/prisma/schema.prisma — models Affiliate, Commission, AffiliatePasswordToken; Tenant.referredByAffiliateId\n- packages/db/prisma/migrations/20260803120000_affiliates/migration.sql\n- packages/db/prisma/migrations/20…",
      "lastProgressAt": 1785782020207,
      "tokens": 29804,
      "toolCalls": 10,
      "durationMs": 49092,
      "resultPreview": "{\"verdict\":\"CONFIRMED\",\"reasoning\":\"The defect is real and reproducible.\\n\\nEvidence chain:\\n1. PATCH handler (apps/api/src/routes/crud.ts:1278-1284): `const row = await db.affiliates.update({ userId: auth(c).userId }, id(c), data)` then `return ... json(c, row)`. This is a distinct handler from the GET list (1250-1251) and GET :id (1252-1256) handlers.\\n2. Repo update (packages/db/src/repos/affil…"
    },
    {
      "type": "workflow_agent",
      "index": 73,
      "label": "verify:apps/api/src/routes/crud.ts::patch /v1/a:2",
      "phaseIndex": 4,
      "phaseTitle": "Round 3",
      "agentId": "a55d68777a7765167",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1785781971115,
      "queuedAt": 1785781971114,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "CONFIRMED",
      "promptPreview": "You are an adversarial verifier trying HARD to REFUTE a claimed defect. \nAFFILIATE PLATFORM SURFACE (Orbetra, a multi-tenant white-label GPS SaaS). Review THESE files/areas:\n- packages/db/prisma/schema.prisma — models Affiliate, Commission, AffiliatePasswordToken; Tenant.referredByAffiliateId\n- packages/db/prisma/migrations/20260803120000_affiliates/migration.sql\n- packages/db/prisma/migrations/20…",
      "lastProgressAt": 1785782024339,
      "tokens": 32497,
      "toolCalls": 9,
      "durationMs": 53224,
      "resultPreview": "{\"verdict\":\"CONFIRMED\",\"reasoning\":\"The PATCH /v1/affiliates/:id handler (apps/api/src/routes/crud.ts:1278-1284) returns json(c, row) where row is the raw Prisma Affiliate model from AffiliateRepo.update (packages/db/src/repos/affiliates.ts:132-136), which calls prisma.affiliate.update({where:{id},data}) with NO select — so every column is returned, including passwordHash (confirmed as a column on…"
    },
    {
      "type": "workflow_agent",
      "index": 74,
      "label": "verify:apps/api/src/routes/partner.ts::partner :1",
      "phaseIndex": 4,
      "phaseTitle": "Round 3",
      "agentId": "a0fbbaa8676185093",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1785781971117,
      "queuedAt": 1785781971117,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "CONFIRMED",
      "promptPreview": "You are an adversarial verifier trying HARD to REFUTE a claimed defect. \nAFFILIATE PLATFORM SURFACE (Orbetra, a multi-tenant white-label GPS SaaS). Review THESE files/areas:\n- packages/db/prisma/schema.prisma — models Affiliate, Commission, AffiliatePasswordToken; Tenant.referredByAffiliateId\n- packages/db/prisma/migrations/20260803120000_affiliates/migration.sql\n- packages/db/prisma/migrations/20…",
      "lastProgressAt": 1785782048397,
      "tokens": 27984,
      "toolCalls": 4,
      "durationMs": 77280,
      "resultPreview": "{\"verdict\":\"CONFIRMED\",\"reasoning\":\"The code matches the claim exactly. In apps/api/src/routes/partner.ts:100-114 the set-password handler runs three independent, separately-awaited repo calls with no wrapping transaction: consumePwToken (L107), setPassword (L109), invalidatePwTokens (L111). In packages/db/src/repos/affiliates.ts these map to three distinct auto-committed Prisma statements (setPas…"
    },
    {
      "type": "workflow_agent",
      "index": 75,
      "label": "verify:apps/api/src/routes/partner.ts::partner :2",
      "phaseIndex": 4,
      "phaseTitle": "Round 3",
      "agentId": "a06e9b38b21e7e812",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1785781971117,
      "queuedAt": 1785781971117,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "PLAUSIBLE",
      "promptPreview": "You are an adversarial verifier trying HARD to REFUTE a claimed defect. \nAFFILIATE PLATFORM SURFACE (Orbetra, a multi-tenant white-label GPS SaaS). Review THESE files/areas:\n- packages/db/prisma/schema.prisma — models Affiliate, Commission, AffiliatePasswordToken; Tenant.referredByAffiliateId\n- packages/db/prisma/migrations/20260803120000_affiliates/migration.sql\n- packages/db/prisma/migrations/20…",
      "lastProgressAt": 1785782076073,
      "tokens": 27985,
      "toolCalls": 4,
      "durationMs": 104956,
      "resultPreview": "{\"verdict\":\"PLAUSIBLE\",\"reasoning\":\"The mechanical claim is accurate and verifiable in the real code. In apps/api/src/routes/partner.ts the set-password handler makes three independent, separately-awaited repo calls with no wrapping transaction: consumePwToken (L107), setPassword (L109), invalidatePwTokens (L111). In packages/db/src/repos/affiliates.ts these map to three distinct Prisma statements…"
    },
    {
      "type": "workflow_agent",
      "index": 76,
      "label": "verify:packages/db/src/repos/affiliates.ts::ref:1",
      "phaseIndex": 4,
      "phaseTitle": "Round 3",
      "agentId": "a3e3f10923ba2e5e9",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1785782020208,
      "queuedAt": 1785781971120,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "CONFIRMED",
      "promptPreview": "You are an adversarial verifier trying HARD to REFUTE a claimed defect. \nAFFILIATE PLATFORM SURFACE (Orbetra, a multi-tenant white-label GPS SaaS). Review THESE files/areas:\n- packages/db/prisma/schema.prisma — models Affiliate, Commission, AffiliatePasswordToken; Tenant.referredByAffiliateId\n- packages/db/prisma/migrations/20260803120000_affiliates/migration.sql\n- packages/db/prisma/migrations/20…",
      "lastProgressAt": 1785782215814,
      "tokens": 39619,
      "toolCalls": 20,
      "durationMs": 195606,
      "resultPreview": "{\"verdict\":\"CONFIRMED\",\"reasoning\":\"I reproduced the defect end-to-end against a live Postgres (timescaledb-ha:pg16) with the repo's real Prisma 6.19.3 client and applied migrations, capturing the emitted SQL.\\n\\nEvery link in the chain holds:\\n\\n1. Input is unescaped and allows `_`. All ref-attribution schemas in packages/shared/src/entities.ts permit underscore: pilotRequest `ref` = `/^[a-zA-Z0-…"
    },
    {
      "type": "workflow_agent",
      "index": 77,
      "label": "verify:packages/db/src/repos/affiliates.ts::ref:2",
      "phaseIndex": 4,
      "phaseTitle": "Round 3",
      "agentId": "a49b76461af706321",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1785782024339,
      "queuedAt": 1785781971120,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "CONFIRMED",
      "promptPreview": "You are an adversarial verifier trying HARD to REFUTE a claimed defect. \nAFFILIATE PLATFORM SURFACE (Orbetra, a multi-tenant white-label GPS SaaS). Review THESE files/areas:\n- packages/db/prisma/schema.prisma — models Affiliate, Commission, AffiliatePasswordToken; Tenant.referredByAffiliateId\n- packages/db/prisma/migrations/20260803120000_affiliates/migration.sql\n- packages/db/prisma/migrations/20…",
      "lastProgressAt": 1785782231009,
      "tokens": 43512,
      "toolCalls": 20,
      "durationMs": 206670,
      "resultPreview": "{\"verdict\":\"CONFIRMED\",\"reasoning\":\"Empirically reproduced against a freshly-migrated Postgres using the repo's own Prisma client (6.19.3). The filter at packages/db/src/repos/affiliates.ts:115 (`{ code: { equals: code, mode: 'insensitive' } }`) compiles to `WHERE \\\"affiliates\\\".\\\"code\\\" ILIKE $1` with the value bound UNESCAPED (captured query log: PARAMS [\\\"AUTUMN_\\\",\\\"active\\\",1,0]) — not LOWER(…"
    },
    {
      "type": "workflow_agent",
      "index": 78,
      "label": "verify:packages/db/__tests__/affiliates.spec.ts:1",
      "phaseIndex": 4,
      "phaseTitle": "Round 3",
      "agentId": "a20cd5f344e1cb4e1",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1785782028770,
      "queuedAt": 1785781971120,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "CONFIRMED as a real test-coverage gap. The recurring accrua…",
      "promptPreview": "You are an adversarial verifier trying HARD to REFUTE a claimed defect. \nAFFILIATE PLATFORM SURFACE (Orbetra, a multi-tenant white-label GPS SaaS). Review THESE files/areas:\n- packages/db/prisma/schema.prisma — models Affiliate, Commission, AffiliatePasswordToken; Tenant.referredByAffiliateId\n- packages/db/prisma/migrations/20260803120000_affiliates/migration.sql\n- packages/db/prisma/migrations/20…",
      "lastProgressAt": 1785782089063,
      "tokens": 34126,
      "toolCalls": 7,
      "durationMs": 60293,
      "resultPreview": "{\"reasoning\":\"CONFIRMED as a real test-coverage gap. The recurring accrual feature genuinely exists: accrueForPaidInvoice (packages/db/src/repos/affiliates.ts L146-173) anchors the commissionMonths window on the FIRST commission's createdAt (L157-159), and the commissions table's only unique constraint is on sourceInvoiceId (migration 20260803120000_affiliates L45; schema.prisma L185 `sourceInvoic…"
    },
    {
      "type": "workflow_agent",
      "index": 79,
      "label": "verify:packages/db/__tests__/affiliates.spec.ts:2",
      "phaseIndex": 4,
      "phaseTitle": "Round 3",
      "agentId": "a4e8508b24f958f99",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1785782029293,
      "queuedAt": 1785781971120,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Confirmed real test-coverage gap. I enumerated every accrue…",
      "promptPreview": "You are an adversarial verifier trying HARD to REFUTE a claimed defect. \nAFFILIATE PLATFORM SURFACE (Orbetra, a multi-tenant white-label GPS SaaS). Review THESE files/areas:\n- packages/db/prisma/schema.prisma — models Affiliate, Commission, AffiliatePasswordToken; Tenant.referredByAffiliateId\n- packages/db/prisma/migrations/20260803120000_affiliates/migration.sql\n- packages/db/prisma/migrations/20…",
      "lastProgressAt": 1785782094507,
      "tokens": 33749,
      "toolCalls": 7,
      "durationMs": 65214,
      "resultPreview": "{\"reasoning\":\"Confirmed real test-coverage gap. I enumerated every accrueForPaidInvoice/accrueCommission caller in apps/ and packages/. Each is either (a) the FIRST commission for the (affiliate,tenant) pair (affiliates.spec L76,120,136,156; billing.spec L353; partner.spec L113), (b) a byte-identical DUPLICATE of that same invoice id (affiliates.spec L80 in_123, L125 in_win_1; billing.spec L360 in…"
    },
    {
      "type": "workflow_agent",
      "index": 80,
      "label": "verify:packages/db/__tests__/affiliates.spec.ts:1",
      "phaseIndex": 4,
      "phaseTitle": "Round 3",
      "agentId": "a55e56d033f0763ff",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1785782048397,
      "queuedAt": 1785781971120,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "CONFIRMED. I ran the exact expression from affiliates.ts:16…",
      "promptPreview": "You are an adversarial verifier trying HARD to REFUTE a claimed defect. \nAFFILIATE PLATFORM SURFACE (Orbetra, a multi-tenant white-label GPS SaaS). Review THESE files/areas:\n- packages/db/prisma/schema.prisma — models Affiliate, Commission, AffiliatePasswordToken; Tenant.referredByAffiliateId\n- packages/db/prisma/migrations/20260803120000_affiliates/migration.sql\n- packages/db/prisma/migrations/20…",
      "lastProgressAt": 1785782096816,
      "tokens": 25710,
      "toolCalls": 6,
      "durationMs": 48419,
      "resultPreview": "{\"reasoning\":\"CONFIRMED. I ran the exact expression from affiliates.ts:160 with the test's inputs (amountPaidCents=10000, commissionPct=33.33): 10000*33.33/100 evaluates to exactly 3333.0 (toPrecision(20) = 3333.0000000000000000, zero floating-point residual), so Math.floor, Math.round, Math.ceil and Math.trunc all return 3333. The test at affiliates.spec.ts:150 titled 'floors a fractional commiss…"
    },
    {
      "type": "workflow_agent",
      "index": 81,
      "label": "verify:packages/db/__tests__/affiliates.spec.ts:2",
      "phaseIndex": 4,
      "phaseTitle": "Round 3",
      "agentId": "a177304620b4ce96b",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1785782050391,
      "queuedAt": 1785781971120,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "CONFIRMED",
      "promptPreview": "You are an adversarial verifier trying HARD to REFUTE a claimed defect. \nAFFILIATE PLATFORM SURFACE (Orbetra, a multi-tenant white-label GPS SaaS). Review THESE files/areas:\n- packages/db/prisma/schema.prisma — models Affiliate, Commission, AffiliatePasswordToken; Tenant.referredByAffiliateId\n- packages/db/prisma/migrations/20260803120000_affiliates/migration.sql\n- packages/db/prisma/migrations/20…",
      "lastProgressAt": 1785782099604,
      "tokens": 26376,
      "toolCalls": 8,
      "durationMs": 49213,
      "resultPreview": "{\"verdict\":\"CONFIRMED\",\"reasoning\":\"The claim is accurate and verified against real code.\\n\\nProduction code (packages/db/src/repos/affiliates.ts:160):\\n`const amountCents = Math.floor((invoice.amountPaidCents * Number(affiliate.commissionPct)) / 100)`\\n\\nTest (packages/db/__tests__/affiliates.spec.ts:150-158) named \\\"floors a fractional commission rate in the platform's favour\\\" feeds commissionP…"
    },
    {
      "type": "workflow_agent",
      "index": 82,
      "label": "verify:apps/api/__tests__/billing.spec.ts::paid:1",
      "phaseIndex": 4,
      "phaseTitle": "Round 3",
      "agentId": "a40bf21a07f68f8ce",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1785782076073,
      "queuedAt": 1785781971120,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Confirmed by reading the actual code. billing.ts::paidInvoi…",
      "promptPreview": "You are an adversarial verifier trying HARD to REFUTE a claimed defect. \nAFFILIATE PLATFORM SURFACE (Orbetra, a multi-tenant white-label GPS SaaS). Review THESE files/areas:\n- packages/db/prisma/schema.prisma — models Affiliate, Commission, AffiliatePasswordToken; Tenant.referredByAffiliateId\n- packages/db/prisma/migrations/20260803120000_affiliates/migration.sql\n- packages/db/prisma/migrations/20…",
      "lastProgressAt": 1785782124787,
      "tokens": 40903,
      "toolCalls": 7,
      "durationMs": 48714,
      "resultPreview": "{\"reasoning\":\"Confirmed by reading the actual code. billing.ts::paidInvoiceFrom (L197-204) has three null-return branches (empty/missing customer, empty/missing invoice id, amount_paid<=0) and a currency default ('eur'). The function is private (grep shows it is only referenced in billing.ts, never exported), so it is reachable only through POST /v1/webhooks/stripe. The single webhook-invoice test…"
    },
    {
      "type": "workflow_agent",
      "index": 83,
      "label": "verify:apps/api/__tests__/billing.spec.ts::paid:2",
      "phaseIndex": 4,
      "phaseTitle": "Round 3",
      "agentId": "ae4dda2a97f71afeb",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1785782089063,
      "queuedAt": 1785781971120,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "CONFIRMED",
      "promptPreview": "You are an adversarial verifier trying HARD to REFUTE a claimed defect. \nAFFILIATE PLATFORM SURFACE (Orbetra, a multi-tenant white-label GPS SaaS). Review THESE files/areas:\n- packages/db/prisma/schema.prisma — models Affiliate, Commission, AffiliatePasswordToken; Tenant.referredByAffiliateId\n- packages/db/prisma/migrations/20260803120000_affiliates/migration.sql\n- packages/db/prisma/migrations/20…",
      "lastProgressAt": 1785782131902,
      "tokens": 34234,
      "toolCalls": 6,
      "durationMs": 42839,
      "resultPreview": "{\"verdict\":\"CONFIRMED\",\"reasoning\":\"Verified against the real code. billing.ts::paidInvoiceFrom (L197-204) has null-return guards for missing/empty customer (L198), missing/empty invoice id (L199), and amount_paid<=0 (L200,L202), plus a currency default to 'eur' (L201). The webhook test helper invoiceEvent (billing.spec L85-88) hardcodes a complete object and every call site (L353, L360, L367) pas…"
    },
    {
      "type": "workflow_agent",
      "index": 84,
      "label": "synthesize",
      "phaseIndex": 5,
      "phaseTitle": "Synthesize",
      "agentId": "aaa5e98f2973c2ab9",
      "model": "claude-opus-4-8",
      "state": "done",
      "startedAt": 1785782231025,
      "queuedAt": 1785782231011,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "The affiliate/partner platform is functionally complete and…",
      "promptPreview": "You are the lead auditor writing the final report on the affiliate platform. 29 defects survived adversarial verification across 3 rounds:\n\n#1 [medium] packages/db/src/repos/affiliates.ts:157 Commission window anchor drifts later on suspend->reactivate -> overpay past commissionMonths\n  The commissionMonths window is anchored on the earliest EXISTING commission ROW for (affiliate, tenant): anchor …",
      "lastProgressAt": 1785782430660,
      "tokens": 77253,
      "toolCalls": 1,
      "durationMs": 199635,
      "resultPreview": "{\"summary\":\"The affiliate/partner platform is functionally complete and its money-critical happy paths (single-payment accrual, per-invoice idempotency on sourceInvoiceId, no double-pay) are sound and tested. But the audit surfaced a consistent theme of missing durability and defensive rigor around money and identity: the commission ledger is not treated as an immutable financial record (it cascad…"
    }
  ],
  "totalTokens": 3444619,
  "totalToolCalls": 797
}
---

## REMEDIATION PLAN (decided 2026-08-03, to apply after the site PR ships)

### HIGH-1 — commission ledger must survive tenant/affiliate deletion
Both `commissions` FKs are `ON DELETE CASCADE`, so `DELETE /v1/tenants/:id` silently destroys paid and
owed commission rows (unrecoverable financial trail).
**Fix:** new migration recreating both FKs as `ON DELETE RESTRICT`; `tenants.remove` catches the FK
violation and throws `TenantHasCommissionsError` → 409, mirroring the existing
`AccountHasUsersError` precedent. Ledger rows then cannot be orphaned or erased.

### HIGH-2 — referral-code lookup must be exact, not ILIKE
`getActiveByCode` uses Prisma `mode:'insensitive'` → `ILIKE` with the value bound UNESCAPED, over a
case-SENSITIVE unique index. Two money-misrouting bugs: `_` in a `?ref` acts as a single-char
wildcard (`?ref=AUTUMN_` matches `AUTUMN1`), and `promo`/`PROMO` can coexist with a nondeterministic
`findFirst`.
**Fix:** `CREATE UNIQUE INDEX affiliates_code_lower_key ON affiliates (lower(code))` + look up with
real equality on `lower(code)`; add an explicit `orderBy` for determinism.

### MED (same pass)
- **Snapshot the terms** on `Commission` (`ratePct`, `baseAmountCents`) per PROJECT_PLAN §6.9, and pin
  the window end at attribution — today editing an affiliate's `commissionPct`/`commissionMonths`
  re-prices history and can reopen a closed window.
- **Anchor the window on the real first payment** (persist `firstPaidAt`) instead of the earliest
  commission row's DB insert time (clock drift + a floored-to-zero first invoice extend the window).
- **Self-referral guard** (§6.9, currently absent): refuse attribution when the tenant admin's email
  domain matches the affiliate's.

---

## REMEDIATION STATUS (2026-08-04)

| Finding | Status |
|---|---|
| **HIGH-2 — ILIKE referral lookup** | ✅ **FIXED** (PR #141). Exact `lower(code) = lower($1)` + functional unique index `affiliates_code_lower_key`; `_` removed from both ref schemas. Escalated to ship-blocking because the new public signup made attribution anonymously reachable. Tests: wildcard-hijack + case-duplicate. |
| **HIGH-1 — commission ledger cascade-deleted** | ✅ **FIXED**. Both `commissions` FKs are now `ON DELETE RESTRICT`; `tenants.remove` counts commissions first and throws `TenantHasCommissionsError` → **409 `tenant_has_commissions`** (mirrors the accounts-with-users guard). Test asserts the ledger survives a delete attempt. |
| **MED — terms read live, not snapshotted** | ✅ **FIXED**. `Commission` gained `ratePct` + `baseAmountCents` (§6.9); accrual snapshots the rate, so editing an affiliate never re-prices history. Test: raising the rate afterwards leaves the earned row untouched. |
| **MED — window anchored on the DB insert time** | ✅ **FIXED**. `Commission.paidAt` records Stripe's clock and the window anchors on the earliest `paidAt` (falling back to `createdAt` for pre-existing rows), so webhook lag can no longer extend the earning window. Test: a payment 7 months after the first payment is refused even when the row was inserted moments ago. |
| **MED — self-referral guard absent** | ✅ **FIXED**. Public signup drops the attribution when the signup email's domain matches the affiliate's (§6.9). The tenant is still created — it simply earns nobody a commission. Test covers both the self and the unrelated-domain case. |

Remaining from the original 29: the lower-severity items (refund/chargeback → `void` reversal has no consumer yet; a few coverage gaps). Not ship-blocking; the money-critical set is closed.
