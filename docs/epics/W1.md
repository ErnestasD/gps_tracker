# W1 Epic Plan — E01 Skeleton & first byte (+E02-1 pulled in)

**Source:** IMPLEMENTATION_PLAN.md E01-1…E01-6, E02-1 · PROJECT_PLAN §8 W1
**Epic exit:** a founder's real car produces a row in `positions`.
**Status:** approved plan altitude — per-story plans written just-in-time before each story.

## Story order & lanes (Appendix D)
```
E01-1 scaffold+CI ──┬── E01-2 infra (compose/Ansible/Photon)   [lane B]
                    └── E01-4 codec (parser+dictionaries+corpus) [lane A, serial-only fixtures]
E01-3 DB layer (after E01-1; can overlap E01-4)
E02-1 simulator v0 (after E01-4) → THEN E01-5 ingest TCP (needs simulator for tests)
E01-6 first real device (needs E01-2,3,5 + E00-1 hardware)
```
Max 2 lanes in W1 (founder review capacity). `packages/codec/__fixtures__` and
`packages/db/sql` changes never run in a parallel lane.

## In-epic decisions that unblock later epics (record as ADR/audit docs)
1. **E01-4:** npm parser choice — evaluate `complete-teltonika-parser` vs
   `teltonika-codec-parser` against the golden corpus in the first hour; wrap the winner.
2. **E01-4/E02-1:** simulator encode engine — verify Go `alim-zanibekov/teltonika` license;
   else TS encoder in packages/codec. → ADR.
3. **E01-3:** TimescaleDB compressed-chunk insert verification on pinned version →
   `docs/audit/ts-compressed-insert.md` (gates E04-2 recompute path).

## Human (E00) dependencies on the critical path
- E00-1 devices+SIMs → blocks E01-6 (order now if not done).
- E00-2 Hetzner+staging → blocks E01-2 staging AC (local compose AC proceeds without it);
  AWS SES production request — submit now, W5 needs it.
- E00-4 Telegram bot — W5, not urgent but free to do now.
- CI host resolved: GitHub origin (see E01-1-plan.md).

## Per-story status (updated 2026-07-04)
| Story | Plan | State |
|---|---|---|
| E01-1 | docs/epics/E01-1-plan.md | **MERGED** (main CI green; demo-PR red AC captured; hooks live) |
| E01-2 | docs/epics/E01-2-plan.md | **MERGED** PR #5 (compose up + smoke locally; staging blocked on E00-2) |
| E01-3 | docs/epics/E01-3-plan.md | **MERGED** PR #4 (R8-2 verified: TS 2.28.2 compressed-inserts OK) |
| E01-4 | docs/epics/E01-4-plan.md | **MERGED** PR #2 (88 tests, cov 97/96; ADR-010..013) |
| E02-1 | docs/epics/E02-1-plan.md | **MERGED** PR #3 (8 tests; CLI `pnpm sim`) |
| E01-5 | docs/epics/E01-5-plan.md | **MERGED** PR #7 (11 e2e tests vs real simulator) |
| E01-6 | just-in-time | ONLY W1 STORY LEFT — needs real device + staging (E00-1/E00-2, human) |

E02-2 (W2 lane, pulled forward): **MERGED** PR #6.

## W2 status (updated 2026-07-07)
| Story | State |
|---|---|
| E02-3 worker pipeline | **MERGED** PR #8 (I1–I3 + chaos vs real ingest/simulator; 16 tests) |
| E02-4 live state + WS | **MERGED** PR #9 (ws-ticket single-use, cross-tenant isolation tested) |
| E02-5 metrics + dashboard | **MERGED** PR #10 (frozen names, Grafana JSON, api entrypoint) |
| E02-6 web shell | **MERGED** PR #12+#13 (plan docs/epics/E02-6-plan.md; 500-dev AC measured p99 9.4 ms; Playwright web-e2e in CI; +fix: worker idle lease starvation) |
| E02-7 invalid-fix e2e | NEXT (S) — trail rendering + fixValid per point already in place |
| E02-8 real-device flood | needs hardware (E00-1) |

